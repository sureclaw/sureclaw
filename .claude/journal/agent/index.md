# Agent

Agent process: runner, tools, prompt builder, MCP server, tool catalog.

## Entries

- 2026-04-22 10:00 — Extend `reqId` binding to hot-path runners (`pi-session`, `claude-code`) [runner.md](runner.md)
- 2026-04-22 09:45 — Bind `reqId` on runner's top-level logger from `AX_REQUEST_ID` (Task 2 of chat-correlation-id plan) [runner.md](runner.md)
- 2026-04-21 09:03 — Tool CLI shims Task 10: replace `execute_script`-oriented hints in `ToolCatalogModule` with CLI-shim teaching — new `### Calling catalog tools` section shows flags + `| jq` projection, `--stdin-args` for complex JSON, and `<shim> --help` for schema lookup; dropped the `ax.callTool` / `{ select: '.x[]' }` lines; tests scoped the "no retired concepts" assertion to the appended usage note only (catalog one-liner render still emits `_select?` per tool until Task 4.2 knob is retired — out of Task 10's scope); 12/12 module tests green, build clean [prompt.md](prompt.md)
- 2026-04-21 09:10 — Tool CLI shims Task 9: install `tool` CLI in agent Docker image; new `scripts/add-shebang.mjs` post-build helper (stdlib-only) prepends `#!/usr/bin/env node` + chmods 0755 on `dist/cli/tool/index.js` so `tsc`'s shebang-stripping doesn't break the shim; `package.json` build chain = `tsc && node scripts/add-shebang.mjs ...`; `container/agent/Dockerfile` adds a block between the `ax` user creation and `USER ax` that `mkdir -p /opt/ax/tools/bin`, `ln -sf /opt/ax/dist/cli/tool/index.js /opt/ax/tools/bin/tool`, `chmod +x`, `chown -R ax:ax /opt/ax/tools`, then `ENV PATH="/opt/ax/tools/bin:${PATH}"`; verified via `head -1` + `ls -la` + symlink-as-`tool` runtime smoke (prints `0.1.0`); no Docker rebuild (deferred to Task 13 E2E) [tool-cli.md](tool-cli.md)
- 2026-04-21 08:55 — Tool CLI shims Task 8: wire symlink farm into agent runners (pi-session + claude-code); extracted `trySyncShims(opts, logger)` helper in `src/agent/tool-shims.ts` that resolves `shimDir` from `opts → AX_SHIM_DIR env → /opt/ax/tools/bin` and `toolBin` from `opts → join(shimDir, 'tool')`, checks both exist (missing → info + skip), wraps `syncToolShims` in try/catch (throw → warn, never throws); called right before tool-definition construction in both runners from `(config.catalog ?? []).map(t => t.name)`; 6 new helper tests (happy path, missing shimDir, missing toolBin, thrown-error swallow, AX_SHIM_DIR env default, hardcoded-default skip), 11/11 tool-shims tests green, 448/448 agent tests green, build clean; no new AgentConfig field; env var is escape hatch for tests [tool-cli.md](tool-cli.md)
- 2026-04-21 08:47 — Tool CLI shims Task 7: symlink farm module `src/agent/tool-shims.ts` exposes `syncToolShims({shimDir, toolBin, names})` — idempotent reconciler that validates names against `/^[a-zA-Z_][a-zA-Z0-9_]*$/` (PATH-entry safety, tighter than a typical identifier check), removes stale symlinks, leaves non-symlink files untouched, and skips recreating existing links (mtime stable); 5 tests green, `npm run build` clean; not wired into runners yet (Task 8), not installed in Dockerfile yet (Task 9) [tool-cli.md](tool-cli.md)
- 2026-04-21 08:44 — Tool CLI shims Task 6: `<shim> --help` routes to `tool describe <shim>` — added a `--help` short-circuit at the top of `callTool()` that reuses the existing `describeCommand()` helper so shim `--help` output matches `tool describe` formatting; exact-token match only (not `-h`, not `--help=...`); other flags ignored when `--help` is present (standard CLI UX); 1 new test in the existing `argv[0] implicit call dispatch` block reusing its mock server (handles both `describe_tools` and `call_tool`); 19/19 tool CLI tests green; build clean [tool-cli.md](tool-cli.md)
- 2026-04-21 08:38 — Tool CLI shims Task 5: busybox-style argv[0] dispatch for shim symlinks; extended `RunOptions` with `argv0?: string` (default `'tool'`), non-'tool' argv0 triggers implicit call mode where argv0 IS the tool name and argv is the call args; refactored existing `callCommand(argv, opts)` into shared `callTool(name, argv, opts)` helper so explicit `tool call <name> ...` and argv[0]-dispatch share one code path; `index.ts` now computes `basename(process.argv[1] ?? 'tool')` and passes through; 3 new tests (flags, no-args, tool-fallthrough), 26/26 tool CLI tests green; build clean [tool-cli.md](tool-cli.md)
- 2026-04-21 08:30 — Tool CLI shims Task 4: implement `tool call <name> [--flags]` subcommand; new `flags.ts` parser handles `--k=v`/`--k v`/bare `--flag` with numeric/boolean/JSON-ish coercion and rejects positional args; `--stdin-args` piped-JSON mode for complex payloads; unwraps `{result:...}` envelope before stdout so `jq` sees the payload directly; maps response `{error, kind}` to exit-1 + stderr; `index.ts` now collects stdin via `for await (process.stdin)` gated on `!isTTY`; applied Task 3 review nits (describe rejects unknown flags, pretty-prints JSON); 20/20 tool CLI tests green (7 flags + 13 main); build clean [tool-cli.md](tool-cli.md)
- 2026-04-21 08:26 — Tool CLI shims Task 3: implement `tool describe <names...>` subcommand; POSTs `{action: 'describe_tools', names}`, errors with `at least one tool name required` when no positional given; positional extractor filters `--` flags for forward-compat; applied two Task 2 review nits in-flight — extracted `HUMAN_NAME_COL = 40` constant and added `AbortSignal.timeout(30_000)` to `ipc.ts` fetch; 9/9 tests green (6 prior + 3 new); build clean [tool-cli.md](tool-cli.md)
- 2026-04-21 08:22 — Tool CLI shims Task 2: implement `tool list` subcommand + `ipcCall` HTTP helper; POSTs `describe_tools` with empty names, prints JSON by default or `name.padEnd(40) + summary` with `--human`; stdlib-only (Node `fetch` + `http.createServer` in tests); 3 new tests + 1 unknown-subcommand nit; build clean [tool-cli.md](tool-cli.md)
- 2026-04-20 14:15 — Tool-dispatch-unification Task 6.4: port the legacy codegen's input-shape guards into the `ax.callTool` preamble; refactor static `AX_PREAMBLE` into `buildPreamble(catalog?)` + `buildSchemaMap(catalog)`; inject compact `__AX_TOOL_SCHEMAS__ = {name:{properties, required}}` map (full JSON schema would bloat every script and duplicate host-side Zod); `ax.callTool` now throws actionable `TypeError` with first-3-keys-plus-ellipsis hint on wrong shape + missing-required-keys error; no per-property type check (redundant with host Zod); thread `catalog?: CatalogTool[]` into `executeScript` + all 3 call sites (pi-session, ipc-tools, mcp-server); 10 new tests, preamble overhead ~70B/tool [execute-script.md](execute-script.md)
- 2026-04-20 13:45 — Tool-dispatch-unification Task 6.2: rewrite `execute_script` tool description — replace stale `/workspace/.ax/tools/` import example with the canonical `ax.callTool` team→cycle→issues 3-liner, document untyped response shape + `opts.select` jq projection + thrown-exception error path, keep 10KB stdout cap / `/tmp/ax-results/` spill advice; also update `code` param descriptions in `tool-catalog.ts` and `mcp-server.ts`; new sync test locks the new wording in; agent suite 435/435 green [execute-script.md](execute-script.md)
- 2026-04-20 13:32 — Tool-dispatch-unification Task 6.1: inject `globalThis.ax` preamble into every execute_script body so LLM scripts can call catalog tools via `ax.callTool` / `ax.describeTool` over HTTP IPC; 11 new real-subprocess integration tests (fake `/internal/ipc` runs in a worker_thread to bypass `execFileSync` event-loop blocking); full agent suite 431/431 green [execute-script.md](execute-script.md)
- 2026-04-19 19:25 — Tool-dispatch-unification Task 3.5: register agent-side `describe_tools` + `call_tool` meta-tools (catalog entries + MCP defs + factory functions); ship `tool_dispatch` mode from host through stdin payload; conditional registration via `ToolFilterContext.toolDispatchMode`; 11 new unit tests + 6 mode-filter tests; removes the Task 3.2→3.5 knownInternalActions exemption [tools.md](tools.md)
- 2026-04-19 17:45 — Tool-dispatch-unification Task 2.4: render host-delivered `CatalogTool[]` as "## Available tools" block in system prompt; new `ToolCatalogModule` (priority 92, optional) + shared `renderCatalogOneLinersFromArray` in `src/types/catalog-render.ts`; host-side `render.ts` now a backward-compat shim; parallel-path with legacy `.ax/tools/` render [prompt.md](prompt.md)
- 2026-04-18 14:35 — Tool-modules Task 6: agent loads tool index from committed `.ax/tools/<skill>/_index.json` at turn start via new `tool-index-loader.ts`; runtime render block re-added (new-PTC only, no legacy CLI fallback); 10 new loader tests + 2 agent-setup integration tests [prompt.md](prompt.md)
- 2026-04-18 11:58 — Move `/workspace/tools/` → `/workspace/.ax/tools/`: AX-managed artifacts consolidated under `.ax/`; seedAxDirectory writes `.ax/.gitignore` with `tools/` so per-turn auto-generated modules never land in git; prompt/tool-catalog/mcp-server strings + runner resolve path + one test all updated [runner.md](runner.md)
- 2026-04-17 06:26 — Phase 3 Task 7: runner fetches skills_index before prompt build [prompt.md](prompt.md)
- 2026-04-17 06:20 — Phase 3 Tasks 5+6: SkillSummary extension + SkillsModule bullet format [prompt.md](prompt.md)
- 2026-04-01 01:20 — Fix multipart message display in chat history [runner.md](runner.md)
- 2026-04-01 01:10 — Fix image attachments dropped by pi-session runner and proxy-stream [runner.md](runner.md)
- 2026-03-31 18:31 — Fix PDF file attachments not reaching LLM in pi-session/claude-code runners [runner.md](runner.md)
- 2026-03-26 05:55 — Fix skill reading and credential request prompt guidance [prompt.md](prompt.md)
- 2026-03-22 07:18 — Conditionally show skill install instructions based on user message intent [prompt.md](prompt.md)
- 2026-03-19 09:24 — Review PR 106 skill auto-install runner changes [runner.md](runner.md)
- 2026-03-19 08:20 — Add skill dependency installer [runner.md](runner.md)
- 2026-03-16 15:00 — Runner NATS work subscription mode (replace stdin) [runner.md](runner.md)
- 2026-03-16 10:00 — Add NATS IPC transport support in runner.ts [runner.md](runner.md)
- 2026-03-15 15:30 — Implement local sandbox execution (Tasks 1-11) [tools.md](tools.md)
- 2026-03-04 19:05 — Move bash/file tools from local to IPC (Phase 1, Task 3) [tools.md](tools.md)
- 2026-02-28 22:48 — Update all tests for consolidated tool names (Task 6) [tools.md](tools.md)
- 2026-02-28 22:30 — Consolidate MCP server tools (28 -> 10) [tools.md](tools.md)
- 2026-02-28 22:30 — Update prompt modules with consolidated tool names [tools.md](tools.md)
- 2026-02-28 22:00 — Consolidate tool-catalog.ts from 28 tools to 10 [tools.md](tools.md)
- 2026-02-28 21:30 — Update pi-session.ts tool definition generation (Task 4) [tools.md](tools.md)
- 2026-02-27 12:30 — Remove pi-agent-core as a user-facing agent type [runner.md](runner.md)
- 2026-02-27 09:35 — Dev/production mode split for agent runner [runner.md](runner.md)
- 2026-02-27 09:00 — Fix agent delegation EPERM crash / retry loop [runner.md](runner.md)
- 2026-02-26 14:00 — LLM tool call optimization: context-aware filtering [tools.md](tools.md)
- 2026-02-25 16:33 — Add minimal-context guidance to DelegationModule [prompt.md](prompt.md)
- 2026-02-25 16:28 — Add DelegationModule system prompt for agent_delegate [prompt.md](prompt.md)
- 2026-02-25 19:00 — Research OpenClaw/Claude Code skills architecture [prompt.md](prompt.md)
- 2026-02-23 11:00 — Copy OpenClaw prompt optimizations into AX [prompt.md](prompt.md)
- 2026-02-22 19:20 — Fix bootstrap: include tool guidance and user context [prompt.md](prompt.md)
