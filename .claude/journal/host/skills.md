# Host / Skills

Git-native skills rollout: snapshot builder, state store, reconcile orchestrator, hooks — all host-side pieces that walk `.ax/skills/**/SKILL.md` from a bare repo ref and reconcile them into desired MCP/allowlist/state.

## Entries

## [2026-04-19 10:33] — Render tool-index signatures with destructuring braces (single-object-arg call shape)

**Task:** After 10:25 fix landed, next log showed agent calling `getTeam('Product')` and `listCycles(team.id, 'current')` — positional args. The enum hint worked (picked `'current'`), the wrap hint worked (`cycles.cycles?.[0]`), but the signature render `listCycles(teamId, type?: "current"|"previous"|"next")` read to the model as a TypeScript *positional* signature (`fn(a, b)`). It was Claude's best guess from its training, not something it had looked up.
**What I did:** Changed `loadToolIndex` render to wrap params in destructuring braces: `listCycles({ teamId, type?: "current"|"previous"|"next" })`. Zero-arity tools stay bare (`ping()`). Updated the three existing render-assertion tests and added two new ones: "destructuring braces" and "zero-param stays bare — no empty braces". Ran into one cascade test in `agent-setup.test.ts` that asserted the old `(limit?)` form — updated to match.
**Files touched:** src/agent/prompt/tool-index-loader.ts, tests/agent/prompt/tool-index-loader.test.ts, tests/agent/agent-setup.test.ts
**Outcome:** `tsc` clean; 15/15 tool-index tests green; 5/5 agent-setup tests green; full suite 2669/2726 pass (+2 new, same 33 baseline). Deployed image `dev-test3` to kind. No `refresh-tools` needed — `loadToolIndex` runs in the sandbox agent against the existing `_index.json`. Next sandbox pod (K8S_POD_IMAGE already rotated) uses the new render.
**Notes:** This is the fourth-in-a-row fix where each one surfaced the next gap — the "3 errors then 2 errors" log is a data point that the hints are reaching the agent but not all in the right form. Destructuring braces match idiomatic JS call syntax (`fn({ a, b })`) and remove the positional-vs-object ambiguity entirely. Worth keeping an eye on: if the agent ever tries to ACTUALLY destructure on receive (`const { teamId } = await getTeam(...)`), we'll know we pushed the metaphor too far — but that's a future tell, not a current risk.

## [2026-04-19 10:25] — Surface enum values + response-wrapping hint in the prompt (not the module)

**Task:** Prior fix (10:00) put the response-shape warning in the generated `linear.js` file header — but the agent doesn't read the module file before writing scripts. Real end-to-end run showed the agent STILL hallucinating `type: 'active'` for `listCycles` (enum hint buried in JSDoc the agent never saw) and STILL calling `.map` on `{issues: [...]}` (file header unread). Turn count dropped from 10+ to 3 — better but not the ≤2 exit criterion.
**What I did:**
(1) **Tool-index render**: `loadToolIndex` now embeds enum values in the compact signature the agent sees in the system prompt. `listCycles(teamId, type?)` becomes `listCycles(teamId, type?: "current"|"previous"|"next")`. Added `renderParamSuffix(prop)` helper that emits the union for string enums, falls back to no suffix for empty/malformed/non-string enums, and caps at 6 values with `|…` so a 50-value enum can't blow the prompt. 3 new tests (enum, empty/numeric fallback, truncation).
(2) **Runtime prompt hint**: `RuntimeModule` now emits a one-liner about MCP response wrapping when `toolModuleIndex` is present — concrete example (`listIssues` → `{ issues: [...], pageInfo: {...} }`) + the `JSON.stringify` recovery pattern. Omitted when no tool modules to keep the no-skills prompt minimal. 2 new tests (present when modules, absent when none).
Together these move both hints from the file (unread) to the prompt (always read). The module-header warning stays as secondary documentation for agents that do read the file.
**Files touched:** src/agent/prompt/tool-index-loader.ts, src/agent/prompt/modules/runtime.ts, tests/agent/prompt/tool-index-loader.test.ts, tests/agent/prompt/modules/runtime.test.ts
**Outcome:** `tsc` clean; 13/13 tool-index tests + 11/11 runtime tests green; full suite 2667/2724 pass (+5 new, same 33 pre-existing). Deployed image `dev-test2` to kind + triggered `refresh-tools` on linear (commit `e49a912`). Next agent run should see enum values in its prompt and use `type: 'current'` directly, and know to inspect before `.map`-ing list results.
**Notes:** The layered failure — "better, only 3 tool calls now" — was instructive. Each fix moved the agent further along the happy path but surfaced the next gap: guard → signature learning → enum hallucination → response wrapping. Putting hints in the prompt rather than the file is the right level for agent UX because prompt tokens are always loaded; file content is conditional. The 6-value enum cap is a policy choice — real-world cases (cycle types, priority labels) top out at ~5, and truncation with `|…` still conveys "enum, not free-text".

## [2026-04-19 10:00] — Fix codegen guard for no-args + warn about response wrapping

**Task:** User reported Linear skill going in circles across 10+ tool calls: (1) `listTeams()` threw `TypeError: listTeams expects a single object argument` even though all its params are optional — the guard from the prior commit rejected `undefined`; (2) `issues.map is not a function` because Linear wraps collections in `{issues: [...], pageInfo: {...}}` and the agent kept guessing `.map` on a raw array. Each mistake burned a full turn with cryptic feedback.
**What I did:**
(1) **Guard fix**: When a tool's inputSchema has no `required` keys, codegen now emits `export async function listTeams(params = {}) {...}` so no-args calls default cleanly. If any param IS required, we keep the bare `params` signature so missing-required still surfaces the actionable "expects a single object argument" error.
(2) **Response-shape warning**: Added a prominent header comment to every generated module noting that many MCP servers wrap list results in `{<plural>: [...]}` objects, with a concrete example (`listIssues` → `{ issues: [...] }`) and the "log raw first" pattern. MCP SDK doesn't surface `outputSchema` through our discovery (`connectAndListTools` only passes `inputSchema`), so this is the minimum viable shape hint — cheaper than auto-unwrap heuristics (which would lose pageInfo) and more reliable than hoping every skill author documents it in SKILL.md.
Added 3 codegen tests: params defaulting to `{}` when no required, bare signature when any required, module header contains the shape warning.
**Files touched:** src/host/toolgen/codegen.ts, tests/host/toolgen/module-codegen.test.ts
**Outcome:** `tsc` clean; 14/14 codegen tests green; full suite 2662/2719 pass (+3 new, same 33 pre-existing macOS socket-EINVAL failures). Deployed new image to the kind `ax` cluster and triggered `refresh-tools` on linear — verified the regenerated `.ax/tools/linear/linear.js` has `listTeams(params = {})` and the shape warning in the header. End-to-end verification (≤2 tool call turnaround for "issues in product cycle") depends on the next agent run — user to validate.
**Notes:** This is the second-order bug from the prior guard: the guard was meant to catch string-arg mistakes, but `typeof undefined !== 'object'` also rejected no-args, which is valid for all-optional tools. `required.length === 0` is the exact condition that differentiates the two cases — trusting it keeps the fast path fast. The response-shape hint is a documentation lever because we can't derive return shape at codegen time (would need to call the tool or parse MCP output schemas, neither currently available). User's pattern observation — "we're going in circles" — was correct: each prior fix addressed a symptom. This one targets the two root failure modes the logs actually show.

## [2026-04-19 08:30] — Codegen enum in JSDoc + sandbox IPC retry on stale keep-alive

**Task:** Two distinct failures in the same session:
(1) Agent called `listCycles({ teamId, type: 'active' })` — Linear rejected as "invalid_value" for `type` (valid: current|previous|next). Codegen's `@param {string}` let the agent hallucinate an adjacent value.
(2) After a successful-looking tool call, the next IPC `llm_call` failed with `fetch failed (UND_ERR_SOCKET: other side closed)` at `duration=1ms` — classic stale keep-alive on undici's connection pool after a quiet gap.

**What I did:**
(1) **Codegen**: `buildJSDoc` now emits JSDoc union-literal types (`@param {"current"|"previous"|"next"} [params.type]`) when an inputSchema property has a non-empty string `enum`. Falls back to plain `{string}` for empty/malformed enums. Agent can't ignore a union type as easily as a prose hint.
(2) **IPC retry**: `http-ipc-client.ts` now retries once on stale-socket errors (`UND_ERR_SOCKET` code or "other side closed"/"socket hang up" message). No bytes reached the server in that case, so one transparent retry is safe and idempotent. Non-stale errors (ECONNREFUSED, timeouts, HTTP non-2xx) bubble up unchanged. Added `isStaleSocketError(err)` helper and 2 regression tests (retry succeeds on second try; non-stale error surfaces on first try without retry).

**Files touched:** src/host/toolgen/codegen.ts, src/agent/http-ipc-client.ts, tests/host/toolgen/module-codegen.test.ts, tests/agent/http-ipc-client.test.ts
**Outcome:** Success — `tsc` clean; 11/11 codegen tests green; 8/8 http-ipc-client tests green; full suite 2659/2692 pass (+4 new, same 33 pre-existing socket-EINVAL failures). Deploy via `cycle all`; then `refresh-tools` on linear to regenerate stubs with enum hints.
**Notes:** The 1ms duration signature is diagnostic — it tells you the request never left the client. Disabling keep-alive would also fix it but tanks perf for every subsequent call; a single retry keeps the fast path fast and only pays extra cost on the rare stale-pool miss. Could push this further (retry with exponential backoff, cap at N tries), but one retry handles the documented failure mode and avoids masking real outages behind a longer retry curtain.


## [2026-04-19 07:55] — Support `*.parent` wildcard domains for multi-tenant vendors

**Task:** User's Salesforce skill needed to cover per-org subdomains like `acme.my.salesforce.com` — an unknowable-in-advance set that no admin can enumerate. Schema only accepted plain hostnames, so `*.my.salesforce.com` and `*.salesforce.com` tripped `must be a valid hostname (no scheme, no path)`. Listing every known tenant wasn't feasible.
**What I did:** Added TLS-style leading-label wildcard support. (1) Schema: new `WILDCARD_HOSTNAME_RE` accepting exactly one leading `*.` followed by a normal multi-label hostname (`*.foo.com` yes, `*.com` no, `foo.*.com` no, `**.foo.com` no, bare `*` no). `Hostname` refinement now accepts either pattern; error message updated. (2) New pure helper `matchesDomain(allowed, candidate)` in `domain-allowlist.ts` — normalizes the candidate and walks the set for exact hits, then checks wildcard entries via suffix match with a strict-longer length check so `*.foo.com` never matches the apex `foo.com`. (3) Rewired the proxy allowlist `has()` closure in `server-completions.ts` to use `matchesDomain` instead of `allowedSet.has(...)`. Added 11 tests total — 4 on schema (accept, reject overly broad, normalize, mixed), 7 on matcher (exact, miss, subdomain, apex exclusion, sibling exclusion, candidate normalization, mixed exact+wildcard). Skill-creator docs updated with the wildcard example.
**Files touched:** src/host/skills/frontmatter-schema.ts, src/host/skills/domain-allowlist.ts, src/host/server-completions.ts, tests/host/skills/frontmatter-schema.test.ts, tests/host/skills/domain-allowlist.test.ts, skills/skill-creator/SKILL.md
**Outcome:** Success — `tsc` clean; 40/40 targeted skills tests green; full suite 2655/2688 pass (+10 new, same 33 pre-existing socket-EINVAL failures). Admins can now declare `*.my.salesforce.com` once and have it cover every tenant subdomain.
**Notes:** Apex exclusion is the right default even though it surprises newcomers — TLS certs work the same way (a cert for `*.foo.com` does NOT validate `foo.com`). Admins who need the apex add it as a separate entry alongside the wildcard. Rejecting `*.com` / public-suffix-level patterns prevents a single skill from requesting blanket TLD egress. A proper Public Suffix List check would be stricter but adds a runtime dep; the "≥2 labels after the wildcard" check is a decent cheap approximation. `matchesDomain` is exported separately so tests can exercise it in isolation; the existing `getAllowedDomainsForAgent` still returns a plain `Set<string>` — wildcards are stored as-is and the matcher handles the expansion at lookup time, preserving the O(1) path for exact hits.


## [2026-04-19 07:45] — Actionable Zod error + docs for mcpServers.credential mis-shape and CLI-only skills

**Task:** User was drafting a Salesforce skill (CLI tool, credentials for HTTPS API calls — no MCP at all). Frontmatter validation failed with `mcpServers.0.credential: Invalid input: expected string, received object`. The agent had invented a fake mcpServers entry just to "hold" the credential, and nested a full `{envName, authType, scope}` object inside. The default Zod error didn't tell the agent what to do — it just said the type was wrong.
**What I did:** Two-part fix. (1) Schema: replaced the bare `z.string().regex(ENV_NAME)` on `mcpServers[].credential` with `z.string({ error: () => '...' })` carrying an actionable message — names the mistake, points to the correct shape ("string envName reference into credentials[]"), and explicitly calls out the `{envName, authType, scope}` object shape as the wrong one. (2) Docs: added a short "mcpServers is optional — if there's no MCP endpoint, leave it off" note at the top of the Frontmatter Schema section, plus two new "WRONG" side-bar examples — one for the nested-credential-object mistake and one for the "faking an mcpServers entry to hold a credential for a CLI-only service" mistake that the user just hit.
**Files touched:** src/host/skills/frontmatter-schema.ts, tests/host/skills/frontmatter-schema.test.ts, skills/skill-creator/SKILL.md
**Outcome:** Success — `tsc` clean; 20/20 frontmatter-schema tests green (2 new: "rejects nested-object credential with actionable error" + "accepts CLI-only skill with credentials + domains and no mcpServers"); full suite 2645/2678 pass (same 33 pre-existing socket-EINVAL failures as main). Existing skill-creator has been updated; deployed agents will pick up the new docs on the next `seedAxDirectory` for fresh workspaces. Live workspaces need a direct push or the admin to delete `.ax/skills/skill-creator/SKILL.md` so the next turn re-seeds.
**Notes:** Recurring pattern here: layered defense. The schema error now teaches the agent what shape to write; the docs now teach it what case it's in (MCP vs CLI-only). The Bitbucket example already showed the CLI-only shape — the agent just didn't generalize, because nothing in the docs said "mcpServers can be absent." Explicit beats implicit.


## [2026-04-19 07:40] — Auto-discover skill MCP tools at turn start (HEAD-cached)

**Task:** After the JSON-parse fix (51b8bc0e), user redeployed the kind cluster and hit a new failure: `getTeam({query:"Product"})` now failed with `Error: MCP gateway not configured for this tool`. The generated `linear.js` had the stub (import succeeded), but the host tool-batch handler's `resolveServer(agentId, 'get_team')` returned undefined — the `toolServerMap` was empty for this agent in the fresh pod.
**What I did:** Traced to commit `b790f2d2` (Task 5 of the tool-modules-git-native migration) which removed the per-turn `discoverAllTools` call from `server-completions.ts` once tool modules started living in git. The removal dropped both the codegen side-effect (correct — modules are committed now) AND the `toolServerMap` population side-effect (regression — the runtime dispatcher still needs it). So after pod restart, `toolServerMap` only got repopulated when the admin clicked refresh-tools; the subprocess sandbox turn path had no trigger. Added `ensureToolsDiscoveredForHead(agentId, headSha, opts)` to `McpConnectionManager` — a thin HEAD-cached wrapper over `discoverAllTools` that runs exactly once per `(agent, HEAD)` per pod-lifetime. Wired into `processCompletion` right after `getAgentSkills`: builds `serverFilter` from the snapshot's skill-declared `mcpServers[].name`, uses `resolveMcpAuthHeaders` for auth, and swallows errors (partial map > refused turn). Added 2 mcp-manager tests: HEAD-cached dedup and the `force: true` escape hatch for refresh-tools.
**Files touched:** src/plugins/mcp-manager.ts, src/host/server-completions.ts, tests/plugins/mcp-manager.test.ts
**Outcome:** Success — `tsc` clean; 27/27 mcp-manager tests green; full suite 2643/2676 pass (same 33 pre-existing macOS Unix-socket-path-length failures as baseline). Next `npm run k8s:dev cycle all` will auto-populate the tool map on the first turn instead of waiting for an admin click.
**Notes:** Picked HEAD-cache over per-pod cache so a post-restart agent that got a `git push` with new skills doesn't keep its stale tool map — the HEAD changes, the cache invalidates, discovery re-runs. Per-pod wasn't strong enough; per-turn was too expensive. This is the third in a sequence of "Task 5 left a side-effect on the floor" fixes (the others: `registerMcpServersFromSnapshot` restored the `servers` map write; this one restores the `toolServerMap` write). Lesson pattern holds: when you delete an effectful pipeline, enumerate every write destination and explicitly re-wire each one.


## [2026-04-19 07:10] — tool-batch: parse JSON content on unified/plugin/default paths

**Task:** After the codegen JSDoc fix (d728fc6e), user's agent hit a new wall: `getTeam({query:"Product"})` returned a JSON *string* (not an object), so `team.id` was `undefined`, and the next `listCycles({teamId: team.id, type:"current"})` call was rejected by Linear as "expected string" on `teamId`.
**What I did:** Traced the pipeline — `callToolOnServer` (src/plugins/mcp-client.ts) already joins MCP text blocks into `content: string`, and `tool-batch.ts`'s dispatcher path already parses that string. But the unified, plugin, and default-provider success paths all pushed the raw string. Added a `parseContent(content)` helper: if the content is a string, try `JSON.parse` and fall back to the raw string; otherwise pass through untouched (no round-trip through JSON for already-structured content). Applied it to all four success paths — the dispatcher's inline try/catch becomes a helper call, and the three previously-inconsistent paths now match. Added 4 regression tests: unified JSON string → object; plain-text string passes through; already-object content preserved; plugin path parses too.
**Files touched:** src/host/ipc-handlers/tool-batch.ts, tests/host/ipc-handlers/tool-batch.test.ts
**Outcome:** Success — 28/28 tool-batch tests green; `tsc` clean; full suite 2641/2674 pass (same 33 pre-existing macOS Unix-socket-path-length failures as main baseline). Host needs a redeploy (`npm run k8s:dev cycle all`) for the kind cluster to pick up the change — no refresh-tools needed since this is host-side, not generated-stub-side.
**Notes:** This is the mirror of the JSDoc fix: the prior one made generated stubs match their documented contract (object in); this one makes host-side output match what the stub's default-object return-type implies (object out). Together they close both ends of the "agent expects structured data" contract. If a server starts returning pre-parsed objects directly instead of text blocks, the helper still works — non-string content bypasses the parse attempt. The dispatcher path's old pattern (`JSON.parse(content)` in a try/catch, relying on non-string content coincidentally throwing) worked, but wastefully `String()`-coerced objects through JSON; the typeof guard avoids that.


## [2026-04-19 06:55] — Fix codegen JSDoc ↔ signature mismatch causing agent retry spirals

**Task:** User reported a simple "what cycle are we in?" prompt producing 16 consecutive `execute_script` calls. Logs showed a Zod validation error `calls.0.args: Invalid input: expected record, received string` — the agent was calling `listCycles("team-uuid")` instead of `listCycles({ teamId: "..." })`, and every retry hit the same IPC schema error without a hint about what to fix.
**What I did:** Traced to `src/host/toolgen/codegen.ts`. `buildJSDoc` was emitting positional-style tags (`@param {string} teamId`) while the generated function actually takes a single object argument (`function listCycles(params)`) and forwards it verbatim to IPC. Two-part fix: (a) JSDoc now uses object-form — `@param {object} params` + `@param {T} params.name` per property, with `[params.name]` for optional — so the docs match the signature; (b) added a runtime `typeof params !== 'object'` guard that throws `TypeError("listCycles expects a single object argument (keys: teamId, type), e.g. listCycles({ teamId: ... })")` before anything reaches the IPC wire. Added 3 regression tests: object-form JSDoc shape, runtime-guard presence, zero-arity branch untouched.
**Files touched:** src/host/toolgen/codegen.ts, tests/host/toolgen/module-codegen.test.ts
**Outcome:** Success — 9/9 codegen tests green; `tsc` clean; full suite has the same 33 pre-existing macOS Unix-socket-path-length failures as main baseline (no new breakage). Future generated `.ax/tools/<skill>/<server>.js` modules will both document and enforce the object-form calling convention.
**Notes:** Defense in depth matters here: the JSDoc fix alone would not prevent an agent that ignores docs from landing at the Zod error, and the runtime guard alone would not teach agents to pass objects in the first place. Pairing them means the agent either reads the correct shape from the JSDoc, OR sees an actionable error message naming the keys and showing call syntax. The 16-call retry spiral is the ground-truth signal we care about — a cryptic schema error sends models off the rails; a typed error with call syntax ends the loop.


## [2026-04-18 23:10] — Skill-creator guidance: ask user about uncertain frontmatter fields

**Task:** After URL-path transport inference (34ac1dbb) and all the earlier layers, the remaining failure mode for skill creation is the agent silently guessing wrong on the handful of fields that can't be inferred — MCP URL for unfamiliar vendors, OAuth vs API key, extra domains beyond the MCP server. Prompt guidance in the skill-creator tells the agent what those fields do, but it doesn't tell the agent to ASK THE USER when it's uncertain. Result: agent confidently invents a plausible-looking URL or defaults authType to `api_key` for an OAuth-only service, skill approves as pending, tool discovery fails silently, admin debug cycle starts.
**What I did:** Added a new step 5 to the skill-creator's Flow: "Confirm the uncertain fields before writing". Names exactly three high-risk fields (URL, authType, extra domains), gives the agent concrete question wording for each, and tells it to bundle everything into one compact confirmation message instead of three separate turns. Adds an explicit "you never need to ask about" list covering the safe-default fields (name, description, envName, scope, transport post-inference, credential value). Renumbered remaining flow steps 6-8. Pushed the updated `.ax/skills/skill-creator/SKILL.md` directly into the agent's bare repo so the next turn picks up the new guidance without waiting for a full image deployment.
**Files touched:** skills/skill-creator/SKILL.md (source of truth — seeded into new workspaces via `seedAxDirectory`), plus a direct git push to the kind cluster's existing agent workspace.
**Outcome:** Success — `tsc` clean; 134/134 skills tests pass (no behavior changes, docs only). Updated SKILL.md is live in the agent's bare repo (commit `26fff46`). The deployed host image (`debug-layer8`) still contains the URL-path transport inference so the combination of (a) infer-what-you-can + (b) ask-for-what-you-can't should keep silent-failure skills to near-zero.
**Notes:** The "compact one-liner" framing matters — a barrage of three questions is worse UX than one confirmation ("I'll use URL=X, authType=api_key, no extra domains. Correct?"). The agent still gets a final chance to bail before committing a bad SKILL.md. For existing agents, skill-creator updates don't auto-propagate because `seedAxDirectory` is first-run guarded (`!existsSync(destDir)`). To force an update, admin deletes `.ax/skills/skill-creator/SKILL.md` from the agent's workspace and the next turn reseeds from the current image. OR push directly via git-http (what I did for the kind cluster).


## [2026-04-18 23:00] — Infer MCP transport from URL path when field is omitted

**Task:** Even after the hook-sweep fix (ea54792c), the user's delete-then-reinstall flow still produced empty `.ax/tools/linear/`. Audit showed `toolSync: {moduleCount:0, toolCount:0, commit:null}` from the latest approval. Root cause: agent's re-authored `linear/SKILL.md` used `url: https://mcp.linear.app/sse` but didn't include `transport: sse` — Zod default kicked in at `http`, AX tried Streamable HTTP against Linear's SSE-only endpoint, discovery got 0 tools, syncToolModulesForSkill bailed early with null commit. The skill-creator docs clearly warn about this, but the agent keeps omitting the field. Prompt guidance alone keeps failing.
**What I did:** Made the schema infer `transport` from the URL path when the field is absent. New `inferTransportFromUrl(url)`: any URL where the pathname contains `/sse` as a segment → `sse`, else `http`. Changed the field from `.default('http')` to `.optional()` plus a Zod `.transform()` that fills it in. Explicit `transport: http` or `transport: sse` still wins — escape hatch preserved for weird vendor URLs. Added 2 regression tests in `frontmatter-schema.test.ts`: inference from `/sse` URL; explicit `http` overrides inference. Existing tests updated to reflect the new default logic.
**Files touched:** src/host/skills/frontmatter-schema.ts, tests/host/skills/frontmatter-schema.test.ts
**Outcome:** Success — `tsc` clean; 134/134 skills tests pass. Deployed `debug-layer8` image to kind cluster. Called refresh-tools against the agent's existing SKILL.md (which still has no `transport` field): `{"ok":true, "commit":"0234dd4d...", "moduleCount":2, "toolCount":42}`. `.ax/tools/linear/{index.js, linear.js, _index.json}` committed with the 42 Linear MCP tools. End-to-end flow now works whether the agent remembers to add the `transport` field or not.
**Notes:** URL-path inference is a fallback for when the skill author doesn't know / forgot the transport convention. The skill-creator SKILL.md still documents the explicit `transport:` field + curl diagnostic as the PRIMARY guidance — inference is defense-in-depth. One caveat: if a vendor ships an endpoint URL that ENDS in `/sse` but actually speaks Streamable HTTP, they'd need `transport: http` explicitly. Known in the wild: none. Linear, Anthropic's legacy servers, and the handful of other SSE MCPs I've seen all follow the convention cleanly.


## [2026-04-18 22:30] — Hook-triggered orphan sweep: fix delete-then-reinstall auto-enable race

**Task:** User reported that after agent deletes linear skill in one turn and re-installs it in the next, the skill auto-enables without going through approval. This regressed despite the earlier orphan sweep fix (cbbf0d7b). Root cause: sweep runs only at state-read entry points (`getAgentSkills`, `getAgentSetupQueue`, `getAllowedDomainsForAgent`). If both the delete-commit and the re-add-commit land between two `getAgentSkills` calls (typical when the two turns are part of the same chat session), by the time sweep next runs the skill is already back in the snapshot — sweep sees it as present and keeps the credential row, so state-derivation sees a match and marks the skill enabled.
**What I did:** Added orphan-sweep to the post-receive hook endpoint. The hook already invalidates the snapshot cache after every push; now it also loads the fresh snapshot and runs `sweepOrphanedRows` before returning. Catches the "delete-then-reinstall in one session" race because each push individually triggers sweep — the delete-push cleans the row BEFORE the re-add-push lands. Extended `HookEndpointDeps` with optional `agentSkillsDeps` so setups without a database still get the cache-bust path. Wired via `server.ts:166`. Added 2 regression tests for hook-endpoint (sweeps when skill absent in fresh snapshot; push still succeeds if sweep throws — sweep failure must not break pushes).
**Files touched:** src/host/skills/hook-endpoint.ts, src/host/server.ts, tests/host/skills/hook-endpoint.test.ts
**Outcome:** Success — `tsc` clean; 15/15 hook-endpoint tests + 130/130 skills tests pass. Deployed `debug-layer7` image to kind cluster. Reset DB + bare repo to clean state, simulated the user's exact flow:
  1. Agent pushes a new `linear/SKILL.md` (with `transport: sse`) → skill appears as `pending` with `missing credential LINEAR_API_KEY (user)` ✓
  2. Admin POST to `/admin/api/skills/setup/approve` with `LINEAR_API_KEY=lin_api_...` → skill transitions to `enabled`, audit shows `toolSync: {moduleCount:2, toolCount:42, commit:"ea2f323..."}` ✓
  3. Verified `list_teams` tool call via MCP SDK with the real API key returns three teams: Product (PROD), Documentation (DOC), Support (SUP) — exactly the 3 teams the user expected ✓
**Notes:** The hook-triggered sweep is the correct layer for this — cache invalidation already happens there, and the sweep needs to run at the push-timing, not at state-read-timing. Layered defence: sweep also still runs at `loadSnapshot`, which catches any rows that were orphaned before this host process started (e.g., after an old host version had the bug). One caveat: the hook now does a small amount of DB work per push. At current scale it's invisible; at 1000+ agents it's worth revisiting if commit throughput matters.


## [2026-04-18 21:00] — Flatten .ax/tools/<skill>/index.js barrel (namespace → flat re-exports)

**Task:** Agent was calling `import { listIssues } from '/workspace/.ax/tools/linear/index.js'` (matching the example in the `execute_script` tool description) but the generated barrel was `export * as linear from './linear.js'` (namespaced). Import failed with "has no export named 'listIssues'". Two options to reconcile: update the example to show the namespaced import, or flatten the barrel. Flat wins — the agent's natural pattern is to import function names directly; the namespaced form is just a hoop the model doesn't consistently clear from examples.
**What I did:** Changed `src/host/toolgen/codegen.ts::generateIndex` to emit `export * from './<server>.js'` (flat re-export) instead of `export * as <server> from './<server>.js'` (namespace). One skill with one server (common case) works exactly as the example shows. For rare multi-server skills with overlapping function names, ES-module link-time raises a clear "ambiguous export" error the admin can act on. Updated 2 test assertions (module-codegen + e2e) to pin the flat form; added an anti-assertion that `as <server>` must NOT appear. Deployed `debug-layer6` image to kind cluster + ran `POST /admin/api/agents/ax/skills/linear/refresh-tools` — new commit `234153a7` has `export * from './linear.js'` as expected.
**Files touched:** src/host/toolgen/codegen.ts, tests/host/toolgen/module-codegen.test.ts, tests/host/toolgen/e2e.test.ts
**Outcome:** Success — `tsc` clean; 17/17 toolgen tests pass; E2E verified the new barrel is committed to the agent's repo. Agent can now `import { listIssues }` directly from the barrel.
**Notes:** ES modules handle ambiguous re-exports at link time (SyntaxError), so a collision between two skill-declared servers' tool names would surface clearly with server + tool name in the error. Simpler than collision-detection at codegen time, and skills typically have one server each anyway.


## [2026-04-18 20:15] — Fix authForServer: rewire tool-batch + fast-path to read skill_credentials

**Task:** After Layers 1-4 landed, the user reported Linear tool calls still failed: `Streamable HTTP error: Error POSTing to endpoint: {"error":"invalid_token",...}`. Root cause: the `authForServer` callback wired into the tool-batch IPC handler (server-init.ts:433) was reading from `providers.credentials.get(envName)` — the LEGACY global key/value store. Skill credentials live in `skill_credentials` (tuple-keyed by agent + skill + env + user) after the SSoT migration (step 5). No Bearer header was sent → Linear 401. Second, subtler issue: the `authForServer({name, url}) => ...` signature had no agentId/userId, so even if rewired, it couldn't query the tuple-keyed store.
**What I did:** Extended `authForServer` signature across tool-batch.ts, tool-router.ts, tool-dispatcher.ts to receive `{name, url, agentId, userId}`. Each call site threads `ctx.agentId` + `ctx.userId ?? ''` through. Rewired server-init.ts:433 + inprocess.ts:264 (call-time) + inprocess.ts:186 (discovery-time fallback) to use `resolveMcpAuthHeaders({skillCredStore, ...})` — same function `syncToolModulesForSkill` already used correctly. `inprocess.ts` discovery-time auth keeps the `providers.credentials.get(envName)` path as a fallback for admin-added servers that still live there. Added `skillCredStore` to `FastPathDeps` + threaded through in `server-completions.ts` when constructing the fast-path opts. Regression test in `tool-batch.test.ts` asserts the new signature: `authForServer` receives agentId + userId from ctx.
**Files touched:** src/host/ipc-handlers/tool-batch.ts, src/host/tool-router.ts, src/host/tool-dispatcher.ts, src/host/inprocess.ts, src/host/server-init.ts, src/host/server-completions.ts, tests/host/ipc-handlers/tool-batch.test.ts
**Outcome:** Success — `tsc` clean; 265/265 targeted tests pass (all skills + admin + ipc-handlers + plugins). Deployed `debug-layer5` image to kind cluster.
**Notes:** This completes the "reach Linear with a valid Authorization header at call time" leg. Failure evolved across 5 layers as each fix landed: (1) empty registry → (2) Streamable HTTP vs SSE at discovery → (3) --mirror refspec push → (4) Streamable HTTP vs SSE at call time → (5) credential source mismatch at call time. Observability tip: at each layer the error MESSAGE changed, proving the previous layer's fix reached production even when it surfaced the next latent bug underneath.


## [2026-04-18 19:55] — Thread transport through tool-batch + tool-router call paths

**Task:** After Layers 1-3 landed, `.ax/tools/linear/` commit worked and agents could import tools. But at runtime, agent tool calls to Linear failed with `invalid_token` from Linear's API. Root cause: the same SSE-vs-Streamable-HTTP mismatch as Layer 2, but at call time not discovery time. Layer 2 fixed transport for `listToolsFromServer` (discovery); the call-time path `callToolOnServer` via `tool-batch.ts` + `tool-router.ts` only passed `{headers}`, letting transport default to `streamable-http` → SDK makes wrong-shaped requests to `mcp.linear.app/sse` → Linear can't find the session and rejects with invalid_token.
**What I did:** Extended `McpConnectionManager.getServerMeta` + `getServerMetaByUrl` to return `transport` (the field was already stored on `ManagedServer` from Layer 2). Extended `ToolBatchOptions.mcpCallTool` + `ToolBatchOptions.getServerMetaByUrl` + `ToolRouterContext.mcpCallTool` + `ToolRouterContext.getServerMetaByUrl` type signatures to carry `transport?: 'streamable-http' | 'sse'`. Updated the call sites in both `tool-batch.ts` and `tool-router.ts` to extract `transport` from server meta + pass to `mcpCallTool` alongside headers. `callToolOnServer` was already transport-aware from Layer 2. Added 2 regression tests in `tool-batch.test.ts` (transport flows through with headers; transport alone without headers) and 3 in `mcp-manager.test.ts` (`getServerMeta`/`getServerMetaByUrl` return transport + returns undefined when not declared).
**Files touched:** src/plugins/mcp-manager.ts, src/host/ipc-handlers/tool-batch.ts, src/host/tool-router.ts, tests/host/ipc-handlers/tool-batch.test.ts, tests/plugins/mcp-manager.test.ts
**Outcome:** Success — `tsc` clean; 78/78 targeted tests pass. Deployed `debug-layer4` image to kind cluster; `/opt/ax/dist/host/ipc-handlers/tool-batch.js` contains 5 references to `transport`, confirming the fix is live. Left the image deployed so the user can verify their "list Linear teams" agent prompt works end-to-end.
**Notes:** This completes the end-to-end Linear skill path: Layer 1 (registration) + Layer 2 (discovery transport) + Layer 3 (mirror unification) + Layer 4 (call-time transport). A skill declaring `transport: sse` now exercises the SSE wire protocol consistently from initial discovery through every tool call. The key observability at each layer was that the failure MODE changed as each fix landed — from "zero tools committed" to "git push failed" to "invalid_token at call time" — each step was progress even when it surfaced the next bug underneath.


## [2026-04-18 18:45] — Unify local-mirror management behind WorkspaceProvider.ensureLocalMirror

**Task:** After Layer 2 (transport field) landed, E2E verification surfaced a third bug: `workspace.commitFiles` was failing with `git push failed: --mirror can't be combined with refspecs`. Root cause: two parallel mirror-management paths in the codebase. `server-init.ts::getBareRepoPath` cloned with `--mirror` but never unset `remote.origin.mirror` (used by snapshot walkers); `git-http.ts::ensureMirror` cloned + properly unset (used by `commitFiles`). Both wrote to the same on-disk directory with separate `seededMirrors` sets — whichever path ran first left the other broken.
**What I did:** Unified the two paths behind a new interface method. Added `ensureLocalMirror(agentId): Promise<string>` to `WorkspaceProvider`. `git-local.ts` returns the bare-repo path (no mirror concept — `git-local`'s bare repo IS authoritative). `git-http.ts` exposes its existing `ensureMirror` via the interface method. `server-init.ts::getBareRepoPath` now delegates to `providers.workspace.ensureLocalMirror` — dropped its duplicate `seededMirrors` set, its `execFileAsync('git', ['clone', '--mirror', ...])` call, and 4 now-unused imports (`existsSync`, `rmSync`, `dirname`, removed by build-clean). Updated 4 test mocks (`tool-module-sync`, `admin-gate`, `bootstrap`, `scheduler/plainjob`) to add the new method. Added 3 new tests in `commit-files.test.ts`: `ensureLocalMirror` returns a bare repo path for git-local; git-http correctly unsets `remote.origin.mirror` + sets the per-branch fetch refspec; `ensureLocalMirror` pre-seeding does NOT break a subsequent `commitFiles` push (exact regression this fix addresses).
**Files touched:** src/providers/workspace/types.ts, src/providers/workspace/git-local.ts, src/providers/workspace/git-http.ts, src/host/server-init.ts, tests/providers/workspace/commit-files.test.ts, tests/host/skills/tool-module-sync.test.ts, tests/host/admin-gate.test.ts, tests/cli/bootstrap.test.ts, tests/providers/scheduler/plainjob.test.ts
**Outcome:** Success — `tsc` clean; 23/23 workspace tests + 263/263 affected-scope tests green. E2E verified against kind cluster: built `debug-layer3` image, deployed, hit `POST /admin/api/agents/ax/skills/linear/refresh-tools` → `{ok:true, toolCount:42, moduleCount:2, commit:"acccd881..."}`. Bare repo at `/var/git/repos/ax.git` now has `ax: refresh tools for linear` commit with `.ax/tools/linear/{index.js, linear.js, _index.json}` containing all 42 Linear MCP tools. Full end-to-end fix complete.
**Notes:** This bug was latent from the initial `git-http` provider implementation but only surfaced after Layers 1+2 made Linear tool discovery actually produce a commit (previously discovery returned zero tools, so push never ran). All three layers (registration + transport + mirror) had to land before the Linear skill could work end-to-end. Cluster restored to prior image after verification; `debug-layer3` image stays in kind cache but is not deployed. The debug-mcp-fix, debug-mcp-sse, and debug-layer3 images can be cleaned up when convenient via `docker image rm` + a fresh kind load.


## [2026-04-18 18:30] — Add `transport` field to skill mcpServers frontmatter (Streamable HTTP vs SSE)

**Task:** After Layer 1 (mcpServer registration) landed, E2E verification against the kind cluster revealed a second layer: `listToolsFromServer` uses `StreamableHTTPClientTransport` exclusively, but Linear's `mcp.linear.app/sse` speaks only the legacy SSE transport. Tool discovery 404'd with `Error POSTing to endpoint: Not Found`. Chose frontmatter-declared transport over runtime auto-detect — aligns with the "git is SSOT" philosophy, zero round-trip waste, PR-visible migration path, no brittle error-pattern matching.
**What I did:** (1) New optional `transport: z.enum(['streamable-http', 'sse']).default('streamable-http')` field on `McpServerSchema` in `frontmatter-schema.ts`. Default preserves current behavior; explicit `sse` opts into the legacy transport. (2) Threaded through `PluginMcpServer.transport` in `mcp-manager.ts` and the `discoverAllTools → listToolsFromServer` call site. (3) `mcp-client.ts::createTransport(url, headers, transport)` branches on discriminator — returns `SSEClientTransport` (new import from `@modelcontextprotocol/sdk/client/sse.js`) for `transport === 'sse'`, else the existing `StreamableHTTPClientTransport`. (4) Extended `listToolsFromServer` + `callToolOnServer` + `connectAndListTools` + `ToolModuleSyncInput.mcpServers[]` + `registerMcpServersFromSnapshot` to carry the field through. (5) Updated `skills/skill-creator/SKILL.md` — added transport field to schema reference, added "Picking the right MCP transport" section with `curl` diagnostic, added `transport: sse` to the Linear example in both the main example and the "Not this" WRONG/RIGHT side-by-side. (6) 3 new frontmatter-schema tests (default, explicit sse, reject unknown), 3 new mcp-client tests (accepts transport opts, both values compile+run).
**Files touched:** src/host/skills/frontmatter-schema.ts, src/plugins/mcp-manager.ts, src/plugins/mcp-client.ts, src/host/skills/tool-module-sync.ts, src/host/skills/mcp-registry-sync.ts, skills/skill-creator/SKILL.md, tests/host/skills/frontmatter-schema.test.ts, tests/plugins/mcp-client.test.ts
**Outcome:** Success — `tsc` clean; 135/135 targeted tests (skills + mcp-client) green. E2E in kind cluster: built `debug-mcp-sse` image, loaded via `kind load docker-image`, deployed. After pushing a `transport: sse` amendment to the agent's `.ax/skills/linear/SKILL.md`, refresh-tools advanced PAST tool discovery — error shape changed from "zero tools" to "git push fails (--mirror refspec)", proving the SSE transport path works and tools are now being discovered + codegen-ready. The remaining push error is a separate, pre-existing bug in `server-init.ts:281` (local mirror is cloned with `--mirror` but never unsets `remote.origin.mirror`, so subsequent refspec-push fails). Rolled cluster back to prior image; Layer 3 to be fixed next.
**Notes:** Design discussion: considered runtime auto-detection (try Streamable HTTP first, catch "POSTing to endpoint: Not Found", fall back to SSE) — rejected. Brittle error-pattern matching against SDK internals; wastes a round-trip on every SSE server unless cached (then cache invalidation concerns); silently re-detects without audit trail. Frontmatter field is declarative, visible in git diffs, skill author already picked the URL from vendor docs so knowing transport is cheap. The `streamable-http` default preserves behavior for every existing skill.


## [2026-04-18 18:10] — Fix: skill-declared MCP servers never registered with mcpManager

**Task:** Admin reported `.ax/tools/linear/` not being committed after approving the linear skill. Debug via `ax-debug` Tier 2 (kind cluster): `mcp_servers` table empty, audit shows `toolSync: {moduleCount:0, toolCount:0, commit:null}`, agent bare repo lacks `.ax/tools/linear/`. Root cause: skills SSoT migration (step 6) deleted the reconciler that used to call `addMcpServer` on approved skills — the replacement approve flow writes credentials + domain approvals but never registers the skill's declared `mcpServers[]` with `McpConnectionManager`, so `discoverAllTools` iterates an empty Map and returns zero tools.
**What I did:** Lazy registration, not persistence. New helper `src/host/skills/mcp-registry-sync.ts::registerMcpServersFromSnapshot(agentId, snapshot, mcpManager)` — idempotent addServer for every `ok: true` entry's `frontmatter.mcpServers`, tagged `source: 'skill'`. Two hook points: (1) `loadSnapshot` in `get-agent-skills.ts` calls it on both cache-hit and cache-miss paths, so the registry is re-asserted every session start (crucial after host restart since mcpManager is in-memory); (2) `syncToolModulesForSkill` calls it before `discoverAllTools` so a fresh approval's commit doesn't have to wait for a snapshot invalidation cycle. `GetAgentSkillsDeps.mcpManager` added as optional — existing test fixtures continue to work. Added 6-test unit suite for the helper, regression test in `tool-module-sync.test.ts` asserting addServer is called before discoverAllTools with `source: 'skill'`, three loadSnapshot tests (cache miss, cache hit after restart, optional mcpManager backwards-compat).
**Files touched:** src/host/skills/mcp-registry-sync.ts (new), src/host/skills/get-agent-skills.ts, src/host/skills/tool-module-sync.ts, src/host/server-init.ts, tests/host/skills/mcp-registry-sync.test.ts (new), tests/host/skills/tool-module-sync.test.ts, tests/host/skills/get-agent-skills.test.ts
**Outcome:** Success — `tsc` clean; 127/127 skills tests green. E2E verification in kind cluster: deployed fix as `debug-mcp-fix` image, audit log shows `syncToolModulesForSkill` correctly reached `discoverAllTools`, registry now has linear. Commit still `null` because of a separate second-layer bug — AX's mcp-client only speaks Streamable HTTP transport but Linear's `mcp.linear.app/sse` is SSE-only. Rolled cluster back to prior image; second layer to be fixed in a follow-up commit via `transport` frontmatter field.
**Notes:** No orphan sweep for `skill`-tagged `mcpManager.servers` entries — harmless leak because (a) no agent's toolServerMap routes to stale URLs after `discoverAllTools` clears their mappings, (b) leaked entries vanish on process restart. If a stricter sweep is needed later, walk all agents' snapshots and drop `source: 'skill'` entries not referenced by any. Scale analysis: lazy (hook in loadSnapshot) scales to 10k+ agents because work is proportional to session traffic, not agent population. Eager startup walk would hit readiness-probe cliff past ~1k agents on git-http.


## [2026-04-18 17:30] — Skill-creator prompt guardrails against Claude-Desktop format

**Task:** Admin reported the agent produced a `.ax/skills/linear/SKILL.md` that parsed as `[INVALID] — missing or unterminated YAML frontmatter`. Root-caused to the model pattern-matching to `claude_desktop_config.json` shape (`title:`, bare credential strings, `mcp: type: stdio, command: npx, args: [...]`, YAML inside a ```yaml fenced block) instead of reading `.ax/skills/skill-creator/SKILL.md` first. The existing skills module prompt just said "read skill-creator" — too soft against the model's "I know MCP configs" reflex.
**What I did:** (1) Hardened `src/agent/prompt/modules/skills.ts` "Creating new skills" section: changed "read ... and follow it" to "you MUST read ... **in full** before writing", added a five-bullet anti-pattern list (fenced yaml block, `title:` vs `name:`, bare cred strings, stdio MCP, unknown top-level keys), and added a self-correct hint pointing to next-turn `[INVALID]` feedback. (2) Added "Not this (common mistakes from training data)" section to `skills/skill-creator/SKILL.md` with a side-by-side WRONG/RIGHT comparison showing the exact shape the model tends to produce vs what the parser wants, plus a new "Don't use stdio MCP" entry in the Don'ts list. (3) New vitest regression test in `tests/agent/prompt/modules/skills.test.ts` pinning every anti-pattern string in the rendered prompt so future edits can't quietly drop them.
**Files touched:** src/agent/prompt/modules/skills.ts, skills/skill-creator/SKILL.md, tests/agent/prompt/modules/skills.test.ts
**Outcome:** Success — `tsc` clean; 18/18 skills module tests green. Purely prose/prompt change, no runtime code behavior touched.
**Notes:** This is advisory only — a stronger guarantee would be a post-write validator that surfaces the parse error same-turn. Deferred that in favor of prompt+skill text improvements since the existing next-turn `[INVALID] — <error>` feedback already enables self-correction on turn 2; the new guardrails should catch most cases on turn 1. The `skills/skill-creator/` directory is seeded into every agent workspace via `seedAxDirectory` at `server-completions.ts:454-486`, so existing agents get the updated text on their next first-run seed (new workspaces only — the seeder is first-run guarded by `!existsSync(destDir)`). To force-update an existing agent's `.ax/skills/skill-creator/SKILL.md`, the admin would need to delete it from the workspace so the seeder re-copies on next turn.


## [2026-04-18 17:15] — Fix: delete-then-re-add skill auto-enables without approval

**Task:** Admin reported that re-adding a previously deleted `linear` skill silently enables it — Approvals tab empty, Agents/Skills shows Enabled. Root cause: `loadAgentProjection` built `storedCredentials` and `approvedDomains` as flat sets keyed on `envName@scope` / `domain` (dropping skill_name), AND rows from the prior approval lingered under `skill_name='linear'` after the deletion.
**What I did:** Two-part fix. (1) Skill-scoped keys: `loadAgentProjection` now emits `${skillName}/${envName}@${scope}` and `${skillName}/${normalizedDomain}`; `computeSkillStates` + `computeSetupQueue` look up per-skill. Prevents cross-skill envName bleed (e.g., skill A's `GOOGLE_API_KEY@agent` row can't silently satisfy skill B). (2) Orphan sweep: added `sweepOrphanedRows(agentId, snapshot, deps)` that deletes rows whose `skill_name` isn't in the current workspace snapshot. Called from `getAgentSkills`, `getAgentSetupQueue`, `getAllowedDomainsForAgent` before projection. Added `deleteForSkill` to `SkillCredStore` + `SkillDomainStore` interfaces (SQLite + PG). Updated 6 test stubs across 4 test files; added regression tests for cross-skill contamination + orphan sweep + re-add surfaces setup card.
**Files touched:** src/host/skills/types.ts, src/host/skills/state-derivation.ts, src/host/skills/get-agent-skills.ts, src/host/skills/domain-allowlist.ts, src/host/skills/skill-cred-store.ts, src/host/skills/skill-domain-store.ts, tests/host/skills/{state-derivation-states,state-derivation-setup,get-agent-skills,skill-cred-store,skill-domain-store,domain-allowlist}.test.ts, tests/host/server-admin-skills.test.ts, tests/host/server-admin-refresh-tools.test.ts, .claude/skills/ax-provider-skills/SKILL.md
**Outcome:** Success — `npm run build` clean; 204/204 skills + admin tests green. Pre-existing `server.test.ts` EINVAL failures unrelated (macOS socket-path length, confirmed on stash'd main baseline).
**Notes:** Cross-skill value *reuse* still works via `approveSkillSetup` step 6 — when admin clicks Approve on skill B, the helper auto-fills from skill A's row for the same envName. The fix only blocks *auto-enabling* without admin action. Orphan sweep runs on every state read; it's idempotent and cheap. One edge case: a temporarily-empty snapshot (all skills absent from HEAD) will sweep every row for the agent — that's the intended invariant. Callers with an untrustworthy snapshot should skip the sweep.


## [2026-04-18 14:40] — Tool-modules Task 7: undo `.ax/.gitignore` seed

**Task:** Remove the `.ax/.gitignore` write from `seedAxDirectory` (added by fix 2, commit `56adaab5`). After Tasks 2-6, tool modules live in `.ax/tools/` as committed artifacts — gitignoring them is actively wrong. Also verify ax-agent/ax-runners/ax-provider-skills SKILL docs don't reference per-turn tool generation.
**What I did:** Deleted the 7-line block in `src/host/server-completions.ts::seedAxDirectory` (the comment + `axGitignorePath` existence check + write + `ax_seed_gitignore_failed` catch). Verified with grep: no remaining `.ax/.gitignore`/`tools/\n` references in `src/`, `tests/`, or `templates/`; no `gitignore` tokens anywhere in `server-completions.ts`; no SKILL.md under `.claude/skills/` mentions tool-modules/`.ax/tools`/`prepareMcpCLIs`/etc. (zero hits — no skill doc updates needed). Imports (`existsSync`, `writeFileSync`) stay since both are used by sibling blocks in the same function.
**Files touched:** src/host/server-completions.ts (-7 lines)
**Outcome:** Success — `npm run build` clean; 16/16 `tests/host/server-completions*` tests green. Broader `tests/host/server*` has 29 pre-existing failures in `server.test.ts`/`server-history.test.ts`/`server-multimodal.test.ts` (IPC socket setup in `ipc-server.ts:400`) — verified present on stash'd main baseline, unrelated to this change.
**Notes:** Pre-existing agents may still have `.ax/.gitignore` committed in their workspace from fix 2 — that's fine, the file is benign (matches a `tools/` dir the runner no longer writes to). Users can delete it manually if they want to commit tool modules there. Did not add cleanup code for this edge case per the task instructions.


## [2026-04-18 14:30] — Tool-modules Task 5: delete per-turn tool generation

**Task:** Delete the per-turn MCP tool-module generation flow — host-side MCP discovery + CLI/module writing via stdin payload + agent-side unpack into `.ax/tools/`. After Tasks 2-4, tool modules are committed to git at skill-approval time; the per-turn path is dead weight.
**What I did:** (1) Deleted the MCP tool-gen block in `server-completions.ts` (both `deps.mcpManager` + legacy `providers.mcp.listTools` branches, `mcpCLIsPayload` + `toolModuleIndex` declarations, and stdin-payload fields). (2) In `runner.ts`, dropped `StdinPayload.mcpCLIs`, `StdinPayload.toolModuleIndex`, `AgentConfig.toolModuleIndex`, the `parseStdinPayload` extractors, and the bin/ + `.ax/tools/` write blocks in `applyPayload`; removed now-unused `unlinkSync`, `statSync`, `sep` imports. (3) `agent-setup.ts`: deleted `scanMcpCLIs()` and its invocation, dropped `mcpCLIs` + `toolModuleIndex` from `buildSystemPrompt`'s `PromptContext` spread, removed 4 unused `node:fs`/`node:path` imports. (4) `prompt/types.ts`: removed `mcpCLIs` + `toolModuleIndex` from `PromptContext`. (5) `prompt/modules/runtime.ts`: removed the PTC + legacy-CLI render blocks (Task 6 re-adds tool-index rendering via filesystem scan of `.ax/tools/*/_index.json`). (6) Deleted the now-unused `prepareMcpCLIs` function from `toolgen/generate-and-cache.ts`, renamed `PrepareMcpCLIsOptions` → `PrepareToolModulesOptions`, dropped the re-export in `toolgen/index.ts`. (7) Updated the stale doc comment in `plugins/mcp-manager.ts`. (8) Trimmed the `prepareMcpCLIs` describe block + imports from `tests/host/toolgen/generate-and-cache.test.ts`.
**Files touched:** src/host/server-completions.ts (-65), src/agent/runner.ts (-73), src/agent/agent-setup.ts (-23), src/agent/prompt/modules/runtime.ts (-15), src/agent/prompt/types.ts (-4), src/host/toolgen/generate-and-cache.ts (-27), src/host/toolgen/index.ts (-1 re-export), src/plugins/mcp-manager.ts (comment), tests/host/toolgen/generate-and-cache.test.ts (-14)
**Outcome:** Success — `npm run build` clean; 397/397 targeted tests green (`tests/host/server-completions tests/agent/ tests/host/toolgen/`). Net -220 LOC. No test files deleted — no tests asserted on the dropped payload fields.
**Notes:** Answers to gating questions: (1) `deps.mcpManager` on `CompletionDeps` — KEPT; still used at `server-completions.ts:698` to pass into the fast-path `processFastPathInprocess`. Only the per-turn `discoverAllTools` call inside `processCompletion` was removed. (2) `providers.mcp?.listTools` — only the call site in the legacy fallback branch was deleted, provider contract stays. (3) Test deletions: ZERO files deleted. After Task 5 the prompt intentionally has no tool-module block — Task 6 re-adds it via the filesystem scan of `.ax/tools/*/_index.json` committed during approval/refresh.


## [2026-04-18 14:05] — Tool-modules Task 4: refresh-tools admin endpoint + UI button

**Task:** Add POST /admin/api/agents/:agentId/skills/:skillName/refresh-tools to regenerate a skill's committed `.ax/tools/<skill>/` tree on demand. Unlike approval, sync errors surface as 500 (admin clicked the button to see the result). Plus per-enabled-skill "Refresh tools" button on the dashboard's Skills tab.
**What I did:** TDD — wrote 11 failing tests in `tests/host/server-admin-refresh-tools.test.ts` covering happy path (asserts `syncToolModules` called with `reason: 'refresh'`), BetterAuth user threading, `defaultUserId` fallback, 404 on missing skill, 404 on pending skill, 200-with-zero-counts for enabled-but-no-mcp-servers, 500 surfacing with error message, audit `skill_tools_refreshed` success + error variants, 503 when `agentSkillsDeps` absent, 401 when unauthenticated. Added `reason?: 'approval' | 'refresh'` to `ToolModuleSyncInput` so the helper picks the commit message (`ax: regenerate tools for <skill>` vs `ax: refresh tools for <skill>`) — approval path keeps defaulting to the existing wording. Wired the route in `server-admin.ts` using `getAgentSkills` + `loadSnapshot` to gate on `kind: 'enabled'` and extract frontmatter `mcpServers`. UI: split the Skills-tab skill rendering into a new `SkillCard` component with per-card refresh state (in-flight spinner, success/error feedback banners via `useState`). Added `api.refreshTools` + `RefreshToolsResponse` type. Extended playwright tests in `ui/admin/tests/agent-tabs.spec.ts` with button-visibility + success-feedback coverage; fixed the pre-existing broken `beforeEach` that relied on a removed `<table>` row selector.
**Files touched:** src/host/server-admin.ts (new route), src/host/skills/tool-module-sync.ts (+ `reason` param), tests/host/server-admin-refresh-tools.test.ts (new), tests/host/skills/tool-module-sync.test.ts (+2 tests for commit-message variant), ui/admin/src/lib/types.ts (+ `RefreshToolsResponse`), ui/admin/src/lib/api.ts (+ `refreshTools` method), ui/admin/src/components/pages/agents-page.tsx (SkillCard + refresh UI), ui/admin/tests/agent-tabs.spec.ts (+3 tests, fixed beforeEach), ui/admin/tests/fixtures.ts (+ refresh-tools mock route)
**Outcome:** Success — 11/11 new backend tests green, all 12 playwright agent-tabs tests green (was 0/12 pre-change — fixed broken pre-existing selector). `npm run build` + admin-ui `npm run build` both clean. Full backend admin + skills scope (201 tests) green.
**Notes:** Decisions: (1) Zero-MCP-servers skill → 200 with `{ commit: null, moduleCount: 0, toolCount: 0 }` (mirrors the helper's "no tools" shape; no 400 — the state is valid, nothing to do). (2) `reason` lives on `ToolModuleSyncInput` not `ToolModuleSyncDeps` — keeps the helper's deps shape a stable per-host-process singleton. (3) Audit `result` uses `'error'` not `'failure'` (the provider type is `'success' | 'blocked' | 'error'` — plan spec said "failure", I normalized to match the existing contract). (4) Button is `disabled` while in-flight (standard pattern), hidden entirely for pending/invalid skills. (5) `resolveAuthenticatedUser` is threaded through the refresh route the same way the approve route does it — keeps user-scoped credential resolution consistent across the two write paths.


## [2026-04-18 13:26] — Tool-modules Phase 2 Task 2: `syncToolModulesForSkill` helper

**Task:** Per-skill tool-module generator: discover a skill's MCP tools, wrap each server into an ES module via `prepareToolModules`, and commit the files into `.ax/tools/<skill>/` on the agent's repo in one atomic commit. Approval-time entry point for the broader "tool modules are git-committed" migration (Task 3 wires it into the approval handler; Task 4 adds the refresh-tools admin button).
**What I did:** TDD — wrote 8 failing tests in `tests/host/skills/tool-module-sync.test.ts` covering: committed path layout, `_index.json` shape (skill/tools[]/generated_at + inputSchema as parameters), approval-time commit message + AX Host author, serverFilter contains the skill-declared server names, authForServer closure resolves creds via `resolveMcpAuthHeaders`, zero-tools skips `commitFiles` entirely, skillName traversal rejection (`../evil`, `foo/bar`), errors from `commitFiles` propagate. Implemented `src/host/skills/tool-module-sync.ts` — reuses the existing `resolveMcpAuthHeaders` export from `server-completions.ts` (no extraction needed) and the existing `prepareToolModules` from `toolgen/generate-and-cache.ts`. Files written: `<server>.js` per discovered server (from `prepareToolModules`), `index.js` barrel (from `prepareToolModules`), plus a new `_index.json` with the tool metadata. Path rewrite: flat `<server>.js` → `.ax/tools/<skillName>/<server>.js`.
**Files touched:** src/host/skills/tool-module-sync.ts (new), tests/host/skills/tool-module-sync.test.ts (new)
**Outcome:** Success — 8/8 new tests pass, all 105 tests in `tests/host/skills/` green, `npm run build` clean.
**Notes:** Design decisions captured in JSDoc: (1) `_index.json` includes full `inputSchema` as `parameters` — Task-6's tool-index-loader + future drift detection both want the full shape; dropping it for size is a pre-mature trim. (2) `moduleCount` counts ALL `.js` files (per-server modules + barrel) — the `_index.json` is metadata, not a loadable module. (3) Errors propagate — Task 3's `approveSkillSetup` caller is responsible for log-and-continue; this helper stays a primitive. (4) `skillName` safety: repo-relative path goes verbatim into `commitFiles`, so a `..` or `/` segment could silently land a commit at a bad path. `safePath` guards filesystem ops; here we fail-fast on `.`, `/`, `\`, `..`, `\0` before calling `commitFiles` at all.


## [2026-04-18 10:45] — Step 8: drop retired tables; delete dual-writes + backfill + listScopePrefix

**Task:** Step 8 (final step) of the single-source-of-truth redesign. Drop `skill_states` and `skill_setup_queue` tables, delete dual-write branches to `credential_store`, delete the startup backfill, and remove `listScopePrefix` (the backfill was its only caller). Leave `credential_store` alone — oauth-skills.ts (agent-initiated OAuth), inprocess.ts, server-completions.ts (process.env fallback), server-init.ts (MCP bearer lookups), and onboarding.ts still read/write it for non-skill concerns.

**What I did:**
1. TDD: added `skills_003_drop_retired_tables` tests to `tests/migrations/skills.test.ts` (drops both tables, idempotent, leaves skill_credentials + skill_domain_approvals intact). Verified RED → GREEN.
2. Added migration `skills_003_drop_retired_tables` to `src/migrations/skills.ts` with `ifExists()` drops and a re-create-only down migration for tooling completeness.
3. `server-admin-skills-helpers.ts`: dropped the `providers.credentials.set(..., scopeKey)` call from the approve handler's credential write loop. Dropped the `providers.credentials` guard at the top. Narrowed `ApproveDeps.providers` to `{ audit }`. Simplified the scope-key computation block (no more agentName / scopeKey variables).
4. `admin-oauth-flow.ts`: removed the two `input.credentials.set(...)` dual-writes (access_token + refresh blob). Dropped `credentials` from `ResolveCallbackInput`. Dropped the `effectiveUserId/scopeKey` block.
5. `server-request-handlers.ts`: removed `credentials: providers.credentials` from the `resolveCallback` call site. Updated skillCredStore comment.
6. Deleted `src/host/skills/backfill-skill-credentials.ts` + `tests/host/skills/backfill-skill-credentials.test.ts`. Removed the backfill invocation from `server-init.ts`.
7. Deleted `listScopePrefix` from `CredentialProvider` interface (`src/providers/credentials/types.ts`), from the database implementation (`src/providers/credentials/database.ts`), and from test mocks (7 test files: server-admin-skills, admin-oauth-flow, server-oauth-callback, server-admin-oauth-start, server-admin-oauth-providers, provider-sdk/harness, dotenv, providers/mcp/database).
8. Deleted the `listScopePrefix` tests from `tests/providers/credentials/database.test.ts` (case-sensitivity + GLOB-metachar rejection).
9. Rewrote approve-handler + admin-oauth-flow tests: credsSet spies replaced with `skillCredStoreMem.rows` / `skillCredStore.putCalls` assertions. Kept all security-critical invariants (credential values never in audit, atomic validation before any write, OAuth rejected through the API-key path, stale-card 404, etc.).
10. Cleaned up stale scope-string comments in server-admin.ts (approve route BetterAuth prefer-session comment refers to `user_id` column now, not `user:<agentName>:<userId>` scope strings).
11. Verified oauth-skills.ts stays self-contained — it writes its own blobs at unscoped `oauth:<name>` keys and reads what it writes. Not cross-coupled with admin-oauth-flow.

**Files touched:**
- Modified: `src/migrations/skills.ts`, `src/host/{server-admin-skills-helpers,server-admin,server-init,server-request-handlers,admin-oauth-flow}.ts`, `src/providers/credentials/{types,database}.ts`.
- Modified tests: `tests/migrations/skills.test.ts`, `tests/providers/credentials/database.test.ts`, `tests/providers/mcp/database.test.ts`, `tests/dotenv.test.ts`, `tests/provider-sdk/harness.test.ts`, `tests/host/{server-admin-skills,admin-oauth-flow,server-oauth-callback,server-admin-oauth-start,server-admin-oauth-providers}.test.ts`.
- Deleted: `src/host/skills/backfill-skill-credentials.ts`, `tests/host/skills/backfill-skill-credentials.test.ts`.

**Outcome:** Success. Build clean. Migration test (13 tests) green. Admin-oauth-flow (21), server-admin-skills (27), credential-db (16), MCP-db (4) all green. Full suite: 2543 passed / 33 pre-existing darwin EINVAL Unix-socket failures (not caused by this step; verified by checking they match the prior run's failure list exactly). 

**Notes:** `credential_store` stays as a general-purpose credential store — the skills subsystem just no longer reads or writes it. oauth-skills.ts (agent-initiated OAuth) still uses it at unscoped keys; MCP bearer lookups still use it; process.env fallback still uses it; onboarding wizard still writes to it. The `CredentialProvider` contract now has 4 methods (get/set/delete/list) instead of 5. Skills subsystem migration complete across 8 steps.

## [2026-04-18 10:17] — Step 7: collapse ProxyDomainList to per-request query

**Task:** Step 7 of the single-source-of-truth redesign. Replace the in-memory `ProxyDomainList` (builtins + skill-decl + admin-approved + pending queue) with a per-agent domain allowlist computed at session start from `skill_domain_approvals` joined with enabled skills.

**What I did:**
1. TDD: wrote `tests/host/skills/domain-allowlist.test.ts` first (10 tests covering builtins, enabled-only contribution, pending/invalid exclusion, no-leak on approval-without-declaration, normalization, per-agent isolation).
2. Created `src/host/skills/domain-allowlist.ts` — exports `BUILTIN_DOMAINS` + `getAllowedDomainsForAgent(agentId, agentSkillsDeps, skillDomainStore)`. Intersects declared domains on enabled skills with approval rows; unions `BUILTIN_DOMAINS`.
3. Added `listForAgent(agentId)` to `SkillDomainStore` (Kysely one-liner), plus a unit test.
4. Refactored `get-agent-skills.ts`: dropped the `proxyDomainList` dep from `GetAgentSkillsDeps` and added `skillDomainStore`. `loadApprovalsAndCreds` now reads `skill_domain_approvals` rows to build the approved-domains Set. Exported `loadSnapshot` + `normalizeDomain` for reuse by `domain-allowlist.ts`.
5. `server-init.ts`: deleted the seed-skills parsing block (file read + parseAgentSkill + `domainList.addSkillDomains`) — seed skills no longer get implicit global whitelisting. Removed `ProxyDomainList` construction and the `domainList` fields throughout. `agentSkillsDeps` now takes `skillDomainStore`.
6. `server-completions.ts`: at the proxy-start block, call `getAllowedDomainsForAgent(agentId, ...)` once and hand the frozen Set's `has` to `startWebProxy.allowedDomains`. Dropped `onDenied` (pending queue is gone). Dropped `domainList` from `CompletionDeps`; added `skillDomainStore`.
7. `server.ts` (host-process shared proxy): changed `allowedDomains` to `BUILTIN_DOMAINS`-only — the k8s shared proxy handles infrastructure traffic (admin dashboard callouts, host-side fetches), not agent traffic. Agent traffic routes through per-session proxies with per-agent allowlists.
8. `server-admin.ts`: deleted the three `/admin/api/proxy/domains{,/approve,/deny}` routes. Removed `domainList` from `AdminDeps`.
9. `server-admin-skills-helpers.ts`: removed `domainList` from `ApproveDeps`; removed the `domainList.approvePending(domain)` call — domain approvals now only write `skill_domain_approvals` rows.
10. `server-webhook-admin.ts`, `ipc-server.ts`: dropped `domainList` from opts.
11. Deleted `src/host/proxy-domain-list.ts` and `tests/host/proxy-domain-list.test.ts`.
12. Updated all affected tests: `get-agent-skills.test.ts`, `backfill-skill-credentials.test.ts`, `server-admin-skills.test.ts`, `server-admin.test.ts`, `server-admin-oauth-start.test.ts`, `server-admin-oauth-providers.test.ts`. Introduced `InMemorySkillDomainStore` fixture in both get-agent-skills and domain-allowlist tests. `approvePending` spies in admin-skills tests migrated to `skillDomainStoreMem.approve` assertions.
13. Updated web-proxy.ts doc comment for `allowedDomains` to describe the new session-frozen Set model.
14. Updated `.claude/skills/ax-web-proxy/SKILL.md` + `ax-provider-skills/SKILL.md` + `ax-host/SKILL.md` to reflect the new domain-allowlist architecture.

**Behavior shift (flagged per task):** Seed skills shipped in the AX bundle previously had their declared domains globally whitelisted at host boot (implicit approval for every agent). After step 7, seed skills behave like any other skill — an admin must approve them per-agent via the Approvals page for their domains to land in that agent's allowlist. This matches the single-source-of-truth stance: every allowed domain has a reason and an agent.

**Files touched:**
- Created: `src/host/skills/domain-allowlist.ts`, `tests/host/skills/domain-allowlist.test.ts`.
- Modified: `src/host/skills/{get-agent-skills,skill-domain-store}.ts`, `src/host/{server,server-init,server-completions,server-admin,server-admin-skills-helpers,server-webhook-admin,ipc-server,web-proxy}.ts`, 6 test files, 3 skill docs.
- Deleted: `src/host/proxy-domain-list.ts`, `tests/host/proxy-domain-list.test.ts`.

**Outcome:** Success. Build clean. 103 skills tests + 27 admin-skills tests + 10 new domain-allowlist tests + 4 domain-store tests green. Pre-existing socket-path test failures in `server.test.ts` / `server-history.test.ts` / `server-multimodal.test.ts` are environmental (macOS Unix socket path length; not caused by step 7 — verified by stashing the diff).

**Notes:** The per-session allowlist is frozen at session start. Two shared helpers live in `get-agent-skills.ts`: `loadSnapshot` and `loadAgentProjection` — the second returns `{approvalsBySkill, approvedDomainsFlat, storedCredentials}` in one pass so both state-derivation callers (flat sets) and `getAllowedDomainsForAgent` (per-skill buckets) share it. `normalizeDomain` lives in `domain-allowlist.ts` (domain concerns belong there). Code-quality review follow-ups on this commit: dropped the dead `onDenied` field from `WebProxyOptions` (no callers), collapsed the dual store guards in `server-init.ts` into one, removed can't-happen `if (!norm) continue` guards, tightened the proxy-startup comment, updated the `getAllowedDomainsForAgent` signature from 3-arg to 2-arg (drops the redundant `skillDomainStore` — it's already in `deps`). Host-process OAuth token exchange uses `fetch()` directly (Node.js fetch doesn't respect `HTTP_PROXY`), so the host-process proxy's BUILTIN_DOMAINS-only gate doesn't affect Google OAuth.

## [2026-04-18 09:45] — Step 6: delete reconciler pipeline; hook endpoint becomes cache-buster

**Task:** Step 6 of the single-source-of-truth redesign. Delete the reconciler pipeline now that the live `getAgentSkills` path is the only consumer of skills state. Collapse the post-receive hook to cache-bust duty.
**What I did:**
1. Added `invalidateAgent(agentId)` to `SnapshotCache` (prefix-match drop keyed by `${agentId}@`). Unit tests for the new method + guard against prefix-collision (`a1` vs `a10`).
2. Created `src/host/skills/state-derivation.ts` with the two pure helpers (`computeSkillStates`, `computeSetupQueue`). Updated `get-agent-skills.ts` to import from there. Renamed the two surviving reconciler tests to `state-derivation-{states,setup}.test.ts`.
3. Rewrote `src/host/skills/hook-endpoint.ts`: HMAC verify → Zod parse → `snapshotCache.invalidateAgent(agentId)` → `{ ok:true, invalidated: N }`. Deps simplified from `{ secret, reconcileAgent }` to `{ secret, snapshotCache }`.
4. Deleted source files: `reconcile-orchestrator.ts`, `reconciler.ts`, `state-store.ts`, `mcp-applier.ts`, `proxy-applier.ts`, `startup-rehydrate.ts`, `current-state.ts`.
5. Deleted test files: `reconcile-orchestrator.test.ts`, `reconcile.test.ts`, `state-store.test.ts`, `mcp-applier.test.ts`, `proxy-applier.test.ts`, `startup-rehydrate.test.ts`, `current-state.test.ts`, `e2e-reconcile.test.ts`, `reconciler-{mcp,events,allowlist}.test.ts`.
6. Rewrote `approveSkillSetup` in `server-admin-skills-helpers.ts` to use `getAgentSetupQueue` for the pending-card lookup and `getAgentSkills` for the post-write state read; dropped `skillStateStore` + `reconcileAgent` from `ApproveDeps`, added `agentSkillsDeps`. Post-write call: `snapshotCache.invalidateAgent(agentId)` so the live state read is fresh.
7. `server-admin.ts`: dropped `skillStateStore` + `reconcileAgent` from `AdminDeps`. `GET /admin/api/agents/:agentId/skills`, OAuth start, and DELETE `/admin/api/skills/setup/:agentId/:skill` all now read the live path via `agentSkillsDeps`. The DELETE endpoint no longer persists anything — it confirms existence + audits + returns `removed` boolean; the live queue regenerates on the next load.
8. `server.ts`: dropped `OrchestratorDeps` construction, `adminReconcileAgent`, and the `rehydrateSkillsForAgents` fire-and-forget block. Hook handler wiring is now `createReconcileHookHandler({ secret, snapshotCache: agentSkillsDeps.snapshotCache })`. Passed `snapshotCache` to `createRequestHandler` so the OAuth callback invalidates on success.
9. `admin-oauth-flow.ts::resolveCallback`: swapped `reconcileAgent` dep for optional `snapshotCache`; the fire-and-forget reconcile is replaced with a try/catch-wrapped `snapshotCache.invalidateAgent(agentId)` after token writes.
10. `server-request-handlers.ts`: `RequestHandlerOpts.reconcileAgent` → `snapshotCache`; OAuth callback forwards it into `resolveCallback`.
11. `server-webhook-admin.ts`: dropped `skillStateStore` + `reconcileAgent` from `AdminSetupOpts`.
12. `server-init.ts`: removed state-store + applier construction; `HostCore` type loses `stateStore`, `mcpApplier`, `proxyApplier`. `agentSkillsDeps` + `skillCredStore` stay.
13. Slimmed `types.ts`: kept `SkillSnapshotEntry`, `SkillStateKind`, `SkillState`, `SetupRequest`; renamed `ReconcilerCurrentState` → `SkillDerivationState` and dropped its unused `registeredMcpServers` + `priorSkillStates` fields; dropped `ReconcilerInput` + `ReconcilerOutput`.
14. Rewrote the affected test files (`server-admin-skills.test.ts`, `server-admin-oauth-start.test.ts`, `admin-oauth-flow.test.ts`, `server-oauth-callback.test.ts`, `hook-endpoint.test.ts`, `mount-route.test.ts`) to use a stubbed `agentSkillsDeps` that synthesizes the live queue from an in-memory `setupByAgentId` Map.

**Files touched:**
- Created: `src/host/skills/state-derivation.ts`.
- Renamed: `tests/host/skills/reconciler-{states,setup}.test.ts` → `state-derivation-{states,setup}.test.ts`.
- Modified: `src/host/skills/{get-agent-skills,hook-endpoint,snapshot-cache,types}.ts`, `src/host/{server,server-init,server-admin,server-admin-skills-helpers,server-request-handlers,server-webhook-admin,admin-oauth-flow}.ts`, `tests/host/{server-admin-skills,server-admin-oauth-start,server-admin,server-oauth-callback,admin-oauth-flow}.test.ts`, `tests/host/skills/{hook-endpoint,mount-route,snapshot-cache,get-agent-skills}.test.ts`.
- Deleted: `src/host/skills/{reconcile-orchestrator,reconciler,state-store,mcp-applier,proxy-applier,startup-rehydrate,current-state}.ts` and 11 corresponding test files.

**Outcome:** Success. Net -3020 LOC. `npm run build` clean. Targeted tests (`tests/host/server-admin{,-skills,-oauth-start}.test.ts tests/host/server-oauth-callback.test.ts tests/host/admin-oauth-flow.test.ts tests/host/skills/`) → 192/192 pass. Full suite delta vs main: identical (same 33 pre-existing EINVAL socket failures in `server.test.ts` + friends, none introduced by this step). Residual grep of every deleted symbol (`reconcileAgent`, `OrchestratorDeps`, `SkillStateStore`, etc.) → 0 hits in `src/` or `tests/`. Migrations for `skill_states` / `skill_setup_queue` intentionally left alone (step 8's job); `ProxyDomainList` likewise (step 7).

**Notes:**
- Approve handler's "stale-scope recovery" reuse path still works — it listsForAgent and picks the first row whose envName matches, preferring `userId` > `''` > other.
- DELETE `/admin/api/skills/setup/:agentId/:skill` is now effectively informational: returns `removed: true` when the card is in the live queue, audits it, but doesn't mutate persistent state. Next GET will show the card again unless the underlying facts changed.
- Only gotcha during the refactor: the `admin-oauth-flow.ts` callback's `snapshotCache.invalidateAgent` must be wrapped in try/catch — the old fire-and-forget reconcile swallowed errors, and stripping the catch would break the "swallow failure" contract tests rely on.

## [2026-04-18 09:20] — Step 5 amendment: kill dead `agentName` field + no-op store fallback

**Task:** Code-review feedback on the step 5 commit. Two Important issues: (1) `GetAgentSkillsDeps.agentName` is a dead field after the credential-reads moved to `skillCredStore`; delete it everywhere. (2) Replace the no-op `SkillCredStore` fallback in `server-init.ts` with a fail-fast check — DB-less installs don't run `processCompletion` in the test suite (every config uses `credentials: database`, which itself refuses to boot without `providers.database`), so the fallback is dead code.
**What I did:**
1. Dropped `agentName: string` from `GetAgentSkillsDeps` (src/host/skills/get-agent-skills.ts). Removed the field from every caller (`server-init.ts`, the two test fixture builders, backfill test setup) — 14 call sites total; net-negative diff.
2. Replaced the 6-line no-op store construction in `server-init.ts` with a fail-fast `if (!skillCredStore) throw new Error(...)` guard. Simplified the backfill call to drop the `if (skillCredStore && providers.credentials && providers.database)` gate since those invariants are now established earlier.
3. Cleaned one narrative comment in `tests/host/admin-oauth-flow.test.ts` ("Old dual-write..." → "Scope-string write ...") and trimmed "phase-5's approve handler" from a comment in `admin-oauth-flow.ts`.
4. Renamed `src/host/credential-scopes.ts` → `src/host/session-credential-context.ts` via `git mv` (only session helpers remain; the filename now matches). Updated the single importer (`server-completions.ts`) and the skill doc (`ax-provider-credentials/SKILL.md`).

**Files touched:**
- Modified: `src/host/skills/get-agent-skills.ts`, `src/host/server-init.ts`, `src/host/admin-oauth-flow.ts`, `src/host/server-completions.ts`, `.claude/skills/ax-provider-credentials/SKILL.md`, `tests/host/skills/get-agent-skills.test.ts`, `tests/host/skills/backfill-skill-credentials.test.ts`, `tests/host/server-admin-skills.test.ts`, `tests/host/admin-oauth-flow.test.ts`.
- Renamed: `src/host/credential-scopes.ts` → `src/host/session-credential-context.ts`.

**Outcome:** Success. `npm run build` clean. Targeted suite (`tests/host/skills tests/host/server-completions tests/host/server-admin-skills.test.ts tests/providers/credentials tests/host/server-admin.test.ts tests/host/admin-oauth-flow.test.ts`) → 270/270 pass. Amended commit c4a17995 with same message.

## [2026-04-18 09:00] — Step 5: credential injection via `skill_credentials` tuple lookup

**Task:** Step 5 of the single-source-of-truth redesign. Switch every READ of credentials to the tuple-keyed `skill_credentials` table, backfill historical rows, then delete `credentialScope` / `resolveCredential` / the bulk of the scope-string plumbing.
**What I did:**
1. Extended `SkillCredStore` with `get(input)`, `listForAgent(agentId)`, `listEnvNames(agentId)`. `get` prefers user_id match then falls back to `''`; `listForAgent` returns every row for the agent; `listEnvNames` returns distinct envNames. Unit tests per method.
2. Rewrote turn-time injection in `src/host/server-completions.ts` (~line 1180) to call `deps.skillCredStore.listForAgent(agentId)`. Sorts user-scoped rows first, then injects each applicable row as placeholder (web_proxy) or real value (subprocess). Added `skillCredStore` (required) to `CompletionDeps`.
3. Threaded `skillCredStore` through `server-init.ts`. DB-less installs get a no-op store (`put/get/list*` all no-op) so `CompletionDeps` stays total.
4. Rewrote `loadApprovalsAndCreds` in `src/host/skills/get-agent-skills.ts` to populate the `storedCredentials` Set from `skillCredStore.listForAgent(agentId)` — `user_id === ''` → `${envName}@agent`, non-empty → `${envName}@user`. Dropped the `credentials: CredentialProvider` dep from `GetAgentSkillsDeps`; replaced with `skillCredStore: SkillCredStore`.
5. OAuth callback (`src/host/admin-oauth-flow.ts::resolveCallback`) now dual-writes: the legacy `credentials.set(..., scopeKey)` stays AND `skillCredStore.put({agentId, skillName, envName, userId: '' | resolvedUserId, value})` lands alongside. Same for the refresh blob. `ResolveCallbackInput` grows a required `skillCredStore` field.
6. Threaded `skillCredStore` through `server-request-handlers.ts` (callback path guarded on `adminOAuthFlow && skillCredStore`) and added it to `server.ts`'s `createRequestHandler` call.
7. Swapped the Approvals-page probe in `src/host/server-admin.ts` to `skillCredStore.listEnvNames(a.id)` per agent. Dropped the `listScopePrefix`+`list` calls.
8. Swapped the approve-handler reuse path in `src/host/server-admin-skills-helpers.ts`: scans `skillCredStore.listForAgent(body.agentId)` for rows matching the envName; prefers caller's userId, then `''`. Inlined `user:<agent>:<userId>` / `agent:<agent>` scope strings for the lingering `credentials.set(...)` dual-write (step 8 removes it). Dropped the `credentialScope` import.
9. Added backfill module `src/host/skills/backfill-skill-credentials.ts` + unit tests. Scans each active agent's declared skills; for each credential, pulls legacy-scope values (agent → `credential_store[agent:<name>]`; user → iterate `listScopePrefix('user:<name>:')`) and writes them into `skill_credentials`. Pre-loads existing tuple rows to keep re-runs idempotent (the `rowsBackfilled` counter only increments on value changes).
10. Wired backfill into `server-init.ts` — runs once after agent registration, before ipcServer starts. Logs `backfill_complete`.
11. Deleted `credentialScope` and `resolveCredential` from `src/host/credential-scopes.ts`; kept the session-context helpers (`setSessionCredentialContext` et al). Inlined `agent:<name>` in `current-state.ts` to unblock deletion (no semantic change).
12. Deleted dead tests: `tests/host/credential-scopes.test.ts`, `tests/host/credential-injection-integration.test.ts`. Updated `tests/host/skills/get-agent-skills.test.ts` to use an `InMemorySkillCredStore` stub instead of the old credential-provider stub. Updated `tests/host/admin-oauth-flow.test.ts` (15 tests) to thread the new `skillCredStore` arg; added agent-scope + tuple-put assertions. Updated `tests/host/server-admin-skills.test.ts::makeAgentSkillsDeps` to default a no-op store. Updated `tests/host/server-oauth-callback.test.ts` to thread `skillCredStore` through the request-handler harness.

**Files touched:**
- Modified: `src/host/skills/skill-cred-store.ts`, `src/host/skills/get-agent-skills.ts`, `src/host/skills/current-state.ts`, `src/host/server-completions.ts`, `src/host/server-init.ts`, `src/host/server.ts`, `src/host/server-admin.ts`, `src/host/server-admin-skills-helpers.ts`, `src/host/admin-oauth-flow.ts`, `src/host/server-request-handlers.ts`, `src/host/credential-scopes.ts`, `src/host/oauth-skills.ts`.
- Created: `src/host/skills/backfill-skill-credentials.ts`, `tests/host/skills/backfill-skill-credentials.test.ts`.
- Modified: `tests/host/skills/skill-cred-store.test.ts`, `tests/host/skills/get-agent-skills.test.ts`, `tests/host/server-admin-skills.test.ts`, `tests/host/admin-oauth-flow.test.ts`, `tests/host/server-oauth-callback.test.ts`.
- Deleted: `tests/host/credential-scopes.test.ts`, `tests/host/credential-injection-integration.test.ts`.

**Outcome:** Success. `npm run build` clean. Targeted suite (`tests/host/skills tests/host/server-completions tests/host/server-admin-skills.test.ts tests/providers/credentials tests/host/server-admin.test.ts`) → 248/248 pass. Full suite: 2631 passed, 33 darwin-socket-path failures (pre-existing), 24 skipped.

**Notes:**
- Semantic tightening acknowledged: credential injection is now restricted to what SKILL.md files declare, AND what has a matching row in `skill_credentials`. The old three-scope `providers.credentials.list(...)` blanket inject is gone. Any agent that relied on arbitrary env values sitting in `credential_store` without being mentioned by a skill will stop seeing them — which is the point.
- `listScopePrefix` is retained in `src/providers/credentials/{database,types}.ts` because `current-state.ts` (retiring in step 6) AND the backfill still call it. Step 6 removes current-state.ts; step 8's cleanup can then drop `listScopePrefix` once the backfill is also gone.
- MCP `bearerCredential` flow is unchanged: the applier writes `Authorization: Bearer ${ENV}` as a placeholder; the web-proxy resolves via the per-session `credentialMap`, which we now populate from `skill_credentials`. No MCP manager changes were needed.

## [2026-04-18 08:24] — Step 4: push skills via stdin payload; delete `skills_index` IPC

**Task:** Step 4 of the single-source-of-truth redesign (`docs/plans/2026-04-18-skills-single-source-of-truth-design.md` §8). Replace the agent's IPC pull of its skills index with a host-side push: the host computes `getAgentSkills(agentId, deps)` per turn and includes the result in the stdin payload. Delete the `skills_index` IPC end-to-end and the filesystem fallback.
**What I did:**
1. Added `agentSkillsDeps: GetAgentSkillsDeps` (required) to `CompletionDeps` in `src/host/server-completions.ts`. Moved the construction of `agentSkillsDeps` (plus its `getBareRepoPath` / `probeHead` / snapshot-cache plumbing) out of `server.ts` and into `initHostCore` so `completionDeps` is fully populated at construction; `HostCore` now exposes `agentSkillsDeps` + `getBareRepoPath` for the reconcile orchestrator in `server.ts`.
2. In `processCompletion`, compute `skillsPayload = (await getAgentSkills(agentId, deps.agentSkillsDeps)).map(toSkillSummary)` and include it in the stdin JSON as `skills`. Added `toSkillSummary` helper — `{ name, description: s.description ?? '', kind, ...(pendingReasons? ? { pendingReasons } : {}) }`, with `error` dropped.
3. Added `skills?` to `StdinPayload` in `src/agent/runner.ts`, extracted it in `parseStdinPayload`, applied it to `config.skills` in `applyPayload`.
4. Collapsed `buildSystemPrompt` skills derivation in `src/agent/agent-setup.ts` to `const skills = config.skills ?? []`. Deleted `fetchSkillsIndex`, removed it from both runners (`pi-session.ts`, `claude-code.ts`), dropped unused imports.
5. Deleted the `skills_index` IPC action end-to-end: `SkillsIndexSchema` from `src/ipc-schemas.ts`, the handler from the IPC-handlers module (file `git mv`d from `skills.ts` → `audit.ts`, function renamed `createSkillsHandlers` → `createAuditHandlers`), the `stateStore?` handler option from `createIPCHandler` in `src/host/ipc-server.ts` (since that option only fed the deleted handler), and the `stateStore` wire-through in `createIPCHandler({...})` in `src/host/server-init.ts`. `stateStore` itself stays — reconciler + admin endpoints still own it until step 6.
6. Removed `loadSkills` and `loadSkillsMultiDir` from `src/agent/stream-utils.ts` (orphaned post-fallback-removal), and trimmed the corresponding test sections in `tests/agent/stream-utils.test.ts`.
7. Deleted dedicated IPC tests (`tests/host/ipc-handlers/skills-index.test.ts`, `tests/host/ipc-server-stateStore.test.ts`, `tests/ipc/skills-index-schema.test.ts`). Rewrote `tests/agent/agent-setup.test.ts` around the new contract (payload-only, no IPC, no filesystem scan). Inverted the StdinPayload test in `tests/sandbox-isolation.test.ts` (now asserts `skills?` IS declared on `StdinPayload`). Added `parseStdinPayload` cases for `skills`. Added `tests/host/server-completions-skills-payload.test.ts` around the `toSkillSummary` mapping (exported for test reach). Removed `skills_index` from `tests/agent/tool-catalog-sync.test.ts` knownInternalActions.
8. Updated three `.claude/skills/` docs to match (`ax-agent`, `ax-ipc`, `ax-runners`) — drop `skills_index` from the IPC action tables, replace the prompt-time fetch narrative with "pushed via stdin payload, no fallback."

**Files touched:**
- Modified: `src/ipc-schemas.ts`, `src/host/ipc-server.ts`, `src/host/server-init.ts`, `src/host/server-completions.ts`, `src/host/server.ts`, `src/agent/agent-setup.ts`, `src/agent/runner.ts`, `src/agent/runners/pi-session.ts`, `src/agent/runners/claude-code.ts`, `src/agent/stream-utils.ts`.
- Renamed: `src/host/ipc-handlers/skills.ts` → `src/host/ipc-handlers/audit.ts` (only holds `audit_query` now).
- Modified: `tests/agent/agent-setup.test.ts`, `tests/agent/runner.test.ts`, `tests/agent/stream-utils.test.ts`, `tests/agent/tool-catalog-sync.test.ts`, `tests/sandbox-isolation.test.ts`.
- Created: `tests/host/server-completions-skills-payload.test.ts`.
- Deleted: `tests/host/ipc-handlers/skills-index.test.ts`, `tests/host/ipc-server-stateStore.test.ts`, `tests/ipc/skills-index-schema.test.ts`.
- Modified: `.claude/skills/ax-agent/SKILL.md`, `.claude/skills/ax-ipc/SKILL.md`, `.claude/skills/ax-runners/SKILL.md`.

**Outcome:** Success. `npm run build` clean. Targeted suite (`tests/host/skills tests/agent tests/host/server-completions.test.ts tests/host/server-completions-skills-payload.test.ts tests/host/server-admin-skills.test.ts tests/sandbox-isolation.test.ts tests/ipc`) → 608/608 pass. Broader suite shows only the pre-existing darwin-socket-path failures (same as on clean main).

**Notes:** Snapshot cache stays authoritative: when the admin approves creds + domains, the approve handler already bumps `skillDomainStore`/`skillCredStore` and then kicks `adminReconcileAgent`, which exercises `getAgentSkills` on the next `processCompletion` — no extra cache invalidation plumbing needed for this step.

## [2026-04-18 08:02] — Step 3: Approvals page reads live via `getAgentSetupQueue`

**Task:** Step 3 of the single-source-of-truth redesign (`docs/plans/2026-04-18-skills-single-source-of-truth-design.md` §8). Rewire `GET /admin/api/skills/setup` to derive cards live from the git snapshot instead of reading `skill_setup_queue`. Approve endpoint untouched.
**What I did:**
1. Added `getAgentSetupQueue(agentId, deps)` in `src/host/skills/get-agent-skills.ts` alongside `getAgentSkills`. Reuses the module-private `loadSnapshot` + `loadApprovalsAndCreds` helpers (same snapshot cache, same deps shape), calls `computeSetupQueue(snapshot, current)` from `reconciler.ts`, returns `Promise<SetupRequest[]>`.
2. Rewired `GET /admin/api/skills/setup` in `src/host/server-admin.ts` to loop active agents and call `getAgentSetupQueue(a.id, deps.agentSkillsDeps)` instead of `deps.skillStateStore.getSetupQueue(a.id)`. 503 precondition now checks `!deps.agentSkillsDeps`. `hasExistingValue` decoration preserved as-is.
3. Added 3 new unit tests alongside `getAgentSkills`: pending-card-in-a-mix, empty-queue, and missing-credential cases for `getAgentSetupQueue`.
4. Rewrote the 5 `GET /admin/api/skills/setup` tests in `tests/host/server-admin-skills.test.ts` to use real bare repos (`initBareRepo` + `seedRepo` helpers copied from `get-agent-skills.test.ts`), a `ProxyDomainList`, and a new `InMemoryCredentialProvider`. Added a `makeAgentSkillsDeps` helper that shells out to `git rev-parse HEAD` for `probeHead` so the snapshot cache keys stay stable per-test.
5. Added a `hasExistingValue=true` regression test: pending card emitted by the reconciler, while the endpoint's credential probe sees a different scope's stored entry — asserts the hint flips on.

**Files touched:**
- Modified: `src/host/skills/get-agent-skills.ts`, `src/host/server-admin.ts`, `tests/host/skills/get-agent-skills.test.ts`, `tests/host/server-admin-skills.test.ts`.

**Outcome:** Success. `npm test -- tests/host/skills tests/host/server-admin-skills.test.ts tests/host/server-admin.test.ts` → 205/205 pass. `npm run build` clean. Broader suite shows only the pre-existing darwin-socket-path failures (verified by `git stash` → same failures on clean main).

**Notes:** Test fixture gotcha: `buildSnapshotFromBareRepo` runs `git ls-tree refs/heads/main` so a `git init --bare` with no commits produces a 500. Seeding every test repo with at least `README.md` gives `main` a commit to resolve against. For the `hasExistingValue=true` test, used two separate `InMemoryCredentialProvider` instances — one for `deps.providers.credentials` (endpoint probe), one for `agentSkillsDeps.credentials` (reconciler view). Same instance in production, two instances in the test to isolate the pending-card path from the hint path.

## [2026-04-18 07:35] — Step 2: `skill_credentials` + `skill_domain_approvals` tables + dual-write

**Task:** Step 2 of the single-source-of-truth redesign (`docs/plans/2026-04-18-skills-single-source-of-truth-design.md` §8). Land tuple-keyed Kysely tables + dual-write from the approve handler. Read paths stay on old tables.
**What I did:**
1. **Migration `skills_002_tuple_tables`** in `src/migrations/skills.ts`. Tables: `skill_credentials (agent_id, skill_name, env_name, user_id)` with an empty-string sentinel for `user_id` meaning "agent-scoped, shared across users" (Postgres disallows NULL in PK); `skill_domain_approvals (agent_id, skill_name, domain)`. Both use `sqlEpoch(dbType)` integer timestamps like `skills_001_initial`. Indexes on `(agent_id, skill_name)` for both tables.
2. **Store modules** (Shape B per plan recommendation): `src/host/skills/skill-cred-store.ts` exposes `put(agentId, skillName, envName, userId, value)` with `INSERT ON CONFLICT DO UPDATE SET value, updated_at`; `src/host/skills/skill-domain-store.ts` exposes `approve(agentId, skillName, domain)` with `INSERT ON CONFLICT DO NOTHING`. Factories match the `state-store.ts` style: typed interface, `create…(db)` factory, ESM `.js` suffix imports, no default export. YAGNI on read methods — step 5/7 will add them.
3. **Dual-write in `approveSkillSetup`** (`src/host/server-admin-skills-helpers.ts`). Added `skillCredStore?`, `skillDomainStore?` to `ApproveDeps`. After the existing `providers.credentials.set(...)` call, optionally invoke `skillCredStore.put({…, userId: entry.scope === 'user' ? userId : ''})`. After the existing `domainList.approvePending(domain)` call, optionally invoke `skillDomainStore.approve(...)`. Stores are optional on the dep shape so test fixtures that construct partial `AdminDeps` keep working; in production wiring (server-init → server.ts → setupAdminHandler → createAdminHandler) they're always present.
4. **Composition-root wiring.** `server-init.ts` now constructs `createSkillCredStore(db)` + `createSkillDomainStore(db)` alongside `createSkillStateStore(db)` in the `if (providers.database)` block, and includes them in the returned `HostCore`. `server.ts` destructures + threads into `setupAdminHandler`. `server-webhook-admin.ts` + `server-admin.ts`'s `AdminDeps` both get the two optional fields.
5. **Tests.**
   - `tests/migrations/skills.test.ts` (new) — 10 tests covering migration idempotency, both tables' PK shape, empty-string sentinel semantics, and that the two expected indexes get created.
   - `tests/host/skills/skill-cred-store.test.ts` (new) — 5 tests: insert, upsert on same tuple, distinct user_ids, sentinel co-exists with real userId, preserves `created_at` on conflict.
   - `tests/host/skills/skill-domain-store.test.ts` (new) — 3 tests: insert, idempotent repeat, distinct tuples make distinct rows.
   - `tests/host/server-admin-skills.test.ts` (extended) — 4 new tests in a `dual-writes skill_credentials + skill_domain_approvals` describe block: agent-scope writes `userId=''`, user-scope writes caller userId, stores absent back-compat, end-to-end against real SQLite Kysely.

**Files touched:**
- New: `src/host/skills/skill-cred-store.ts`, `src/host/skills/skill-domain-store.ts`, `tests/migrations/skills.test.ts`, `tests/host/skills/skill-cred-store.test.ts`, `tests/host/skills/skill-domain-store.test.ts`.
- Modified: `src/migrations/skills.ts`, `src/host/server-admin-skills-helpers.ts`, `src/host/server-admin.ts`, `src/host/server-init.ts`, `src/host/server-webhook-admin.ts`, `src/host/server.ts`, `tests/host/server-admin-skills.test.ts`.

**Outcome:** Success — `npm test -- tests/host/skills tests/migrations tests/host/server-admin-skills.test.ts tests/host/server-admin.test.ts` 216/216 pass; full `tests/host/` suite shows the 29 pre-existing darwin-socket-path failures (verified unchanged against clean main), zero new failures. `npm run build` clean.

**Notes:** Picked Shape B (store modules) over Shape A (inline DB in helper) because it matches `state-store.ts`'s pattern and makes step 5's read-path cutover slot in cleanly. Used JS-computed `Math.floor(Date.now() / 1000)` for `updated_at` inside `put` rather than a dialect-aware `sqlEpoch()` raw builder — the column default still uses `sqlEpoch()` for initial inserts via the migration, but explicit ON CONFLICT update sets an epoch integer computed in JS. Dialect-neutral, matches the `credential_store` provider's ISO-string-via-JS approach in style.

## [2026-04-18 07:50] — Step 2 amend: clock unification + required ApproveDeps + doc trim

**Task:** Address reviewer feedback on f0585416: (1) one clock not two — DB-side timestamps throughout, (2) kill dead-code paranoia by making stores required on `ApproveDeps`, (3–6) nits: trim WHY headers, drop task-preface comments in tests, trim JSDoc that references caller/phase, remove a 1.1s `setTimeout` that didn't tighten the assertion.
**What I did:**
1. **Clock unification.** Dropped explicit `created_at` / `updated_at` / `approved_at` from the store inserts — now rely on the migration's `sqlEpoch(dbType)` defaults. In `skill-cred-store.put`'s `doUpdateSet`, `updated_at` uses `sqlEpoch(dbType)` too (factory now takes a `DbDialect` arg). `skill-domain-store.approve` is `DO NOTHING` so needs no update expression. One clock (DB), both dialects.
2. **Required-on-`ApproveDeps`.** `skillCredStore` + `skillDomainStore` are now non-optional on `ApproveDeps`. Dropped the `if (deps.skillCredStore)` / `if (deps.skillDomainStore)` guards in the apply loops. The route handler at `/admin/api/skills/setup/approve` narrows via `const { skillCredStore, skillDomainStore } = deps; if (!…) return 503;` before calling, then spreads `{ ...deps, skillCredStore, skillDomainStore }` into `approveSkillSetup`. `AdminDeps` / `AdminSetupOpts` / `HostCore` keep them optional (legitimate no-DB boot path). Test fixture (`mockDeps`) now pairs the stores with `withStateStore` — they're wired together in production, same gate in tests. Replaced the "stores absent back-compat" test with one that asserts 503 when the route has no stores.
3. **Nits.** Trimmed the WHY header in `skill-domain-store.ts` to one line. Removed `// Step 2 of …` prefaces from all three new test files + the describe-block preface in `server-admin-skills.test.ts`. Trimmed JSDoc on `AdminDeps.skillCredStore/skillDomainStore` and `HostCore.skillCredStore/skillDomainStore` to match the clean `AdminSetupOpts` comment — "Tuple-keyed skill credential store." without caller reference. Deleted the 1.1s `setTimeout` in `skill-cred-store.test.ts`'s updated_at test — with DB-side timestamps the assertion still holds as `>=` without the wait.

**Files touched (amend):**
- Modified: `src/host/skills/skill-cred-store.ts`, `src/host/skills/skill-domain-store.ts`, `src/host/server-admin-skills-helpers.ts`, `src/host/server-admin.ts`, `src/host/server-init.ts`, `tests/host/skills/skill-cred-store.test.ts`, `tests/host/skills/skill-domain-store.test.ts`, `tests/migrations/skills.test.ts`, `tests/host/server-admin-skills.test.ts`.

**Outcome:** Success — `npm test -- tests/host/skills tests/migrations tests/host/server-admin-skills.test.ts tests/host/server-admin.test.ts` 216/216 pass; `npm run build` clean.

**Notes:** TS narrowing on the `deps` object directly doesn't carry through to an argument of the `AdminDeps` → `ApproveDeps` call (the property type on the declared object type stays optional). Pulling the fields into locals + narrowing the locals + spreading them back in (`{ ...deps, skillCredStore, skillDomainStore }`) is the minimal-friction pattern. One shared `sqlEpoch(dbType)` RawBuilder used in both the migration default and the `doUpdateSet` — any future stores that need `updated_at` on conflict can pick up the same helper.

## [2026-04-18 07:20] — Step 1: `getAgentSkills` + snapshot cache alongside reconciler

**Task:** Implement step 1 of the skills single-source-of-truth migration (`docs/plans/2026-04-18-skills-single-source-of-truth-design.md`): land a live-computed `getAgentSkills(agentId, deps)` next to the existing reconciler pipeline. Rewire the per-agent Skills tab to use it. No deletions.
**What I did:**
1. **Snapshot cache.** `src/host/skills/snapshot-cache.ts` — tiny LRU keyed on arbitrary strings, `{ get, put, clear, size }`. Map-insertion-order LRU, move-to-end on `get`, evict oldest on over-bound put. Tests: `tests/host/skills/snapshot-cache.test.ts` (6 tests, all green).
2. **Live loader.** `src/host/skills/get-agent-skills.ts` — calls `probeHead(agentId)` → checks cache keyed `${agentId}@${headSha}` → on miss resolves `getBareRepoPath(agentId)` + `buildSnapshotFromBareRepo(path, 'refs/heads/main')` → parallel-loads `approvedDomains` (from `ProxyDomainList.getAllowedDomains`) + `storedCredentials` (from `credentials.list(agent:<name>)` + `credentials.listScopePrefix('user:<name>:')`, same `${envName}@agent` / `${envName}@user` key shape as `current-state.ts`) → composes via the existing `computeSkillStates` helper. Returns `SkillState[]`. Tests: `tests/host/skills/get-agent-skills.test.ts` (7 tests, all green — enabled/pending/invalid coverage, empty-repo, cache-hit avoids I/O even when the bare repo is deleted mid-test, cache-miss on sha change, direct reconciler-equivalence check on a realistic mix).
3. **Server.ts wiring.** Hoisted `getBareRepoPath` out of the `if (stateStore)` block (it's now useful to both the reconciler *and* the new live path). Added `probeHead` closure using `git ls-remote <bareRepoPath> refs/heads/main` (no network for local bare repos; empty repo → `<empty>` sentinel). Added a process-scoped `snapshotCache` with `maxEntries: 1024`. Bundled everything into `agentSkillsDeps: GetAgentSkillsDeps` and passed it into `setupAdminHandler`.
4. **Admin handler wiring.** Extended `AdminDeps` + `AdminSetupOpts` with optional `agentSkillsDeps`. In `GET /admin/api/agents/:agentId/skills` the handler prefers `getAgentSkills(agentId, deps.agentSkillsDeps)` when present; falls back to `deps.skillStateStore.getStates(agentId)` otherwise. Response shape `{ skills: SkillState[] }` unchanged — UI, fixtures, Playwright tests all still pass.
5. **No deletions.** `state-store.ts`, `reconciler.ts`, `reconcile-orchestrator.ts`, `current-state.ts`, `mcp-applier.ts`, `proxy-applier.ts`, `startup-rehydrate.ts` untouched. Steps 2–6 will retire them.

**Files touched:**
- New: `src/host/skills/snapshot-cache.ts`, `src/host/skills/get-agent-skills.ts`, `tests/host/skills/snapshot-cache.test.ts`, `tests/host/skills/get-agent-skills.test.ts`.
- Modified: `src/host/server.ts`, `src/host/server-admin.ts`, `src/host/server-webhook-admin.ts`.

**Outcome:** Success — `npm test -- tests/host/skills` 131/131 pass (20 files); `tests/host/server-admin` 83/83; `npm run build` clean. Full `npm test` shows the usual 33 darwin-socket-path failures, zero new.

**Notes:** The correctness test (`matches the reconciler pipeline on the same snapshot + current state`) rebuilds the reconciler's `storedCredentials` set inline from `listScopePrefix` + `list` so both paths see the same inputs — this proves `getAgentSkills` output is identical, which is the step-1 "validate correctness" bar. Cache key uses `<empty>` sentinel for repos with no commits; that's a correct cache state (snapshot is empty-array, which stays valid until next push). `AdminDeps.skillStateStore` guard kept as a fallback so any test fixture that wires only the old dep still works — steps 2–6 will drop it.

## [2026-04-18 00:15] — Auto-fill existing credential values on skill approve

**Task:** User wanted "ignore existing credentials when a new skill is added" OR "auto-populate in the approval screen" so the approve dance doesn't require retyping tokens for cross-skill sharing and doesn't require hand-deleting DB rows to recover from stale-scope credentials.
**What I did:**
1. **Approve handler reuse path** (`src/host/server-admin-skills-helpers.ts`). When the approve body omits a value for a credential the setup card lists, the handler now looks it up at `user:<agentName>:*` (any userId under this agent via `listScopePrefix`) then `agent:<agentName>`, and re-stores whatever it finds at the caller's current-scope key. That caller-scope key is `user:<agentName>:<sessionUserId>` under BetterAuth or `agent:<agentName>` for agent-scoped creds, per the prior fix. Net: a stale-scope credential (e.g. `user:ax:default` left over from the pre-BetterAuth-session-id build) gets rewritten to the right scope on one-click approve. Same mechanism also handles cross-skill sharing (e.g. drafting a Google Drive skill after Google Calendar already approved `GOOGLE_API_KEY` — the second approval requires zero typing). Audit log now splits envNames into `envNames` (fresh entry) vs `reusedEnvNames` (auto-filled from storage) so the trail distinguishes admin-provided from silently-carried-forward values. OAuth creds are excluded from reuse since they flow through the OAuth machinery.
2. **Setup card decoration** (`src/host/server-admin.ts`). The `GET /admin/api/skills/setup` handler probes the credential store once per request (`listScopePrefix('user:<agentName>:')` + `list('agent:<agentName>')`) and annotates each `missingCredential` with a `hasExistingValue: boolean` hint. Lets the UI know which fields are optional-for-reuse vs. genuinely-new. Keeps the `SetupRequest` shape stored in the state store unchanged — the hint is a response-only decoration.
3. **Admin UI UX** (`ui/admin/src/lib/types.ts`, `ui/admin/src/components/pages/approvals-page.tsx`). Added `hasExistingValue?: boolean` to `SetupCardCredential`. Relaxed the "Approve" button-disable rule: an api-key credential blocks submit only when its input is empty AND `hasExistingValue` is false — otherwise blank means "reuse existing" and the server auto-fills. Added a small in-label hint "reusing existing value; paste a new one to replace" with the input placeholder flipping to "Leave blank to reuse existing".
4. **Tests.** `tests/host/server-admin-skills.test.ts`: updated two assertions that did verbatim equality on `cards` to account for the new `hasExistingValue: false` field each missingCredential now carries.
**Files touched:** `src/host/server-admin-skills-helpers.ts`, `src/host/server-admin.ts`, `ui/admin/src/lib/types.ts`, `ui/admin/src/components/pages/approvals-page.tsx`, `tests/host/server-admin-skills.test.ts`.
**Outcome:** Success — `npm run build` clean; admin UI typecheck clean; `tests/host/server-admin` 83/83 pass; full vitest matches pre-change baseline (same 33 pre-existing darwin socket-path failures, zero new).
**Notes:** Doesn't fully solve "delete skill → re-draft → expect new approval card": the reconciler's prefix-match for stored credentials still reports the skill as `enabled` without producing a setup card. To get the full loop, add a reconciler pass that forces newly-appearing skills (`priorSkillStates.get(name) === undefined`) with declared credentials into `pending` with a synthetic setup queue entry, so first-install admin review is required even when creds are already stored. That's the next logical step; held off because it broadly touches `reconcile.test.ts` and `reconciler-*.test.ts` fixtures.

## [2026-04-17 23:30] — Approve handler uses BetterAuth session userId so credentials land at the right scope

**Task:** User approved a Zendesk skill through the admin dashboard, saw it flip to `[ENABLED]` in the per-agent Skills tab, but the chat agent still said "credentials haven't been provided" and `env | grep -i zendesk` in the sandbox returned empty. Reconciler saw the creds (skill went enabled) but they never got injected at turn start.
**Root cause:** Credential **storage** (`server-admin-skills-helpers.ts:129`) used `credentialScope(agentName, body.userId ?? defaultUserId ?? 'admin')`. Admin UI didn't send `body.userId`, so approvals landed at `user:ax:default` (the container's `process.env.USER` fallback). Credential **injection** at turn start (`server-completions.ts:1161`) uses `credentialScope(agentId, currentUserId)` — where `currentUserId` is the BetterAuth session user.id. These never match, so the lookup fell through to `agent:ax` (no match — cred was user-scoped) then to `undefined` (no match) and never got injected. Meanwhile `loadCurrentState` for the reconciler uses `listScopePrefix("user:ax:")`, which is a prefix match — it happily found the credential at ANY user scope, so the skill flipped to `enabled` even though no chat turn could ever see it.
**What I did:**
1. Added `resolveAuthenticatedUser?: (req) => Promise<{id, email?} | undefined>` to `AdminDeps` and piped it through `AdminSetupOpts` / `setupAdminHandler`.
2. In `server.ts`, constructed the resolver when `providers.auth` is populated (i.e. BetterAuth is configured): dynamic-imports `authenticateRequest` and returns `{id, email}` from the session, or `undefined` if the request has no valid session.
3. In `server-admin.ts`'s `/admin/api/skills/setup/approve` branch, call `resolveAuthenticatedUser(req)` before dispatching. If we get a user AND the body didn't explicitly set `userId`, substitute the session user.id into the approve body. That way credentials land at `user:<agentName>:<sessionUserId>` — the same scope key `server-completions.ts:1161` builds at turn start.
4. Behavior unchanged for deployments without external auth (resolver is `undefined`) and for approvals that pass an explicit `userId` in the body.
**Files touched:** `src/host/server-admin.ts`, `src/host/server-webhook-admin.ts`, `src/host/server.ts`.
**Outcome:** Success — `npm run build` clean; `tests/host/server-admin` all 83 pass; full vitest suite matches pre-change baseline (same 33 pre-existing darwin socket-path failures, zero new).
**Notes:** This is the minimal fix. Longer term: (a) admin endpoints should also enforce BetterAuth auth when `externalAuth=true` (currently they skip the admin-token check and nobody re-verifies the session — the resolver here reads the session for userId but doesn't require it for access); (b) the scope-prefix vs scope-exact asymmetry between reconciler and completion deserves a single canonical helper; today they've been diverging. For this session: user redeploys → re-approves their existing Zendesk skill (so creds get re-stored at the real UUID scope), OR we backfill by copying `user:ax:default:<envName>` → `user:ax:<uuid>:<envName>` (one-off SQL). The re-approve path is simpler.

## [2026-04-17 23:15] — Fetch-on-demand git-http mirror so reconciler can read pushed refs

**Task:** After the Helm chart fix wired `AX_HOOK_SECRET` + `AX_HOST_URL`, the post-receive hook started reaching the host (✅), but the reconciler then failed with `fatal: cannot change to '/home/ax/.ax/repos/ax': No such file or directory`. In the k8s + git-http deployment the bare repo lives in the git-server pod, not on the host filesystem — the original resolver only worked for `git-local` layouts.
**Root cause:** `server.ts`'s `getBareRepoPath` hardcoded `join(axHome(), 'repos', agentId)`, which is the canonical location for git-local. For git-http, the host has no bare repo at that path; it has to fetch one from the git-server service. Comment at the call site even flagged this: "phase-2 task 9 will need its own reconcile path for git-http."
**What I did:**
1. `src/host/skills/reconcile-orchestrator.ts`: loosened the `OrchestratorDeps.getBareRepoPath` return type from `string` to `string | Promise<string>` and added `await` at the call site. Existing sync test mocks still satisfy the type — no test changes needed.
2. `src/host/server.ts`: rewrote `getBareRepoPath` to branch on `config.providers.workspace`. git-local / unset: return the local path unchanged. git-http: resolve the clone URL via `providers.workspace.getRepoUrl`, and either `git clone --mirror <url> <local>` (first touch per agent per host process) or `git -C <local> fetch --prune origin` (subsequent calls). Cached in a module-scope `seededMirrors` Set so we don't re-clone every reconcile. The mirror lives at the same `axHome/repos/<agentId>` path the reconciler expects, so `buildSnapshotFromBareRepo` stays unchanged.
3. Added `dirname` to the `node:path` import, `mkdirSync` to `node:fs`, and `execFile` + `promisify` for running git.
**Files touched:** `src/host/skills/reconcile-orchestrator.ts`, `src/host/server.ts`.
**Outcome:** Success — `npm run build` clean; 118/118 skills tests pass unchanged. User needs a new image at this SHA and a helm upgrade to pick it up; next skill push should complete the full loop (sidecar → git-server → post-receive hook → host HMAC check → reconciler fetches mirror → parses SKILL.md → writes to state store → admin dashboard Approvals tab surfaces the setup card).
**Notes:** Per-agent fetch cost is a single `git fetch --prune` against the in-cluster git-server, which is a ~fast operation over HTTP. The `seededMirrors` Set survives host-process lifetime; a pod restart re-clones on first touch, which is the same cost pattern as `startup-rehydrate`. If we ever want instant cold-start reconcile, we could fold the mirror maintenance into startup-rehydrate too — but that's optional.

## [2026-04-17 23:05] — Split admin dashboard: top "Approvals" for pending setup, per-agent "Skills" for the full inventory

**Task:** Top-level "Skills" tab only ever showed things needing approval (setup cards), which is better named "Approvals" — and there was nowhere in the dashboard to see what skills an individual agent actually has. Wanted to keep the approval workflow at the top (it spans all agents) and move the per-agent skill list into the agent detail sidebar (next to Identity/Workspace/Memory).
**What I did:**
1. **Top-level rename.** `ui/admin/src/App.tsx`: `Page` union `'skills'` → `'approvals'`, `NAV_ITEMS` label "Skills" → "Approvals" (kept the `Sparkles` icon). `git mv skills-page.tsx → approvals-page.tsx`, renamed the component `SkillsPage` → `ApprovalsPage`, updated the heading to "Approvals", empty-state copy to "Nothing to approve. Nice." and the error-state copy to "Failed to load approvals". Tagline now clarifies it's for pending skills.
2. **Per-agent Skills section.** `ui/admin/src/components/pages/agents-page.tsx`: added `'skills'` to `SectionId`, placed a new "Skills" nav entry under the TOOLS group (above Connectors), wired a `SkillsTab` component that renders each skill's state as an ENABLED/PENDING/INVALID pill (emerald/amber/rose), its description below, and for pending skills a bulleted list of `pendingReasons` (drawn straight from the reconciler output).
3. **New backend endpoint.** `GET /admin/api/agents/:agentId/skills` in `src/host/server-admin.ts`, returning `{ skills: SkillState[] }` straight from `deps.skillStateStore.getStates(agentId)`. 503 when the state store isn't wired, same pattern as the setup-queue endpoint.
4. **Types + api client.** Added `AgentSkillsResponse` to `ui/admin/src/lib/types.ts` (re-uses the existing `SkillState` type) and `api.agentSkills(agentId)` in `ui/admin/src/lib/api.ts`.
5. **Playwright tests.** `git mv skills.spec.ts → approvals.spec.ts`, renamed the top-level describe "Skills Page" → "Approvals Page", all 16 `?page=skills` references → `?page=approvals`, heading check "Skills" → "Approvals", empty-state text "All your skills are set up" → "Nothing to approve". `navigation.spec.ts` button check "Skills" → "Approvals". `agent-tabs.spec.ts` expanded from five sections to six (adds Skills) + two new tests: one listing both enabled and pending skills with their state labels, and one verifying pending reasons are surfaced per skill. `fixtures.ts` gained `MOCK_AGENT_SKILLS` + a route mock for `/admin/api/agents/*/skills`; removed the now-dead `mockCredentialRequests` / `MOCK_CREDENTIAL_REQUESTS` exports (leftovers from the prior request_credential deletion that this work surfaced).
**Files touched:** `src/host/server-admin.ts`, `ui/admin/src/App.tsx`, `ui/admin/src/components/pages/agents-page.tsx`, `ui/admin/src/components/pages/approvals-page.tsx` (renamed from skills-page.tsx), `ui/admin/src/lib/api.ts`, `ui/admin/src/lib/types.ts`, `ui/admin/tests/agent-tabs.spec.ts`, `ui/admin/tests/approvals.spec.ts` (renamed from skills.spec.ts), `ui/admin/tests/fixtures.ts`, `ui/admin/tests/navigation.spec.ts`.
**Outcome:** Success — `npm run build` clean, admin UI typecheck clean, full vitest suite matches pre-change baseline (same 33 pre-existing darwin socket-path failures, zero new).
**Notes:** The two views share the same underlying state store — approvals are the subset of skills whose `kind === 'pending'` with non-empty setup queue entries. The per-agent Skills tab reads the union of states (enabled + pending + invalid) so operators can audit what an agent has even when nothing's pending. Both views still share the `mockSkillsSetup` / `MOCK_SKILL_SETUP` fixtures since the `/admin/api/skills/setup` endpoint is unchanged.

## [2026-04-17 22:50] — Delete request_credential tool; skill-creator drives approvals through the dashboard

**Task:** After the skill-creator flow started working, the agent was double-asking for credentials: once via `request_credential` IPC (chat modal) and again via the reconciler setup card (admin dashboard). User wanted one true path. Also needed to teach the skill-creator to write a "what to do when pending" section into generated skills, and to render skill state more prominently in the system prompt so the agent sees pending state before opening the SKILL.md.
**What I did:**
1. **Agent-facing tool gone.** Removed `request_credential` from `src/agent/tool-catalog.ts` and `src/agent/mcp-server.ts`. Dropped the `'credential'` category from `ToolCategory`. Tool count drops from 14 → 13.
2. **IPC action + handler gone.** Removed `CredentialRequestSchema` from `src/ipc-schemas.ts` and the `credential_request` handler from `src/host/ipc-handlers/skills.ts`. `SkillsHandlerOptions` no longer takes `requestedCredentials` or `eventBus` — just `stateStore`.
3. **Server plumbing pruned.** Dropped `requestedCredentials` map creation + forwarding from `server-init.ts` (HostCore field + construction), `ipc-server.ts` (handler opts), and `server-completions.ts` (session cleanup). Removed the `credentialRequestQueue` + `unsubscribeCredentialRequests` fields from `HostCore`, the queue from `AdminDeps`/`AdminSetupOpts`, and deleted `src/host/credential-request-queue.ts` entirely.
4. **HTTP endpoints gone.** Removed `/v1/credentials/provide` (from `server-request-handlers.ts`), `/admin/api/credentials/requests`, `/admin/api/credentials/provide` (from `server-admin.ts`), and the `credential.required` SSE named-event forwarding in `server-request-handlers.ts`.
5. **Chat UI gone.** Deleted `ui/chat/src/components/credential-modal.tsx`. Removed `CredentialRequiredEvent` type + `onCredentialRequired` option from `ui/chat/src/lib/ax-chat-transport.ts` and the `credential_required` named-event branch in the SSE processor. Simplified `ui/chat/src/App.tsx` + `useAxChatRuntime.tsx` accordingly.
6. **Admin UI gone.** Removed the `CredentialRequestCard` component (~100 lines), `credentialRequests()` + `provideCredential()` from `ui/admin/src/lib/api.ts`, `CredentialRequest` + `CredentialRequestsResponse` types from `ui/admin/src/lib/types.ts`, and the "Credential Requests" section from `skills-page.tsx`.
7. **Skill-creator rewrites.** `skills/skill-creator/SKILL.md` no longer tells the agent to call `request_credential` — instead it explicitly says "Credentials are provided through the admin dashboard — not through the chat." Added a new section 2 ("Body structure") requiring every generated skill to include a "When pending" section with a template telling the next-turn agent to stop and say the skill is awaiting approval rather than trying raw `fetch()` / `execute_script` / `npx` as a workaround. Both inline examples (Linear + Bitbucket) now include the "When pending" section.
8. **Prompt renders state more prominently.** `src/agent/prompt/modules/skills.ts` now prepends `[ENABLED]` / `[PENDING]` / `[INVALID]` state labels to every skill bullet, and when any pending/invalid skill is present, prefixes the "Available skills" list with a warning paragraph that says only `[ENABLED]` skills are usable and DOs/DON'Ts for pending ones.
9. **Tests.** Deleted `tests/agent/tool-catalog-credential.test.ts`, `tests/ipc-schemas-credential.test.ts`, `tests/host/credential-request-queue.test.ts`, `tests/host/ipc-handlers/skills-credential.test.ts`. Updated `tests/agent/tool-catalog.test.ts` + `tests/agent/mcp-server.test.ts` + `tests/agent/ipc-tools.test.ts` + `tests/sandbox-isolation.test.ts` for the 14→13 count and removed-tool asserts. Rewrote the `SkillsModule` format assertions in `tests/agent/prompt/modules/skills.test.ts` and `tests/agent/agent-setup.test.ts` for the new `[STATE]` bullet shape. Removed the "Credentials" describe blocks from `tests/host/server-admin-skills.test.ts`. Pruned `skills: skillsDir` (a misshaped string pretending to be a SkillSummary[]) from 9 call sites in `tests/agent/runners/pi-session.test.ts` — it only ever worked because the old prompt code accidentally tolerated strings.
**Files touched:** `src/agent/tool-catalog.ts`, `src/agent/mcp-server.ts`, `src/agent/prompt/modules/skills.ts`, `src/ipc-schemas.ts`, `src/host/ipc-handlers/skills.ts`, `src/host/ipc-server.ts`, `src/host/server-completions.ts`, `src/host/server-init.ts`, `src/host/server-request-handlers.ts`, `src/host/server-admin.ts`, `src/host/server-webhook-admin.ts`, `src/host/server.ts`, `src/host/credential-request-queue.ts` (deleted), `skills/skill-creator/SKILL.md`, `ui/chat/src/App.tsx`, `ui/chat/src/components/credential-modal.tsx` (deleted), `ui/chat/src/lib/ax-chat-transport.ts`, `ui/chat/src/lib/useAxChatRuntime.tsx`, `ui/admin/src/components/pages/skills-page.tsx`, `ui/admin/src/lib/api.ts`, `ui/admin/src/lib/types.ts`, and 8 test files (4 deleted, 4 updated).
**Outcome:** Success — `npm run build` clean; `npx vitest run` matches the pre-change baseline (35 failures in 5 files, all pre-existing darwin socket-path-length issues in `server.test.ts`, `server-history.test.ts`, `server-multimodal.test.ts`, and 2 integration smoke tests — confirmed identical count via `git stash` comparison).
**Notes:** User wanted seed-on-existing-agent backfill separately; that was reverted per their request. They'll destroy+recreate the agent to test this round. The admin dashboard's Skills page now only shows setup cards (from the reconciler's setup queue); there's no parallel "Credential Requests" section anymore — one approval path, always.

## [2026-04-17 21:20] — Add skill-creator default skill, drop placeholder default.md

**Task:** Ship a seed skill that teaches agents how to author a new AX skill end-to-end, so when a user asks for a capability the agent doesn't have (e.g. "show me our team's Linear tickets"), the agent drafts `.ax/skills/<name>/SKILL.md` instead of improvising with `execute_script` + `npm install`.
**What I did:**
- Deleted `skills/default.md` (was 23 lines of safety rules already covered by the system prompt — it seeded into every workspace but did nothing useful).
- Added `skills/skill-creator/` (directory-based seed, ends up at `.ax/skills/skill-creator/SKILL.md` in each new agent's workspace via `server-init.ts` + `server-completions.ts` seed loops).
- Body derived from Anthropic's `anthropics/skills` skill-creator (Apache 2.0, preserved `LICENSE` + attribution in the SKILL.md + in a dedicated LICENSE file). Stripped their eval-viewer/benchmark infrastructure (we don't ship that). Retargeted output from Claude.ai's skill shape to AX's strict frontmatter schema (`name`, `description`, `source`, `credentials[]`, `mcpServers[]`, `domains[]` per `src/host/skills/frontmatter-schema.ts`).
- AX-specific guardrails the skill teaches the agent: don't run git (sidecar auto-commits); MCP-first; fall back to `npx -y` (registry.npmjs.org is already in the BUILTIN proxy allowlist at `src/host/proxy-domain-list.ts:10`); never `npm install -g`; always declare service hostnames in `domains[]` or the proxy denies them; admin approval gate is real (users should expect a 1-turn delay before the skill is live).
- Worked examples inline for Linear (MCP-available case) and Bitbucket (npx fallback case).
- Tidied two stale source comments in `server-init.ts:229` and `server-completions.ts:396,405` that cited `default.md` — replaced with parameterized `<name>.md` phrasing so they don't rot when `skills/` changes.
**Files touched:** `skills/default.md` (deleted), `skills/skill-creator/SKILL.md` (new, 204 lines), `skills/skill-creator/LICENSE` (new, Apache 2.0 copy), `src/host/server-init.ts`, `src/host/server-completions.ts`
**Outcome:** Success — `npm run build` clean; `parseSkillFile()` against the skill-creator frontmatter returns `{ok:true}` with the exact schema the reconciler expects; 118/118 tests in `tests/host/skills/` pass; no remaining code refs to `default.md`.
**Notes:** Rationale-level background from the user's Linear chat log that motivated this: the agent ran `npm install @linear/sdk` (which actually succeeded — registry.npmjs.org was on the allowlist) then failed at the `api.linear.app` call because that host wasn't declared anywhere. The skill-creator closes exactly this loop by teaching the agent to declare per-service hostnames in the skill's `domains[]` at authoring time.

## [2026-04-17 16:10] — Phase 6 follow-up: fix 2 blocking semgrep findings

**Task:** Address two blocking semgrep findings on `feat/skills-phase6-oauth`: (1) `gcm-no-tag-length` in `admin-oauth-providers.ts` — AES-256-GCM cipher constructors didn't pin `authTagLength: 16`, letting Node technically accept shorter forged tags; (2) `raw-html-format` in `server-request-handlers.ts` — the OAuth callback response template interpolated `adminResult.reason` via `escapeHtml()`, which semgrep's regex didn't understand.
**What I did:** (1) Passed `{ authTagLength: 16 }` as the 4th arg to both `createCipheriv` and `createDecipheriv`; added a block comment explaining why the explicit pin matters even though our blob layout already forces a 16-byte tag slice. Added a `decryptSecret`-truncation test that strips 2 bytes off the tag and expects `toThrow()` — structural blob guard still passes at that size, so only the cipher-layer pin rejects it. (2) Replaced the template string with a module-scoped `OAUTH_CALLBACK_HTML` lookup table keyed by the closed `reason` union (`token_exchange_failed | invalid_response | error`) plus `success`; each value is a static string literal, so no variable interpolation, so no semgrep hit. The new copy is also friendlier per CLAUDE.md voice. Removed the now-unused `escapeHtml` helper. Updated `server-oauth-callback.test.ts` to assert on the new friendly substring ("didn't look right") instead of the raw `invalid_response` enum identifier.
**Files touched:** `src/host/admin-oauth-providers.ts`, `src/host/server-request-handlers.ts`, `tests/host/admin-oauth-providers.test.ts`, `tests/host/server-oauth-callback.test.ts`.
**Outcome:** Success. Semgrep: 0 findings (down from 2 blocking). Target tests: 44/44 pass across `admin-oauth-providers.test.ts`, `server-oauth-callback.test.ts`, `admin-oauth-flow.test.ts`. `tsc --noEmit` clean.
**Notes:** Scope discipline — left the `req.headers.host` pattern alone (semgrep's warning mentioned "host portion" but the flagged rule was on the reason interpolation).

## [2026-04-17 15:30] — Phase 6 follow-up: 5 CodeRabbit review fixes on PR #181

**Task:** Address all 5 CodeRabbit findings on PR #181 (phase 6 OAuth): provider-name case sensitivity, init-order bug for admin.token + OAuth key derivation, x-forwarded-proto scheme validation, fire-and-forget reconcile docstring/impl mismatch, and un-tracked 30s setTimeout after unmount.
**What I did:** (1) Normalized provider names to lowercase in `AdminOAuthProviderStore.{get,upsert,delete}` so `'Linear'` and `'linear'` can't produce duplicate rows. (2) Hoisted `admin.token = randomBytes(32).toString('hex')` into `initHostCore` BEFORE `deriveOAuthKey` — the previous order left OAuth provider store disabled on fresh installs with no `AX_OAUTH_SECRET_KEY`. (3) Whitelisted `X-Forwarded-Proto` to `http`/`https`, case-normalized, fall back to socket.encrypted otherwise. (4) Replaced `await input.reconcileAgent(...)` with `void input.reconcileAgent(...).catch(logger.warn)` so the OAuth success HTML returns to the browser immediately (UI polls for state). (5) Tracked per-envName 30s connect timers in a ref Map; unmount effect clears all pending timers.
**Files touched:** `src/host/admin-oauth-providers.ts`, `src/host/server-init.ts`, `src/host/server-admin.ts`, `src/host/admin-oauth-flow.ts`, `ui/admin/src/components/pages/skills-page.tsx`, plus test additions in `tests/host/admin-oauth-providers.test.ts`, `tests/host/server-admin-oauth-start.test.ts`, `tests/host/admin-oauth-flow.test.ts`.
**Outcome:** Success. All 258 backend tests pass (26 files, including 3 new test cases for case normalization, 2 for x-forwarded-proto, and an unhandled-rejection guard on the fire-and-forget reconcile). tsc clean at root and ui/admin. 13/13 Playwright skills.spec.ts pass.
**Notes:** Pre-existing `admin_token_generated` log-leak in `createAdminHandler` was left alone per scope guidance. The `createAdminHandler` defensive auto-gen is now a no-op on the normal path but still protects tests that construct AdminDeps directly.

## [2026-04-17 14:17] — Phase 6 complete: OAuth PKCE + admin-registered providers

**Task:** Implement phase 6 of git-native skills: OAuth PKCE flow for skill credentials, with admin-registered provider fallback for confidential-flow providers (Google etc).
**What I did:** 9 commits across backend (Tasks 1-4) and UI (Task 5), plus live E2E verification (Task 6) and documentation (Task 7). Tests: 135 backend + 23 Playwright pass. tsc clean at root and ui/admin.
**Files touched:** See commits 1a229e8d..1500a6b8.
**Outcome:** Success. The full Connect → authorize → callback → credential persistence → reconcile flow works against a live local host with a mock token endpoint. Credential values, client_secrets, and tokens never appear in audit, logs, or HTTP responses; secret-leak substring guards are locked in tests.
**Notes:** Kind-ax image rebuild was again skipped (pre-dates phase 4). Background refresh-on-read is deferred — the refresh blob is stored for a future phase. Admin UI for OAuth provider CRUD is deferred — operators POST directly via curl for now.

## [2026-04-17 18:15] — Phase 6 Task 6: end-to-end OAuth verification against local host

**Task:** Live-wire verification of the phase-6 admin-initiated OAuth flow. Walk GET `/skills/setup` → POST `/oauth/start` → GET `/v1/oauth/callback/linear` → DB inspection against a real phase-6 host, with a tiny mock token endpoint standing in for the OAuth provider. Module tests (135+ across admin-oauth-flow/server-admin-oauth-start/server-oauth-callback/admin-oauth-providers) already cover behavior; this task just proves the code boots + the full HTTP path executes against a live process.
**What I did:** Option A (local host, phase-5 precedent). (1) `npm run build` clean. (2) Wrote `/tmp/ax-phase6-verify/ax.yaml` — SQLite + `apple` sandbox + `admin.token=phase6-verify-token-abc12345678` + `agent_name: verify-agent`. (3) Wrote `/tmp/ax-phase6-verify/mock-token-server.js` — a 30-line node HTTP server listening on 127.0.0.1:9077, responds to `POST /token` with `{access_token:"at-fake-xyz", refresh_token:"rt-fake-abc", expires_in:3600, token_type:"bearer"}` and appends every request body to `/tmp/ax-phase6-verify/mock-token.log`. (4) Started host via `AX_HOME=/tmp/ax-phase6-verify AX_HOOK_SECRET=verify-hook-secret BIND_HOST=127.0.0.1 node dist/cli/index.js serve --port 8099`; default agent `verify-agent` auto-registered; host logged `oauth_secret_key_derived_from_admin_token` (expected — no AX_OAUTH_SECRET_KEY set, phase-6 Task 1 followup soft-degrade path is healthy with a 30-char admin.token). (5) Wrote `/tmp/ax-phase6-verify/seed.js` using `better-sqlite3` from the ax node_modules to upsert one row into `skill_setup_queue` + `skill_states` for skill `e2e-linear` with an OAuth missingCredential whose `tokenUrl` pointed at `http://127.0.0.1:9077/token`, `provider:linear`, `scope:user`. (6) Ran the flow end-to-end, captured outputs, killed host + mock server cleanly.

**The flow:**

1. **GET /admin/api/skills/setup** — returned the seeded card verbatim (agentId=verify-agent, skillName=e2e-linear, missingCredentials=[{envName:LINEAR_TOKEN, authType:oauth, scope:user, oauth:{provider:linear, clientId:e2e-client-id, authorizationUrl, tokenUrl:http://127.0.0.1:9077/token, scopes:[read,write]}}]).

2. **POST /admin/api/skills/oauth/start** with body `{"agentId":"verify-agent","skillName":"e2e-linear","envName":"LINEAR_TOKEN"}` →
   ```json
   {
     "authUrl": "https://linear.app/oauth/authorize?client_id=e2e-client-id&redirect_uri=http%3A%2F%2F127.0.0.1%3A8099%2Fv1%2Foauth%2Fcallback%2Flinear&scope=read+write&response_type=code&code_challenge_method=S256&code_challenge=a802D2IWIA0Bkv6Wn4lk1HvTIq37uVtq-w1u95dlan0&state=d90e22f08d2df73894095e20638996a2",
     "state": "d90e22f08d2df73894095e20638996a2"
   }
   ```
   All PKCE fields present (code_challenge_method=S256, 43-char code_challenge, state). Host logged `admin_oauth_flow_started` with `hasAdminOverride:false`.

3. **GET /v1/oauth/callback/linear?code=fake-auth-code&state=d90e22f08d2df73894095e20638996a2** → HTTP 200, `<html><body><h2>Authentication successful</h2><p>You can close this tab and return to the dashboard.</p></body></html>`. Exactly the admin-matched success HTML from `server-request-handlers.ts`.

4. **Mock token server observed POST body** (verbatim from `/tmp/ax-phase6-verify/mock-token.log`):
   ```
   grant_type=authorization_code
   &code=fake-auth-code
   &redirect_uri=http%3A%2F%2F127.0.0.1%3A8099%2Fv1%2Foauth%2Fcallback%2Flinear
   &client_id=e2e-client-id
   &code_verifier=koLUTPXM9Q-103xPKWmFnO3LKEHdyPziOmlgvf_nEDQ
   ```
   All four required params present. `client_secret` correctly absent (no admin-registered provider for `linear`). Content-Type `application/x-www-form-urlencoded` — RFC 6749 §4.1.3 compliant.

5. **credential_store** — SQL: `SELECT scope, env_name, length(value) FROM credential_store WHERE env_name LIKE 'LINEAR_TOKEN%'`:
   ```
   scope=user:verify-agent:vpulim  env_name=LINEAR_TOKEN              value_len=11   (value="at-fake-xyz")
   scope=user:verify-agent:vpulim  env_name=LINEAR_TOKEN__oauth_blob  value_len=180
   ```
   Blob parsed: `{"access_token":"at-fake-xyz","refresh_token":"rt-fake-abc","expires_at":1776453180,"token_url":"http://127.0.0.1:9077/token","client_id":"e2e-client-id","scopes":["read","write"]}` — exactly the shape declared in `admin-oauth-flow.ts#resolveCallback`.

6. **audit_log** — SQL: `SELECT timestamp, action, session_id, args FROM audit_log WHERE action LIKE 'oauth_%' ORDER BY timestamp DESC`:
   ```
   oauth_callback_success  sessionId=verify-agent  args={"agentId":"verify-agent","skillName":"e2e-linear","envName":"LINEAR_TOKEN","provider":"linear","hasRefreshToken":true}
   oauth_start             sessionId=verify-agent  args={"agentId":"verify-agent","skillName":"e2e-linear","envName":"LINEAR_TOKEN","provider":"linear","hasAdminProvider":false}
   ```
   Substring-checked the concatenated args JSON for `"at-fake-xyz"` and `"rt-fake-abc"` — neither present. Secret-leak invariant holds.

7. **Replay defense** — re-issued the same callback URL a second time → HTTP 400 with "Invalid or expired OAuth flow" HTML. Confirms single-use state: admin-oauth-flow's `claim()` returned undefined on the second call, path fell through to agent-initiated oauth-skills module, which also didn't know the state, so the 400 came from the agent-path's failure HTML. No token write / no duplicate audit.

**Files touched:** `.claude/journal/host/skills.md` (this entry). No production code changes. Throwaway scratch: `/tmp/ax-phase6-verify/{ax.yaml,seed.js,mock-token-server.js,data/,host.out,mock-token.log}`.
**Outcome:** Success. Every phase-6 endpoint behaved exactly as the unit tests described. No real bugs surfaced.
**Notes:** Five observations worth retaining. (a) `BIND_HOST=127.0.0.1` flips `localDevMode=true` (same gotcha phase-5 Task 8 documented) — admin auth is bypassed on loopback regardless of the token. I still passed `Authorization: Bearer <token>` for realism. (b) Proxy didn't intercept the token-endpoint fetch — confirming that OAuth token exchange runs in the host process, not the agent sandbox, as the architecture intends. (c) Post-callback reconcile fires and fails: `git -C /tmp/ax-phase6-verify/repos/verify-agent ls-tree...` (no bare repo exists). The admin-oauth-flow swallow-and-log handles it per `admin-oauth-flow.ts:394-403`; credentials are already written and audit is already emitted. The setup card remains in `skill_setup_queue` because reconcile can't rewrite it — same behavior as phase-5 Task 8 documented. Not a bug. (d) `better-sqlite3` isn't in `/tmp`'s node_modules; seed script requires it via absolute path `/Users/vpulim/dev/ai/ax/node_modules/better-sqlite3`. Phase-5 footgun repro. (e) Kind-ax deployment skipped — same reason as phase 5 (pinned image pre-dates phase-4). Local coverage + module tests are sufficient.

## [2026-04-17 15:30] — Phase 6 Task 5: UI Connect button + polling

**Task:** Replace the phase-5 "OAuth flow — coming in phase 6" stub in `SetupCardView` with a real Connect button. Click POSTs `/admin/api/skills/oauth/start` and opens the returned `authUrl` in a new tab. Parent page auto-polls `/skills/setup` every 2s whenever any card has an unconnected OAuth credential so the callback's reconcile surfaces (card vanishes / missingCredentials shrinks) without a manual refresh.
**What I did:** Added `StartOAuthResponse` to `lib/types.ts` and `api.startOAuth(body)` to `lib/api.ts`. In `skills-page.tsx`: per-card `connecting: Set<string>` + `connectError: Record<string,string>` state, `handleConnect(envName)` (POST → window.open new tab; popup-blocked path surfaces "Pop-up blocked..." inline; 30s auto-re-enable); renamed `hasOAuth` → `hasUnconnectedOAuth` for intent clarity (same gate). Page-level `useEffect` polls `refreshSetup()` every 2s iff any agent card carries an oauth-typed missing credential — stops when set is empty. Added `MOCK_SKILL_SETUP_WITH_OAUTH` + `mockSkillsSetupWithOAuth` fixture helper (not in `mockAllAPIs`; tests register explicitly so default doesn't win). Updated `skills.spec.ts`: existing gcal test now looks for "Connect with google" button; 5 new cases — Connect renders, click POSTs start + opens authUrl in new tab, Approve enables when missingCreds empty (polling converged state), popup-blocked error, start-404 error.
**Files touched:** `ui/admin/src/lib/types.ts`, `ui/admin/src/lib/api.ts`, `ui/admin/src/components/pages/skills-page.tsx`, `ui/admin/tests/fixtures.ts`, `ui/admin/tests/skills.spec.ts`.
**Outcome:** Success. 13/13 skills.spec.ts + 10/10 navigation.spec.ts = 23/23 pass; `tsc --noEmit` clean in both `ui/admin` and root.
**Notes:** The app strips `?page=...` on boot so `page.reload()` lands on Overview — OAuth-fixture tests must register the mock BOTH before and after `gotoAuthenticated` AND re-navigate with `?page=skills&token=...` (same pattern as the existing empty-state test). Also wasted time on a stale Vite dev server running from the `skills-phase5` worktree (via `reuseExistingServer: true` in playwright.config.ts) — tests served the OLD bundle, Connect button never appeared. Kill cross-worktree dev servers before running tests.

## [2026-04-17 14:00] — Phase 6 Task 4 follow-up: 15s timeout on OAuth token-exchange fetches

**Task:** Review-fix: the three OAuth token-endpoint fetches (admin-initiated in `admin-oauth-flow.ts#resolveCallback`, agent-initiated in `oauth-skills.ts#resolveOAuthCallback` + `refreshOAuthToken`) had no timeout. A hostile or unresponsive IdP hangs the host request forever AND keeps the pending-flow state consumed with no recovery path. DoS hygiene gap on a security-critical path.
**What I did:** Added `const TOKEN_EXCHANGE_TIMEOUT_MS = 15_000` (same name + comment in both files — same invariant). Passed `signal: AbortSignal.timeout(...)` to each of the three fetch calls. In `admin-oauth-flow.ts`, made the timeout injectable via new `CreateAdminOAuthFlowOpts.tokenExchangeTimeoutMs` so tests can override to 50ms without waiting 15s. Existing catch blocks already surface thrown errors as `{ ok:false, reason:'error' }` with audit — timeout folds in as one more failure mode, no behavior changes otherwise. Added one test in `tests/host/admin-oauth-flow.test.ts` that stubs fetch to hang on its own AbortSignal, fires the injected short timeout, and asserts no credential write / no reconcile call / audit failure emitted.
**Files touched:** `src/host/admin-oauth-flow.ts`, `src/host/oauth-skills.ts`, `tests/host/admin-oauth-flow.test.ts`.
**Outcome:** Success. 135/135 across the 8 target files (including pre-existing `tests/host/oauth-skills.test.ts`); `tsc --noEmit` clean.
**Notes:** `AbortSignal.timeout(ms)` is native in Node 18+; no polyfill. Not touching `oauth-skills.ts` beyond the three-line signal add per scope discipline — existing agent-flow tests still pass, no new coverage there. Test uses a DOMException with name `'AbortError'` to match real fetch-abort semantics; the implementation path doesn't branch on error name, so this is just cosmetic parity.

## [2026-04-17 13:51] — Phase 6 Task 4: OAuth callback extension + reconcile trigger

**Task:** Extend `/v1/oauth/callback/:provider` to handle admin-initiated flows first. Adds `resolveCallback` to `AdminOAuthFlow`: exchanges the auth code (PKCE verifier + optional client_secret for admin-registered confidential providers), writes `access_token` at the declared credential scope, persists a refresh blob at `<envName>__oauth_blob`, audits, and fires `reconcileAgent` best-effort. On match (ok or fail), we do NOT fall through to the agent-initiated oauth-skills path — matched:true consumes the state.
**What I did:** Added `ResolveCallbackInput` / `ResolveCallbackResult` + `resolveCallback` method in `src/host/admin-oauth-flow.ts`. Refactored shared claim logic into a private `claimState()` helper. Provider-path mismatch returns `{matched:true, ok:false, reason:'invalid_response', details:'provider mismatch'}` and audits `oauth_callback_failed` — we refuse to POST to the stored tokenUrl when the URL path disagrees. Token exchange failures log `status + bodyLength` only (upstream errors can echo back the code/secret). Restructured the callback route in `src/host/server-request-handlers.ts`: try admin first, fall through only on `matched:false`. Added `escapeHtml()` for the failure-reason interpolation. Wired `adminOAuthFlow?` + `reconcileAgent?` through `RequestHandlerOpts`, passed from `server.ts` (reusing the existing `adminReconcileAgent` closure + `core.adminOAuthFlow`).
**Files touched:** `src/host/admin-oauth-flow.ts` (resolveCallback + types), `src/host/server-request-handlers.ts` (deps + escapeHtml + callback rewrite), `src/host/server.ts` (wiring), `tests/host/admin-oauth-flow.test.ts` (+11 resolveCallback tests), `tests/host/server-oauth-callback.test.ts` (new, 4 HTTP-level cases).
**Outcome:** Success. 127/127 across the 7 target files; `tsc --noEmit` clean. (`tests/host/server.test.ts` has pre-existing EINVAL listen errors from long Unix-socket paths on macOS `/var/folders/...` tmp dirs — unrelated to this change; same failure reproduces on `main`.)
**Notes:** HTTP test needed a real/mocked fetch split — `vi.stubGlobal('fetch', ...)` intercepts the test's own request to the local server. Pattern: keep a bound `realFetch = globalThis.fetch.bind(globalThis)` before stubbing, and the stub proxies `127.0.0.1`/`localhost` through real fetch and forwards everything else to `tokenFetch = vi.fn()`. Secret-leak guard: `JSON.stringify(audit.calls)` substring-checked against `'shh'`, `'at-123'`, `'rt-456'`. Matched-but-failed semantics are load-bearing: if mismatch returned `matched:false`, an attacker could null-route an admin-provenance state through the agent path — so we consume the state and audit the mismatch explicitly.

## [2026-04-17 13:42] — Phase 6 Task 3: admin OAuth flow module + /skills/oauth/start endpoint

**Task:** Admin-initiated OAuth flow: new in-memory pending-flow map (state-keyed, 15-min TTL, single-use claim) and `POST /admin/api/skills/oauth/start` — the endpoint the dashboard calls when a user clicks "Connect with <provider>" on a SetupCard. Frontmatter `clientId` is used unless an admin-registered provider overrides it; admin `clientSecret` is stored server-side in the pending flow but NEVER enters audit args or response bodies.
**What I did:** Created `src/host/admin-oauth-flow.ts` with `createAdminOAuthFlow()` (PKCE via the existing `oauth.ts` helpers — `generateCodeVerifier`/`generateCodeChallenge`/`generateState`). Added `AdminOAuthStartSchema` (Zod strict: `agentId`/`skillName`/`envName`/`userId?`) + route handler in `server-admin.ts` — validates agentId in registry, looks up the setup card, finds the matching OAuth credential, resolves admin-registered provider override, and computes redirectUri from `x-forwarded-proto`/`req.socket.encrypted` + `host` header (or uses `adminRedirectUri` when the provider is admin-registered, for exact OAuth app match). Wired `adminOAuthFlow` through `AdminDeps` → `AdminSetupOpts` → `setupAdminHandler` → `server.ts` call site → `HostCore.adminOAuthFlow` constructed unconditionally in `server-init.ts` (in-memory, no DB deps).
**Files touched:** `src/host/admin-oauth-flow.ts` (new), `src/host/server-admin.ts` (schema + route + `AdminDeps.adminOAuthFlow`), `src/host/server-webhook-admin.ts` (AdminSetupOpts pass-through), `src/host/server.ts` (call site), `src/host/server-init.ts` (construct + expose on HostCore), `tests/host/admin-oauth-flow.test.ts` (new, 9 cases), `tests/host/server-admin-oauth-start.test.ts` (new, 12 cases).
**Outcome:** Success. 21/21 new tests pass; 112/112 across the six target files; `tsc --noEmit` clean.
**Notes:** Scheme detection for redirect_uri: prefer `x-forwarded-proto` (accepting the first value when comma-separated), fall back to `req.socket.encrypted`. Admin-registered provider `redirectUri` wins over the request-derived value — lets operators pin the exact URI that matches their OAuth app registration. The module is parallel to `oauth-skills.ts` (untouched here), with its own flow schema that captures skill setup context (skillName/envName/scope/agentId + admin-override secret). Secret leak audit check substring-guards both `auditCall` and response body against the literal `'admin-shh'`.

## [2026-04-17 13:40] — Phase 6 Task 2: admin OAuth provider CRUD endpoints

**Task:** Expose `GET/POST/DELETE /admin/api/oauth/providers*` so operators can configure admin-registered OAuth providers that override a skill's frontmatter `client_id`/`client_secret` (for providers like Google that don't support PKCE-only public-client flows).
**What I did:** Added a Zod strict schema (`provider`/`clientId`/`clientSecret?`/`redirectUri:url`, all with max-length caps) and three routes in `server-admin.ts`. GET returns `{ providers }` from the store's secret-free `list()`. POST upserts and emits `oauth_provider_upserted` audit with `{ provider, hasSecret: boolean }` — clientSecret value never touches the audit args. DELETE is idempotent and emits `oauth_provider_deleted` ONLY when a row was removed. Narrowed the POST try/catch to `JSON.parse` only so store/audit throws fall through to the outer 500 handler (phase-5 pattern). Added `adminOAuthProviderStore` to `AdminDeps` and mirrored in `AdminSetupOpts`; passed through from `core.adminOAuthProviderStore` at the single `setupAdminHandler` call site.
**Files touched:** `src/host/server-admin.ts`, `src/host/server-webhook-admin.ts`, `src/host/server.ts`, `tests/host/server-admin-oauth-providers.test.ts` (new, 10 cases).
**Outcome:** Success. 91/91 tests pass across the four target files; `tsc --noEmit` clean.
**Notes:** Test file uses a REAL store over in-memory SQLite (runs `adminOAuthMigrations` per test) so the HTTP round-trip exercises encryption-at-rest. `JSON.stringify(auditCall)` substring-guarded against the literal secret value `'shh'` to catch any future audit-arg leak. The `.url()` zod validator accepts `http/https/ftp/...` — no `.startsWith('https://')` tightening here since self-hosted setups run on `http://localhost`; the OAuth start/callback handlers (Task 3/4) will enforce scheme policy where it matters.

## [2026-04-17 12:32] — Phase 6 Task 1 follow-up: refuse sha256('') as at-rest key

**Task:** PR review finding — `deriveOAuthKey` fell back to `sha256(adminToken)` without a length floor, so installs with neither `AX_OAUTH_SECRET_KEY` nor `admin.token` configured would derive from `sha256('')` (a world-known constant), letting any DB-read attacker decrypt stored client secrets offline.
**What I did:** `deriveOAuthKey` now throws for empty / <16-char admin tokens when no env key is set. `server-init.ts` wraps the key derivation in try/catch and soft-degrades: on throw, we log `admin_oauth_provider_store_disabled` at warn level and leave both `adminOAuthKey` and `adminOAuthProviderStore` undefined — matching the DB-less case so Task 2's CRUD endpoints will 503 naturally. Added three tests (empty throws, short throws, 16-char works) and adjusted one existing test that used `deriveOAuthKey('test')` (4 chars) to use a 16+ char token.
**Files touched:** `src/host/admin-oauth-providers.ts`, `src/host/server-init.ts`, `tests/host/admin-oauth-providers.test.ts`.
**Outcome:** Success. 80/80 tests pass (admin-oauth-providers + server-admin + server-admin-skills); tsc clean.
**Notes:** 16 chars ≈ 96 bits of entropy if random — minimum we'll accept as a fallback key source. The soft-degrade keeps dev loops without `admin.token` unblocked (they already have no DB in many cases); production installs should set `AX_OAUTH_SECRET_KEY` (32 hex bytes) directly.

## [2026-04-17 12:12] — Phase 6 Task 1: admin OAuth providers table + crypto + storage

**Task:** Foundation for phase 6 OAuth PKCE work — new `admin_oauth_providers` table (provider pk, client_id, encrypted client_secret, redirect_uri, updated_at), AES-256-GCM crypto helpers, Kysely storage module, and server-init wiring.
**What I did:** Created `src/migrations/admin-oauth-providers.ts` modeled on `skills.ts` (SQLite default export, `buildAdminOAuthMigrations(dbType)` for Postgres path). Created `src/host/admin-oauth-providers.ts` with `encryptSecret`/`decryptSecret` (iv || ct || tag, base64), `deriveOAuthKey` (env var preferred, sha256(admin.token) fallback with warning), and `createAdminOAuthProviderStore` exposing get/list/upsert/delete. `list()` deliberately omits `clientSecret` (wire safety). Extended `server-init.ts`'s `if (providers.database)` block to run the new migration under table name `admin_oauth_migration`, derive the key, and attach `adminOAuthKey` + `adminOAuthProviderStore` to `HostCore`.
**Files touched:** `src/migrations/admin-oauth-providers.ts` (new), `src/host/admin-oauth-providers.ts` (new), `src/host/server-init.ts` (wiring + HostCore fields), `tests/host/admin-oauth-providers.test.ts` (new, 12 cases).
**Outcome:** Success. 12/12 new tests pass; 183/183 phase-5 tests still pass; `tsc --noEmit` clean.
**Notes:** Store's `delete()` sums `numDeletedRows: bigint` across Kysely's `DeleteResult[]` so idempotency returns correct booleans under either single- or multi-statement backends. The conflict branch of upsert explicitly bumps `updated_at = Math.floor(Date.now()/1000)` to match the `sqlEpoch` default applied on first insert. No endpoints/flow logic wired — that's Tasks 2–4.

## [2026-04-17 10:50] — Phase 5 PR #180 review feedback

**Task:** Address CodeRabbit review comments on PR #180 (phase 5 dashboard setup cards).
**What I did:** Four minor findings, three actioned, one deferred:
1. `server-admin-skills-helpers.ts` + `server-admin.ts` — stripped redundant `timestamp: new Date()` from the two new `audit.log` calls (the database provider sets its own timestamp when omitted). Added `sessionId: body.agentId` / `sessionId: agentId` so audit queries can filter by agent for skill_approved and skill_dismissed.
2. `server-init.ts` — captured the unsubscribe function returned by `eventBus.subscribe(...)` in a new `unsubscribeCredentialRequests` field on `HostCore`. Matches the pattern used in server-admin.ts and event-console.ts; no current caller invokes it but future shutdown paths now have a handle.
3. `skills-page.tsx` — added `success` to the Approve + Dismiss disable conditions so a second click during the 1.5s post-success refresh window can't fire a duplicate `/approve` (which would 404 once reconcile drops the setup row) or a stray dismiss. Added a Playwright assertion to the "approve click" test that both buttons are disabled after success. Matches `CredentialRequestCard`'s existing behavior.
4. TOCTOU on dismiss's read-modify-write was flagged as "no change requested for this PR" by the reviewer and genuinely out of scope — acceptable for single-admin dashboards. Deferred.
**Files touched:** `src/host/server-admin-skills-helpers.ts`, `src/host/server-admin.ts`, `src/host/server-init.ts`, `ui/admin/src/components/pages/skills-page.tsx`, `ui/admin/tests/skills.spec.ts`.
**Outcome:** Success. 72/72 backend tests + 8/8 Playwright + root & UI tsc all pass.
**Notes:** `AuditEntry.timestamp` is declared non-optional on the type but `AuditProvider.log(entry: Partial<AuditEntry>)` takes a partial, and the database provider falls back to `new Date().toISOString()` when timestamp is absent — so stripping it is safe and makes the new call sites consistent with existing ones.

## [2026-04-17 10:17] — Phase 5 complete: dashboard setup cards

**Task:** Implement phase 5 of git-native skills: dashboard setup cards + atomic approve + credential request queue + React UI.
**What I did:** 10 commits across backend (Tasks 1-5) and UI (Tasks 6-7), plus E2E verification (Task 8) and documentation (Task 9). Tests: 190 backend + 18 Playwright pass. tsc clean.
**Files touched:** See commits 74970c48..HEAD. Docs updated in Task 9: `.claude/skills/ax-host/SKILL.md`, `docs/plans/2026-04-16-git-native-skills-design.md`, this journal, `.claude/lessons/host/entries.md` (from earlier tasks).
**Outcome:** Success. All 4 admin endpoints behave as specified against a live local host. SkillsPage renders cards, handles atomic approve, dismiss confirm-click, OAuth-disabled state, credential requests.
**Notes:** Image-rebuild path against kind-ax was skipped (pinned to pre-phase-4 image); local E2E covered equivalent scope. Phase 6 (OAuth PKCE) is next per the design doc rollout.

## [2026-04-17 14:15] — Phase 5 Task 8: end-to-end verification against local host

**Task:** Live-wire confidence check for the phase-5 skills dashboard endpoints. The kind-ax cluster is pinned to `ghcr.io/project-ax/ax:980686f7` (pre-phase-4), and rebuilding + loading a phase-5 image into kind on Apple Silicon is heavier than the task warrants. Unit + Playwright suites already cover behavior (190 backend + 18 UI); this task just needs to prove the code boots + routes.
**What I did:** Option A (local host, per task spec fallback). (1) Wrote `/tmp/ax-phase5-verify/ax.yaml` — SQLite + apple sandbox + `admin.token=phase5-verify-token-abc123`, `agent_name: verify-agent`. (2) Started the host via `AX_HOME=/tmp/ax-phase5-verify AX_HOOK_SECRET=verify-hook-secret BIND_HOST=127.0.0.1 node dist/cli/index.js serve --port 8088`; the default agent auto-registered; startup rehydrate logged a `reconcile_failed` for the missing bare repo (expected). (3) Curled GET `/skills/setup` empty → `{agents:[]}`; GET `/credentials/requests` → `{requests:[]}`. (4) Seeded a `weather-skill` card with `TEST_KEY` (user scope) + `api.test.com` via a `better-sqlite3` insert into `skill_setup_queue` + `skill_states`. (5) GET after seed returned the card verbatim (matching `SetupRequest` JSON shape). (6) POST `/skills/setup/approve` with matching body → 200 `{ok:true, state:{...}}`; audit row `skill_approved` written with envNames but NOT values; `credential_store` got `{scope:'user:verify-agent:vpulim', env_name:'TEST_KEY', value:'my-secret-abc'}`; `proxy-domain-list` logged `domain_approved_by_admin`. (7) Verified agent-scoped path by seeding a second `api-skill` card with `scope:'agent'` → credential stored as `scope:'agent:verify-agent'`. (8) Error paths: unexpected-credential → 400 `{error, details:'Unexpected credential:...'}`; unexpected-domain → 400 same shape; missing-skill → 404; malformed JSON → 400. (9) DELETE `/skills/setup/verify-agent/weather-skill` → `{ok:true, removed:true}`; second call → `{ok:true, removed:false}` (idempotent); GET after → `{agents:[]}`. (10) Killed the host cleanly.
**Files touched:** `/tmp/ax-phase5-verify/ax.yaml` (scratch config), `.claude/journal/host/skills.md`, `.claude/lessons/host/entries.md`.
**Outcome:** Success — every endpoint behaved as the spec + tests described. No real bugs surfaced. Two observations worth retaining (see notes).
**Notes:** (a) Post-approve the card stays in `skill_setup_queue` because the reconcile throws on missing `.ax/skills/`, and the helper swallows the throw (per spec line 142-151) without rewriting the queue — so the returned `state` still reads `pending`. In real deployments the push-hook reconcile runs on the actual pushed repo and clears the card naturally; the swallow-on-throw is the right call because DB-state is already consistent. Not a bug. (b) Localhost (`BIND_HOST=127.0.0.1`) flips `localDevMode=true` in server.ts:332, which skips admin auth for loopback connections regardless of the `admin.token` — confirmed by curling without a token and still getting a 200. This is by design (see `server-admin.ts:197`) but would have been worth noting in the task spec's "curl with Authorization" instructions; someone following the spec verbatim might think their token is magically working when auth is actually bypassed. Full curl transcript preserved in this entry.

## [2026-04-17 12:05] — Phase 5 Task 7: Skills sidebar nav entry

**Task:** Wire the Skills page into the admin sidebar. Task 6 had already added `'skills'` to the `Page` union and the `activePage === 'skills'` render branch; the visible entry point (the sidebar button) was intentionally deferred to this task.
**What I did:** (1) In `ui/admin/src/App.tsx`, imported `Sparkles` from `lucide-react` and inserted `{ id: 'skills', label: 'Skills', icon: Sparkles }` into `NAV_ITEMS` between `'agents'` and `'connectors'` so the final order reads Overview / Agents / Skills / Connectors / Security / Logs / Settings. (2) In `ui/admin/tests/navigation.spec.ts`, added `Skills` to the "sidebar shows all navigation items" visibility assertions and added a new `navigating to Skills page` test placed next to the Agents equivalent — clicks the Skills button and asserts the `heading { name: 'Skills', exact: true }` becomes visible (the `<h2>Skills</h2>` inside `skills-page.tsx`). Verified `fixtures.ts` `mockAllAPIs` already calls both `mockSkillsSetup` and `mockCredentialRequests` (added in Task 6) — no fixture changes needed.
**Files touched:** `ui/admin/src/App.tsx`, `ui/admin/tests/navigation.spec.ts`.
**Outcome:** Success — `npx playwright test navigation.spec.ts skills.spec.ts` = 18/18 pass (10 navigation + 8 skills). `npx tsc --noEmit` clean at both the worktree root and in `ui/admin/`.
**Notes:** Scope-discipline pass — two lines of production change and ~6 lines of test change. No backend or SkillsPage edits. `Sparkles` mirrors the icon already used inside the SkillsPage header, so the sidebar and page header match visually.

## [2026-04-17 11:45] — Phase 5 Task 6 follow-up: page-param leak + approve detail parsing

**Task:** Two minor Task 6 review findings. (1) `readInitialPage` in `App.tsx` had a JSDoc claim it stripped `?page=` from history, but the strip only covered `?token=` — reloads on `/admin/?page=skills` pinned the user to the Skills page forever. (2) `handleApprove` in `skills-page.tsx` tried `JSON.parse(msg)` to extract `.details` from the approve-error message, but `apiFetch` flattens the JSON envelope to a plain string before throwing — the parse always failed, so `errorDetails` was never populated. Chose Option A (preserve details on the thrown Error) since approve surfaces genuinely useful strings like "Unexpected credential: EVIL_KEY".
**What I did:** (1) In `App.tsx` `checkAuth()`, unified the `?token=` + `?page=` history cleanup into a single `params.has('token') || params.has('page')` block that deletes both and calls `replaceState` once. Updated the `readInitialPage` JSDoc to point at where the stripping actually happens. (2) In `api.ts` `apiFetch`, extended the non-OK branch: when the body parses as JSON with a `details` string, hoist it onto the thrown Error as `(Error & { details?: string }).details`. (3) In `skills-page.tsx` `handleApprove` catch, replaced the dead `JSON.parse(msg)` block with `const e = err as Error & { details?: string }; setError(e.message); setErrorDetails(e.details ?? null);`. (4) Tests: added a new `approve surfaces both error and details when the server rejects` case in `skills.spec.ts` that routes `/skills/setup/approve` to a 400 `{error:{message:...}, details:'Unexpected credential: EVIL_KEY'}` and asserts both strings render. Also updated the empty-state test — it used to `page.reload()` but the `?page=` strip now means reload lands on Overview, so I re-navigate with `/admin/?page=skills&token=...` instead.
**Files touched:** `ui/admin/src/App.tsx`, `ui/admin/src/lib/api.ts`, `ui/admin/src/components/pages/skills-page.tsx`, `ui/admin/tests/skills.spec.ts`.
**Outcome:** Success — `npx playwright test skills.spec.ts` = 8/8 pass (7 prior + 1 new). `npx tsc --noEmit` clean in both `ui/admin/` and the worktree root.
**Notes:** Follow-up to 66fe4cfd (new commit, not an amend, per CLAUDE.md). The envelope shape `{ error: { message, type, code }, details }` comes from `sendJSON` inside the approve route (see the Task 3 entry below — `server-admin.ts` uses `sendJSON` directly rather than `sendError`, so `details` sits at the top level, not inside `error`). Client-side now handles both string and object `error` fields.

## [2026-04-17 11:15] — Phase 5 Task 6: React SkillsPage (admin UI)

**Task:** Phase 5 Task 6. Build the Skills page in the admin dashboard. Renders pending setup cards grouped per agent (domains, credentials, MCP URLs) plus ad-hoc credential request cards. One submit per card calls the atomic `/approve` endpoint. OAuth credentials render disabled with a "coming in phase 6" stub. Dismiss uses confirm-click. Sidebar entry NOT added (that's Task 7) — instead wired an `activePage === 'skills'` branch + `?page=skills` query-param initial-page reader so Playwright can drive directly to it.
**What I did:** (1) Types: appended `SetupCard`, `SetupCardCredential`, `AgentSetupGroup`, `SkillSetupResponse`, `SkillApproveBody`, `SkillState`, `SkillApproveResponse`, `CredentialRequest`, `CredentialRequestsResponse` to `ui/admin/src/lib/types.ts`. (2) API client: added `skillsSetup()`, `approveSkill(body)`, `dismissSkill(agentId, skillName)`, `credentialRequests()`, `provideCredential(envName, value, sessionId?)` to `ui/admin/src/lib/api.ts` (note: since `apiFetch` prefixes `/admin/api`, the DELETE path is just `/skills/setup/:agentId/:skillName`). (3) Component: created `ui/admin/src/components/pages/skills-page.tsx` with two internal subcomponents — `SetupCardView` (domain checklist pre-checked, api_key password input + OAuth disabled stub, MCP read-only list, error banner, confirm-click dismiss, 1.5s success fade before `refresh()`) and `CredentialRequestCard` (password input + Save → `/credentials/provide`). Approve-disabled rule: ANY oauth credential OR ANY empty api_key value. (4) App.tsx: extended the `Page` union with `'skills'`, added `readInitialPage()` lazy useState initializer that reads `?page=skills` from URL (VALID_PAGES whitelist), added `{activePage === 'skills' && <SkillsPage />}` branch. Did NOT add sidebar button (deferred to Task 7). (5) Fixtures: added `MOCK_SKILL_SETUP` (1 agent, 2 cards — 1 api_key + 1 oauth) + `MOCK_CREDENTIAL_REQUESTS` (1 request) + `mockSkillsSetup`/`mockCredentialRequests` helpers; wired both into `mockAllAPIs`; also patched `gotoAuthenticated` to handle paths that already have a query string (`?page=skills&token=...`). (6) Tests: `ui/admin/tests/skills.spec.ts` with 7 cases (empty state, full card render, approve body + success UI, dismiss confirm-click → DELETE, OAuth disables approve, credential request save → POST body, mock-shape sanity).
**Files touched:** `ui/admin/src/lib/types.ts`, `ui/admin/src/lib/api.ts`, `ui/admin/src/components/pages/skills-page.tsx` (new), `ui/admin/src/App.tsx`, `ui/admin/tests/fixtures.ts`, `ui/admin/tests/skills.spec.ts` (new).
**Outcome:** Success — `npx playwright test skills.spec.ts` = 7/7 pass; full non-agent Playwright suite (`navigation/overview/security/settings/logs/auth/setup/skills`) = 72/72 pass. `npx tsc --noEmit` clean at project root AND `cd ui/admin && npx tsc --noEmit` clean. Pre-existing failures in `agents.spec.ts` (13) and `agent-tabs.spec.ts` (10) are unchanged — they fail identically on the pre-change stash, unrelated to this task.
**Notes:** Two test fixes needed during dev: (a) empty-state test had to install empty overrides AFTER `gotoAuthenticated` + `page.reload()` because Playwright applies the most-recently-registered route first, and `mockAllAPIs` was overwriting the empty overrides when registered first; (b) `(user-scoped)` label matched two cards so the scope-label assertion had to be scoped to the linear-tracker card via `data-testid`. The `error.details` surfacing from the approve endpoint is best-effort — `apiFetch` flattens the JSON error into a string, so the page tries a best-effort `JSON.parse(msg)` to extract `details` but falls back to showing just the bare message. Manual UI verification: started `npm run dev`, curled `/admin/` → valid HTML; curled the skills-page.tsx module → valid compiled JS. Full in-browser flow requires a running backend (out of scope for this task).

## [2026-04-17 09:50] — Phase 5 Task 5: credential request queue + GET endpoint

**Task:** Add an in-memory queue for ad-hoc credential requests emitted by the `request_credential` agent tool, expose it via `GET /admin/api/credentials/requests`, and drain on `POST /admin/api/credentials/provide`. Purpose: a dashboard that connects after the `credential.required` SSE event fires still needs to see pending requests.
**What I did:** Created `src/host/credential-request-queue.ts` — tiny `Map<"${sessionId}:${envName}", CredentialRequest>` with enqueue/dequeue/snapshot. Wired a subscriber in `initHostCore` (`src/host/server-init.ts`) that listens on the event bus for `type === 'credential.required'` and enqueues from `event.data` (the emitter in `src/host/ipc-handlers/skills.ts:273` ships `{ envName, sessionId, agentId, userId }` — no `agentName`, so I fall back `agentName || agentId || ''`). Added the queue to `HostCore`, threaded it through `AdminSetupOpts` + `AdminDeps`, wired `core.credentialRequestQueue` into the `setupAdminHandler` call in `server.ts`. New GET route soft-degrades to `{ requests: [] }` when the dep is absent (additive feature — not a 503). POST /provide dequeues on the SUCCESS path only, inside `if (deps.credentialRequestQueue && credSessionId)`. Unit tests in `tests/host/credential-request-queue.test.ts` (7 cases) plus HTTP tests in `tests/host/server-admin-skills.test.ts` (3 GET cases + 1 dequeue-on-provide case).
**Files touched:** `src/host/credential-request-queue.ts` (new), `src/host/server-init.ts`, `src/host/server-admin.ts`, `src/host/server-webhook-admin.ts`, `src/host/server.ts`, `tests/host/credential-request-queue.test.ts` (new), `tests/host/server-admin-skills.test.ts`.
**Outcome:** Success — `npx vitest run tests/host/credential-request-queue.test.ts tests/host/server-admin-skills.test.ts tests/host/server-admin.test.ts` = 72/72 pass (7 new queue unit + 4 new HTTP + 61 prior). `npx tsc --noEmit` clean.
**Notes:** Queue is intentionally in-memory. Restart drops pending requests, which is fine — the agent re-requests on its next attempt. The `EventBus.subscribe` API is a single callback that receives ALL events (filter by `type` in-handler) — not a `.on('eventName', ...)` style emitter. The dequeue-on-provide guard (`if (credSessionId)`) matters because `sessionId` is optional in the provide body — unscoped stores still work but skip the dequeue.

## [2026-04-17 09:35] — Phase 5 Task 4: DELETE /admin/api/skills/setup/:agentId/:skillName (dismiss)

**Task:** Phase 5 Task 4. Add a dashboard-only "Dismiss" endpoint that drops a card from the user's view without touching repo files or `skill_states`. Idempotent — dismissing a card that's not in the queue returns `removed: false`. If the skill is still pending on the next reconcile, the card reappears.
**What I did:** Added the DELETE route to `src/host/server-admin.ts` right after the approve block. Uses the same regex style as the existing `agents/:id/mcp-servers/:name` delete (`/^\/admin\/api\/skills\/setup\/([^/]+)\/([^/]+)$/`) with `decodeURIComponent` on each segment. 503 short-circuit if `deps.skillStateStore` is missing. Reads the queue, filters out the matching `skillName`, compares lengths: zero-removal → `{ ok: true, removed: false }` with no write/audit; otherwise `putSetupQueue` + `audit.log({ action: 'skill_dismissed', args: { agentId, skillName } })`, then `{ ok: true, removed: true }`. Audit throws propagate (same as approve — audit is a security invariant). Extended `tests/host/server-admin-skills.test.ts` with a new describe block (5 cases: happy path, idempotent-no-op, URL-encoded skill name, 503 missing store, other-agents-untouched).
**Files touched:** `src/host/server-admin.ts`, `tests/host/server-admin-skills.test.ts`.
**Outcome:** Success — `npx vitest run tests/host/server-admin-skills.test.ts tests/host/server-admin.test.ts` = 61/61 pass (56 prior + 5 new). `npx tsc --noEmit` clean.
**Notes:** Zero surprises. The existing `mockDeps` factory already exposed `putSetupQueue` as a `vi.fn()` so the write-back + audit assertions fell out naturally. The "other agents untouched" test seeds a linear card in both `main` and `other` queues and asserts only `main` is written back — load-bearing because it proves the regex pins the write to the URL's `:agentId` segment and doesn't do some global scan.

## [2026-04-17 09:25] — Phase 5 Task 3 second follow-up: narrow skill-approve route catch

**Task:** Second spec-review finding uncovered while doing the audit-catch removal. The approve route's outer `try/catch` wrapped the entire flow and reported every throw as `400 Invalid request: <msg>`. After removing the helper's silent audit-catch, a real audit/DB failure would have surfaced as 400 (client bug) instead of 500 (server bug), misleading operators.
**What I did:** Narrowed the route's try/catch in `src/host/server-admin.ts` to cover only `JSON.parse(await readBody(req))`. Everything else (schema `safeParse`, `approveSkillSetup`, response writes) is now outside the catch and propagates to the outer HTTP handler (`server-request-handlers.ts:735-742`) which already returns 500 for unhandled admin-route throws. Added a regression test in `tests/host/server-admin-skills.test.ts` that mocks `audit.log` to reject and asserts status 500 plus no `"Invalid request:"` prefix. Also updated the test file's `startTestServer` helper to mirror production's outer try/catch (so admin-handler throws surface as 500 in-test too).
**Files touched:** `src/host/server-admin.ts`, `tests/host/server-admin-skills.test.ts`.
**Outcome:** Success — `npx vitest run tests/host/server-admin-skills.test.ts tests/host/server-admin.test.ts` = 56/56 pass (55 prior + 1 new). `npx tsc --noEmit` clean.
**Notes:** Verified `server-request-handlers.ts:735-742` does wrap `adminHandler` in try/catch and returns `500 Admin request failed` on unhandled throws. Follow-up to commit that removed the helper's audit catch — new commit, not an amend.

## [2026-04-17 09:15] — Phase 5 Task 3 follow-up: let skill-approve audit failures surface

**Task:** Spec-review finding on commit 9f19b6b5. The approve helper wrapped `audit.log` in a try/catch that logged `skill_approve_audit_failed` and returned 200 — silent failure for a security-relevant action, in tension with CLAUDE.md's "Everything is audited" invariant. Reconcile's swallow-and-log is justified (DB already consistent; startup-rehydrate catches up), but audit has no such recovery mechanism.
**What I did:** Removed the try/catch around the `providers.audit.log` call in `src/host/server-admin-skills-helpers.ts` (step 10). Replaced the comment with one explaining why audit must propagate. Reconcile's try/catch (step 8) is untouched — that one is spec-aligned. The route handler's outer try/catch is also untouched. No test relied on the silenced audit failure — all existing tests already stub `audit.log` as `mockResolvedValue(undefined)` so the happy path is unchanged.
**Files touched:** `src/host/server-admin-skills-helpers.ts`.
**Outcome:** Success — `npx vitest run tests/host/server-admin-skills.test.ts tests/host/server-admin.test.ts` = 55/55 pass. `npx tsc --noEmit` clean.
**Notes:** Follow-up to 9f19b6b5 (new commit, not an amend, per CLAUDE.md "prefer a new commit"). The original Task 3 journal entry below stays unchanged — history is append-only.

## [2026-04-17 08:50] — Phase 5 Task 3: POST /admin/api/skills/setup/approve (atomic)

**Task:** Phase 5 Task 3. Wire the dashboard's "Approve & enable" button. Validate-all (body schema + card cross-check + OAuth guard) then apply-all (creds + domains + re-reconcile) — if anything fails, zero side effects. Phase 5 is API-key only; OAuth rejects with a clear 400.
**What I did:** Created `src/host/server-admin-skills-helpers.ts` with `ApproveBodySchema` (Zod `.strict()`) + `approveSkillSetup(deps, body)` returning a tagged-union `ApproveResult`. Helper uses a narrower `ApproveDeps` interface (structurally compatible with `AdminDeps`) to avoid a circular import. Scope key: `agent:<name>` for `scope: 'agent'`, `user:<name>:<userId>` with fallback chain `body.userId ?? deps.defaultUserId ?? 'admin'`. Reconcile failure is swallow-and-log (DB is consistent; startup-rehydrate catches up). Audit calls `action: 'skill_approved'` with `domains` + `envNames` — never values. Added the route in `server-admin.ts` right after the GET `/admin/api/skills/setup` route; uses `sendJSON` directly for the `details` field (since `sendError` wraps in `{error: {message,type,code}}`). Extended `tests/host/server-admin-skills.test.ts`: extended `mockDeps` with `withReconcile/withDomainList/withCredentials/defaultUserId/reconcileImpl/getStatesImpl` knobs, added real `ProxyDomainList` + credentials vi.fn stub; wrote 13 new cases covering the full spec (happy paths × 4 scopes, rejections × 3, 404 / 503 × 2 / invalid body, load-bearing atomicity test, reconcile-failure still-200).
**Files touched:** `src/host/server-admin-skills-helpers.ts` (new), `src/host/server-admin.ts`, `tests/host/server-admin-skills.test.ts`.
**Outcome:** Success — `npx vitest run tests/host/server-admin-skills.test.ts` = 18/18 pass (5 existing + 13 new); `tests/host/server-admin.test.ts` = 37/37 pass; `tests/host/skills/` = 118/118 pass; `npx tsc --noEmit` clean.
**Notes:** Circular import on `AdminDeps` dodged cleanly by defining a local `ApproveDeps` in the helper — `Pick<AdminDeps, ...>` would have worked too but the explicit interface makes it clearer what the helper needs. Audit failure is also caught (non-fatal) so a transient DB issue doesn't 500 an approve that already applied creds + domains. Atomicity test asserts that even ONE valid credential stays unapplied when any domain is invalid — that's the whole reason validate-all-then-apply-all exists.

## [2026-04-17 08:40] — Phase 5 Task 2: GET /admin/api/skills/setup endpoint

**Task:** Phase 5 (skills dashboard) Task 2. Surface the per-agent `skill_setup_queue` through an admin endpoint so the dashboard can render setup cards grouped by agent. Return 503 when the state store isn't wired (back-compat for deployments without DB).
**What I did:** TDD. Wrote `tests/host/server-admin-skills.test.ts` (5 cases: happy path / empty queue / 503 missing store / excludes archived / preserves card order+fields). Added a top-level `import type { SetupRequest }` from `./skills/types.js` in `src/host/server-admin.ts`. Added the route inside `handleAdminAPI` under a new "Skills (Phase 5)" section (right after the global MCP server block): 503 short-circuit when `deps.skillStateStore` is undefined, then `agentRegistry.list('active')` + per-agent `getSetupQueue` → only emit agents whose queue is non-empty.
**Files touched:** `src/host/server-admin.ts`, `tests/host/server-admin-skills.test.ts`.
**Outcome:** Success — `npx vitest run tests/host/server-admin-skills.test.ts` = 5/5 pass; regression `npx vitest run tests/host/server-admin.test.ts` = 37/37 pass; `npx tsc --noEmit` clean.
**Notes:** `sendError()` wraps the message in `{ error: { message, type: 'invalid_request_error', code: null } }` — test asserts `body.error.message === 'Skills not configured'` rather than a bare `{ error: '...' }` shape. Spec said "use `sendError`" so we don't re-roll the envelope. Next tasks (approve/dismiss) will hang off this same section.

## [2026-04-17 08:30] — Phase 5 Task 1: extend AdminDeps with skill state store + reconcile bridge

**Task:** Phase 5 (skills dashboard) preparation. Add optional `skillStateStore`, `reconcileAgent`, and `defaultUserId` to `AdminDeps` + `AdminSetupOpts` so upcoming `/admin/api/skills/*` endpoints can persist + re-reconcile without a separate wiring pass. Strict back-compat: all three fields optional.
**What I did:** Added the three optional fields to `AdminDeps` (server-admin.ts) and mirrored them on `AdminSetupOpts` + pass-through in `setupAdminHandler` (server-webhook-admin.ts). In server.ts, hoisted a `adminReconcileAgent` closure out of the `if (stateStore)` block so it captures the same `orchestratorDeps` shared with the push-time hook + rehydrate; passed through to the `setupAdminHandler` call site alongside `stateStore` and `defaultUserId`. Added a back-compat test in `tests/host/server-admin.test.ts` constructing AdminDeps with all three new fields stubbed + a GET /admin/api/status assertion.
**Files touched:** `src/host/server-admin.ts`, `src/host/server-webhook-admin.ts`, `src/host/server.ts`, `tests/host/server-admin.test.ts`.
**Outcome:** Success — `npx vitest run tests/host/server-admin.test.ts` = 37/37 pass; `npx tsc --noEmit` clean. Other host test failures (server.test.ts etc.) are pre-existing on this branch, confirmed by stashing + re-running.
**Notes:** No endpoint implementation yet — just the plumbing. The approve-triggered reconcile shares `orchestratorDeps` with the push hook and startup rehydrate, so all three paths drive the same live MCP + proxy appliers. Tests aren't included in tsc's rootDir, so the "failing test first" gate doesn't catch new-field-type errors — ran vitest + tsc independently.

## [2026-04-17 08:10] — Phase 4 PR #179 review feedback

**Task:** Address CodeRabbit actionable + high-value nitpick comments on PR #179 (feat/skills-phase4-appliers). Two actionable: (1) mcp-applier skips bearer-credential rotation when URL is unchanged; (2) proxy-applier's closure-scoped `prior` Map leaks entries for deleted agents. Two nitpicks adopted: (3) orchestrator emits `skills.live_state_applied` even when both appliers threw; (4) startup rehydration blocks `createServer` return.
**What I did:** TDD per fix. (1) mcp-applier: added 3 tests (bearer rotation, bearer added to previously unset, idempotent same-bearer); extended `ours` map to track `authHeader`; no-op check now compares both URL and the computed `Bearer ${TOKEN}` header string; on mismatch, remove-then-add under same source. Simplified the confusing comment on the unregister loop. (2) proxy-applier: added `removeAgent(agentId)` method that does `prior.delete(agentId)` + `proxyDomainList.removeAgent(agentId)`; covered by 2 tests (happy-path cleanup + unknown-agent no-op). (3) orchestrator: condition for `skills.live_state_applied` emission tightened to "at least one applier produced a non-undefined result"; 2 tests: both-throw → no event, one-succeeds → event fires with `{ mcp: undefined, proxy: result }`. (4) server.ts: wrapped rehydration block in `void (async () => { ... })()` so socket bind isn't blocked; comment explains the fire-and-forget intent.
**Files touched:** `src/host/skills/mcp-applier.ts`, `src/host/skills/proxy-applier.ts`, `src/host/skills/reconcile-orchestrator.ts`, `src/host/server.ts`, plus matching tests under `tests/host/skills/`.
**Outcome:** Success — `npx vitest run tests/host/skills/ tests/host/proxy-domain-list.test.ts` = 134/134 pass. `tsc --noEmit` clean. Pre-existing EINVAL socket-path-length failures in `tests/host/server*.test.ts` are unchanged (phase 4 doesn't touch those test files).
**Notes:** The bearer-rotation fix revealed that `listServersWithMeta` already exposes `.headers`, so comparing `headers.Authorization` against the computed desired header string is all it takes — no new manager API needed. The `prior.delete(agentId)` + `proxyDomainList.removeAgent(agentId)` pairing keeps local baseline and shared store in sync so a re-created agent starts from a clean slate.

## [2026-04-17 07:40] — Phase 4 wrap-up summary

**Task:** Close out phase 4 of git-native skills: bridge the reconciler's computed `desired.{mcpServers,proxyAllowlist}` to live host surfaces, and make live state survive a restart. Phase 2 landed the push hook + DB persistence but intentionally discarded `output.desired`.
**What shipped:** (1) `src/host/skills/mcp-applier.ts` — diffs desired vs live `McpConnectionManager`, tags with `source: 'skill:<agentId>'` so plugin/database/other-agent entries are untouched, injects `Authorization: Bearer ${TOKEN}` header placeholders when `bearerCredential` is set. (2) `src/host/skills/proxy-applier.ts` + new `ProxyDomainList.setAgentDomains(agentId, domains)` replace-semantics API — diffs desired vs live allowlist per-agent, one `proxy_allowlist_updated` audit entry per non-empty diff. (3) `reconcile-orchestrator.ts` now calls both appliers after DB write inside per-applier try/catches and emits a single `skills.live_state_applied` event; deps stay optional for back-compat with phase-2 tests. (4) `src/host/skills/startup-rehydrate.ts` + `server.ts` — on boot, `await agentRegistry.list()` → `rehydrateSkillsForAgents` calls `reconcileAgent(id, 'refs/heads/main', deps)` per agent, wrapped in try/catches so one bad agent can't block startup. Task 5 was a no-op (no central audit action enum). Task 6 is this wrap-up.
**Files touched (cumulative, phase 4):** `src/host/skills/mcp-applier.ts`, `src/host/skills/proxy-applier.ts`, `src/host/skills/startup-rehydrate.ts`, `src/host/skills/reconcile-orchestrator.ts`, `src/host/proxy-domain-list.ts`, `src/host/server-init.ts`, `src/host/server.ts`, plus matching tests under `tests/host/skills/` and `tests/host/proxy-domain-list.test.ts`, plus `.claude/skills/ax-host/SKILL.md`, `docs/plans/2026-04-16-git-native-skills-design.md`.
**Outcome:** Success — all skills suite tests pass; `npm run build` clean; full `npx vitest run` = 2635 passed, 33 failed, but every failure is in `tests/host/server*.test.ts` / `tests/integration/smoke*.test.ts` with pre-existing macOS EINVAL socket-path-length errors (confirmed against origin/main — not caused by phase 4). Zero skills or phase-4 tests fail. Branch `feat/skills-phase4-appliers` ready for review.
**Notes:** The `source: 'skill:<agentId>'` tag is the cross-source isolation primitive — it lets the MCP applier owned-entry-diff without knowing anything about plugins or database MCP servers. Startup rehydration via full reconcile is simpler than persisting a materialized view of live state: the DB already has `SkillState` rows from the last reconcile, and re-running reconcile is idempotent.

## [2026-04-17 07:22] — Phase 4 Task 4: Startup rehydration

**Task:** After a host restart, the DB has the last-reconciled `SkillState` rows, but the `McpConnectionManager` and `ProxyDomainList` (minus BUILTIN) start empty. Re-run `reconcileAgent` for every known agent on boot so the live surfaces match the DB.
**What I did:** (1) Extended `HostCore` in `server-init.ts` with `mcpApplier?`/`proxyApplier?` — constructed inside the `if (stateStore)` branch using the same dynamic-import pattern as the state store. (2) TDD'd a new `rehydrateSkillsForAgents(agentIds, deps, opts)` with a `runReconcile` injection seam (defaults to `reconcileAgent(id, 'refs/heads/main', deps)`). 3 tests: iterates in order, continues past per-agent failures, no-op on empty list. Failing-module run first, then implemented — 3/3 pass. (3) In `server.ts`, consolidated the reconcile-hook's `orchestratorDeps` to include the new appliers and reused that same object for the startup loop. Fetches `await agentRegistry.list()` (no status filter) and calls `rehydrateSkillsForAgents` — wrapped in an outer try/catch so a registry failure can't block server startup.
**Files touched:** `src/host/server-init.ts`, `src/host/server.ts`, `src/host/skills/startup-rehydrate.ts` (new), `tests/host/skills/startup-rehydrate.test.ts` (new), `.claude/journal/host/skills.md`.
**Outcome:** Success — `npx vitest run tests/host/skills/startup-rehydrate.test.ts tests/host/skills/reconcile-orchestrator.test.ts` = 10/10 pass. Full `tests/host/skills/` suite = 111/111. `npm run build` clean. Pre-existing EINVAL socket-path-length failures in `tests/host/server*.test.ts` exist on HEAD b09ea990 unchanged (not caused by this task).
**Notes:** The `runReconcile` seam keeps tests free of git + sqlite setup. The real code path has two layers of safety: the orchestrator's `try/catch` (skills.reconcile_failed event) AND the loop's per-agent `try/catch` (startup_rehydrate_failed log). Redundant, but cheap — startup is the worst time for one bad agent to cascade.

## [2026-04-17 07:14] — Phase 4 Task 3: Wire appliers into reconcileAgent

**Task:** Call the new `mcpApplier` and `proxyApplier` from `reconcileAgent` after DB write, feeding them `output.desired.{mcpServers,proxyAllowlist}`; emit a single `skills.live_state_applied` summary event. Keep deps OPTIONAL for back-compat with phase-2 tests.
**What I did:** TDD. Added 3 tests (w/ `vi` import): (1) stub appliers record call args — asserts agent-1 + `linear-mcp` map entry with `bearerCredential: 'LINEAR_TOKEN'` and `['api.linear.app']` domains; (2) stub appliers return specific results — asserts `skills.live_state_applied` appears in the event bus; (3) bogus `getBareRepoPath` → snapshot throws — asserts neither applier was called. Verified (1) + (2) fail. Implemented: added `McpApplier`/`McpApplyResult` + `ProxyApplier`/`ProxyApplyResult` imports and made `mcpApplier?`/`proxyApplier?` optional fields on `OrchestratorDeps`. After `putStatesAndQueue` and BEFORE the event-emit loop, run each applier inside its own try/catch (warn-log on throw, don't bubble), then emit one `skills.live_state_applied` event with `{ mcp, proxy }` summary payload IFF either applier was configured. The outer try/catch already converts a pre-DB-write throw into `skills.reconcile_failed` and skips the applier block entirely.
**Files touched:** `src/host/skills/reconcile-orchestrator.ts`, `tests/host/skills/reconcile-orchestrator.test.ts`, `.claude/journal/host/skills.md`.
**Outcome:** Success — `npx vitest run tests/host/skills/reconcile-orchestrator.test.ts` = 7/7 pass (4 existing + 3 new). `tsc --noEmit` clean. Phase-2 tests that build `OrchestratorDeps` without appliers continue to pass because both new fields are optional.
**Notes:** The error-path test trivially passed even before implementation (no wiring, no call) — that's by design, not a flaw. Post-wiring it still passes because the throw happens inside `buildSnapshotFromBareRepo`, BEFORE the new applier block, so the outer catch handles it and returns zero counts without touching appliers. Inner try/catch per applier is deliberate: a failing mcp applier must NOT block the proxy applier, because DB is already consistent and next reconcile will catch live state back up.

## [2026-04-17 07:10] — Phase 4 Task 2: Proxy applier + per-agent ProxyDomainList

**Task:** Bridge reconciler's `desired.proxyAllowlist: Set<string>` to the live `ProxyDomainList` using agent-keyed replace semantics. Phase 4 needs an agent-scoped contribution so a reconcile for agent A doesn't clobber agent B's skill domains.
**What I did:** TDD. (1) Appended 5 failing tests to `tests/host/proxy-domain-list.test.ts` covering `setAgentDomains` merge, replace-not-merge, empty-clears-only-this-agent, coexists with `addSkillDomains`/`approvePending`, and normalization (trim + lowercase + strip trailing dot). (2) Extended `ProxyDomainList` with `agentDomains: Map<agentId, Set<domain>>`, `setAgentDomains`, `removeAgent`, and updated `rebuildMerged` to walk BUILTIN ∪ skillDomains ∪ agentDomains ∪ adminApproved. (3) Wrote 4 failing applier tests and implemented `src/host/skills/proxy-applier.ts` with `createProxyApplier({ proxyDomainList, audit })` — internal `prior` map diffs O(|desired|), no-op early-exit skips audit, single `proxy_allowlist_updated` entry per non-empty diff. Legacy `addSkillDomains`/`removeSkillDomains` untouched (phase 7 kills them).
**Files touched:** `src/host/proxy-domain-list.ts`, `src/host/skills/proxy-applier.ts` (new), `tests/host/skills/proxy-applier.test.ts` (new), `tests/host/proxy-domain-list.test.ts`.
**Outcome:** Success — `npx vitest run tests/host/proxy-domain-list.test.ts tests/host/skills/proxy-applier.test.ts` = 20/20 pass (16 proxy-domain-list, 4 proxy-applier). `rebuildMerged` invalidation covered transitively by the existing `isAllowed` tests.
**Notes:** The applier owns the diff state (closure-scoped `prior` map) rather than reading it back from `ProxyDomainList`. Keeps the public surface on the list minimal (`setAgentDomains` is write-only) and makes the applier equally testable with a mock list. Normalization lives in both modules — each has its own mandate: the list normalizes everything that enters it (defense in depth), the applier normalizes so its `prior` comparison uses the same keys the list would store.

## [2026-04-17 07:02] — Phase 4 Task 1: MCP applier module

**Task:** Bridge reconciler's `desired.mcpServers` to live `McpConnectionManager` — diff by source tag `skill:<agentId>` and call `addServer`/`removeServer` without touching plugin/database/other-agent-owned entries.
**What I did:** TDD'd the module with 7 cases: first-apply register, drop-unregister, idempotent no-op, URL-change re-register, cross-source untouched, global conflict emission (skip overwrite), and `bearerCredential` → `Authorization: Bearer ${TOKEN}` header placeholder. Wrote failing tests first (module-not-found), then implemented per plan skeleton verbatim. Audit entries emitted for each mutation; no-ops skip audit.
**Files touched:** `src/host/skills/mcp-applier.ts` (new), `tests/host/skills/mcp-applier.test.ts` (new).
**Outcome:** Success — `npx vitest run tests/host/skills/mcp-applier.test.ts` = 7/7 pass. No changes to orchestrator, server, or proxy surfaces (later tasks).
**Notes:** Silent-skip on same-name-different-source-same-URL is intentional (avoid double-register). URL-change path emits both `registered` and `unregistered` records; callers should treat them as a single replace event when summarizing.

## [2026-04-17 05:40] — Phase 2 PR #177 CodeRabbit review fixes (8)

**Task:** Apply 8 review fixes on PR #177 (git-native skills phase 2): orphan-repo cleanup, case-sensitive scope prefix, skip branch-deletion pushes, buffer-based HMAC, non-Error throw narrowing, atomic states+queue persistence, curl --data-binary, ifExists() on down migration.
**What I did:**
  1. `container/git-server/http-server.js`: best-effort `fs.rmSync(repoPath)` on hook-install failure so retries aren't wedged by the 409 "Repository already exists" branch.
  2. `src/providers/credentials/database.ts` + test: switched `listScopePrefix` from SQLite LIKE (ASCII-case-insensitive) to GLOB (case-sensitive) on sqlite, LIKE on postgres. Widened metachar guard to `/[*?[\]%_\\]/`. New test verifies "Alice" and "alice" don't collide.
  3. `src/providers/workspace/install-hook.ts` + `container/git-server/install-hook.js` + both test files: skip branch-deletion pushes (newSha all zeros) to prevent `skills.reconcile_failed` on `git push --delete`.
  4. `src/host/skills/hook-endpoint.ts` + test: `readRawBody` returns Buffer now (not string); HMAC computed over raw bytes to avoid U+FFFD utf-8 normalization mismatch with the shell client. Added regression test with a raw 0xC3 byte.
  5. `src/host/skills/reconcile-orchestrator.ts`: narrowed `(err as Error).message` to `err instanceof Error ? err.message : String(err)`.
  6. `src/host/skills/state-store.ts` + tests (state-store, current-state, reconcile-orchestrator unaffected): added `putStatesAndQueue(agentId, states, queue)` that wraps both table replacements in ONE transaction. Extracted `replaceStatesInTrx` + `replaceSetupQueueInTrx` helpers so single-table methods and atomic method can't drift. Orchestrator now calls the atomic variant. Added atomicity test using duplicate pk to force rollback.
  7. `src/providers/workspace/install-hook.ts` + container/git-server/install-hook.js` + both tests: `curl -d` → `curl --data-binary` to preserve exact body bytes for HMAC.
  8. `src/migrations/skills.ts`: `dropTable().ifExists()` on down migration.
**Files touched:** `container/git-server/http-server.js`, `container/git-server/install-hook.js`, `src/providers/credentials/database.ts`, `src/providers/workspace/install-hook.ts`, `src/host/skills/hook-endpoint.ts`, `src/host/skills/reconcile-orchestrator.ts`, `src/host/skills/state-store.ts`, `src/migrations/skills.ts`, `tests/providers/credentials/database.test.ts`, `tests/providers/workspace/install-hook.test.ts`, `tests/container/git-server/install-hook.test.ts`, `tests/host/skills/hook-endpoint.test.ts`, `tests/host/skills/state-store.test.ts`, `tests/host/skills/current-state.test.ts`.
**Outcome:** Success — `npm test -- tests/host/skills/ tests/providers/workspace/ tests/container/ tests/providers/credentials/` = 129/129 pass. `npm run build` clean. 9 commits, one per fix plus this journal update.
**Notes:** Fix 2 went dialect-aware (GLOB on sqlite, LIKE on postgres) rather than sqlite-only GLOB — PostgreSQL's LIKE is case-sensitive by default so plain LIKE is fine there, and a shared `substr(...)` approach would have been less idiomatic. Fix 6 factored the table-replace logic into helpers to kill any chance of drift between the two public code paths. Fix 3 and Fix 7 both touch the shell template in two places — the existing byte-identical test between host and container continues to enforce sync.

## [2026-04-16 23:55] — Phase 2 Task 6: mount /v1/internal/skills/reconcile

**Task:** Wire task 5's `createReconcileHookHandler` into the real router at `POST /v1/internal/skills/reconcile`. Run `skillsMigrations` at startup, build the state store, resolve `getBareRepoPath`, construct `OrchestratorDeps`, and hand the handler into the existing `extraRoutes` extension point so `server-request-handlers.ts` stays untouched.
**What I did:** Added imports at the top of `src/host/server.ts` for `runMigrations`, `skillsMigrations`, `createSkillStateStore`, `reconcileAgent`/`OrchestratorDeps`, `createReconcileHookHandler`, and `randomBytes`. Right after `initHostCore` returns, gated a block on `providers.database` (skills persistence needs it): runs `runMigrations(providers.database.db, skillsMigrations, 'skills_migration')`, builds `stateStore = createSkillStateStore(providers.database.db)`, reads `AX_HOOK_SECRET` from env (falls back to `randomBytes(32).toString('hex')` + writes back to `process.env` so task 8's installer sees a stable value + emits `ax_hook_secret_generated` warn log), defines `getBareRepoPath(agentId) = join(axHome(), 'repos', encodeURIComponent(agentId))` — mirrors `src/providers/workspace/git-local.ts` exactly — and builds the handler closing over the orchestrator deps. Added a new branch at the TOP of `handleInternalRoutes` (before the `!isK8s` gate) matching `url === '/v1/internal/skills/reconcile' && req.method === 'POST'` that either calls `reconcileHookHandler(req, res)` or returns 503 when database is absent. `mcpManager` left undefined (phase-2 YAGNI — phase 4 wiring). Wrote `tests/host/skills/mount-route.test.ts` that replicates `handleInternalRoutes`'s dispatch shape in a tiny local `http.createServer`, verifies signed requests → 200 + `reconcileAgent` invoked with `(agentId, ref)`, unsigned → 401, wrong-secret → 401, GET on same path → 404. 4/4 new tests pass. All 79 skills tests pass; 29 failures in `tests/host/server*.test.ts` are pre-existing macOS socket-path-length issues (reproduced with `git stash`, unchanged count).
**Files touched:** `src/host/server.ts`, `tests/host/skills/mount-route.test.ts` (new), `.claude/journal/host/skills.md`.
**Outcome:** Success — `npm run build` clean, 4/4 mount-route tests pass, all skills suite tests pass, no regressions from this change.
**Notes:** Mounted BEFORE the `!isK8s` gate so the route works in every deployment mode — the hook auth is HMAC per request, unlike the turn-token-gated `/internal/ipc` which is inherently k8s-only. `getBareRepoPath` only works correctly for git-local today; git-http/k8s deployments will bring their own reconcile entrypoint in phase-2 task 9 (the git-http container can call this endpoint directly with its own bare-repo view, or inline reconcile — TBD). 503 path added for the edge case of a deployment with no database provider — better than a silent 404 since the endpoint legitimately exists but is structurally unavailable. `AX_HOOK_SECRET` env-var contract is documented implicitly by the warn log; formal docs update will follow in task 8 once the installer actually consumes it.

## [2026-04-16 23:43] — Phase 2 Task 5: HMAC-guarded reconcile hook endpoint

**Task:** Build the HMAC-authenticated HTTP handler for the post-receive reconcile hook. Factory returns `(req,res) => Promise<void>`. Route mounting is task 6.
**What I did:** TDD. Wrote 12 failing tests in `tests/host/skills/hook-endpoint.test.ts` covering: missing / wrong-prefix / wrong-length / non-hex / wrong-HMAC signatures (→ 401), valid sig + missing `agentId` / extra field (strict) / invalid JSON (→ 400 with `{error:'Invalid request', details}`), valid sig + body (→ 200 `{ok:true, skills:1, events:2}` + `reconcileAgent('agent-42','refs/heads/skills')`), case-insensitive header lookup, body > 64 KiB (→ 413 without calling reconcileAgent), and `reconcileAgent` throwing (→ 500, handler catches). Used `Readable.from(Buffer.from(body))` for the fake `IncomingMessage` and a minimal object for `ServerResponse` (writeHead/setHeader/write/end) — matches the pattern in `tests/host/server-webhooks.test.ts`. Verified all 12 fail (module not found). Implemented `src/host/skills/hook-endpoint.ts`: `createReconcileHookHandler({secret, reconcileAgent})` streams the body with a 64 KiB cap via a `data/end/error` promise (destroys stream on overflow), pulls `x-ax-hook-signature` from lowercase headers, `verifySignature` checks `sha256=` prefix + `^[0-9a-f]{64}$` regex (the length gate that prevents `timingSafeEqual` from throwing) + constant-time compare, parses + `.strict()` Zod-validates the body only AFTER signature verification (so attackers can't probe for Zod-specific error shapes), then wraps the `reconcileAgent` await in try/catch and logs via `getLogger().child({component:'skills-hook-endpoint'})`. Local `sendError` / `sendJson` helpers (the existing `sendError` in `server-http.ts` produces `{error:{message,type,code}}` which doesn't match the `{error, details?}` shape the task spec calls for).
**Files touched:** `src/host/skills/hook-endpoint.ts` (new), `tests/host/skills/hook-endpoint.test.ts` (new), `.claude/journal/host/skills.md`, `.claude/lessons/security/entries.md`.
**Outcome:** Success — 12/12 new tests pass, 75/75 in `tests/host/skills/` pass, `tsc` clean. Full suite has 33 pre-existing unrelated failures in `server.test.ts`, `server-history.test.ts`, `server-multimodal.test.ts`, `smoke.test.ts`, `history-smoke.test.ts` — confirmed present before my changes (moved files out, reran, same failures).
**Notes:** Body size cap runs BEFORE signature verification (task spec order), which is correct: no point HMAC-ing 2 MB of garbage. Verifying signature BEFORE parsing is the byte-exact contract — the hook computes HMAC over the exact bytes it sent, not over JSON round-trip. `timingSafeEqual` throws on buffers of unequal length, hence the regex length gate. Task 6 will import `createReconcileHookHandler` and mount it at `/v1/internal/skills/reconcile` on the real router.

## [2026-04-16 23:36] — Phase 2 Task 4: reconcile orchestrator

**Task:** Build `reconcileAgent(agentId, ref, deps)` — glue that runs the full reconcile cycle: snapshot → current-state → `reconcile()` → persist states + setup queue → emit events. Phase 2 intentionally discards `output.desired` (MCP/proxy wiring lands in phase 4). Push-time hook MUST NOT 500 the push on a reconcile error.
**What I did:** TDD. Wrote 4 integration tests using real snapshot builder (real bare git repo + work-tree push), real reconciler, real state-store (in-memory sqlite + `skillsMigrations`), with stubs only at the provider boundary (ProxyDomainList, CredentialProvider, EventBus). Covered: happy path (missing creds → pending, 2 events, setup queue populated), enabled path (creds + domain met, 2 events, empty queue), snapshot failure (bogus path → returns zeroed counts, emits `skills.reconcile_failed`, no partial writes to state-store), and multi-reconcile replacement (skill A replaced by skill B → A is removed, B is present, `skill.removed` emitted). Verified failing (no module). Implemented `src/host/skills/reconcile-orchestrator.ts`: try/catch wraps every step; on error logs via `getLogger().child({component:'skills-reconcile'}).error('reconcile_failed', …)`, emits single `skills.reconcile_failed` event, returns `{skills:0, events:0}`. Each successful event fired with `requestId === agentId` and `Date.now()` timestamp.
**Files touched:** `src/host/skills/reconcile-orchestrator.ts` (new), `tests/host/skills/reconcile-orchestrator.test.ts` (new), `.claude/journal/host/skills.md`.
**Outcome:** Success — 4/4 new tests pass, full skills suite 63/63 passes, `tsc --noEmit` clean.
**Notes:** YAGNI — `output.desired` is computed by the reconciler but dropped here; phase 4 will pick it up. Error path is holistic: a throw from `getBareRepoPath`, snapshot, current-state, reconcile, `putStates`, `putSetupQueue`, or the emit loop all land in the same catch, so a fully-failed reconcile leaves prior state-store rows untouched (covered by test 3). Note a partial failure AFTER `putStates` but BEFORE `putSetupQueue` WOULD leave the state-store partially updated — phase 2 accepts this because the individual table writes are already transactional and the reconciler never inspects the setup queue during future reconciles. Events sequence: skill.installed first, then status events (enabled/pending/invalid) — matches phase-1 computeEvents output.

## [2026-04-16 23:30] — Phase 2 Task 3: current-state loader

**Task:** Build a pure async `loadCurrentState(agentId, deps)` that aggregates the `ReconcilerCurrentState` (approvedDomains, storedCredentials, registeredMcpServers, priorSkillStates) by polling existing host providers. Its role is to hide provider-specific key formats from the reconcile orchestrator.
**What I did:** TDD. Wrote 3 failing tests (happy path, no mcpManager, empty-everything) using plain-object stubs — no mocking framework. Extended `CredentialProvider` with `listScopePrefix(prefix): Promise<Array<{scope, envName}>>`, implemented it in `src/providers/credentials/database.ts` using Kysely `where('scope','like',`${prefix}%`)` with a LIKE-metachar guard (throws on `%/_/\`). Updated every inline mock in the test suite (dotenv, credential-scopes, credential-injection-integration, skills-credential, harness, mcp/database) to include the new stub. Implemented `src/host/skills/current-state.ts`: reads `proxyDomainList.getAllowedDomains()`, walks `credentials.list(credentialScope(agentName))` → `${env}@agent`, walks `credentials.listScopePrefix('user:${agentName}:')` → `${env}@user`, builds mcp Map from optional manager (empty when absent), awaits `stateStore.getPriorStates(agentId)`.
**Files touched:** `src/host/skills/current-state.ts` (new), `tests/host/skills/current-state.test.ts` (new), `src/providers/credentials/types.ts` (added method), `src/providers/credentials/database.ts` (added method), `tests/dotenv.test.ts`, `tests/host/credential-injection-integration.test.ts` (4 stubs), `tests/host/credential-scopes.test.ts`, `tests/host/ipc-handlers/skills-credential.test.ts`, `tests/provider-sdk/harness.test.ts`, `tests/providers/mcp/database.test.ts` (4 stubs), `.claude/journal/host/skills.md`.
**Outcome:** Success — 3/3 new tests pass, 76/76 in `tests/host/skills/ tests/providers/credentials/` pass, 123/123 across every touched file pass, `tsc` clean. (Pre-existing unrelated socket-path-length failures in `tests/host/server*.test.ts` remain — confirmed present on clean `main`.)
**Notes:** YAGNI: user scope lookup treats "any user has the credential" as sufficient — `${env}@user` lands in the set if ANY `user:<agentName>:*` row exists. Phase 6 OAuth will refine once userId is available. LIKE prefix is built as raw string concat `user:${agentName}:` rather than squeezing `credentialScope` into a prefix helper — keeps the parallel with `credentialScope(agentName, userId)` obvious. The `listScopePrefix` DB guard rejects `%/_/\` in the prefix — cheap defense against a caller that accidentally allows user-controlled prefixes (agentIds are alphanumeric today, so this never fires in prod).

## [2026-04-16 23:18] — Phase 2 Task 2: skill state store + migration

**Task:** Build sqlite-backed persistence for per-agent skill states (name+kind+description+reasons/error) and per-agent setup queue (one `SetupRequest` per pending skill). Both keyed by `(agent_id, skill_name)`. A reconcile for an agent must atomically replace all its rows (no stale rows for removed skills).
**What I did:** TDD. Wrote 10 tests covering: empty-agent read, two-skill roundtrip with different kinds, authoritative replace, empty-list clears rows, other-agent isolation, pendingReasons JSON round-trip via raw row, setup-queue empty read, full SetupRequest (nested creds/oauth/domains/mcpServers) roundtrip, empty-queue clears only that agent, queue replace. Verified failing (module missing). Implemented `src/migrations/skills.ts` (two tables + composite PKs + `idx_skill_states_agent`, `sqlEpoch` default) mirroring `src/migrations/jobs.ts` shape. Implemented `src/host/skills/state-store.ts` — `createSkillStateStore(db)` factory returning `{getPriorStates, putStates, putSetupQueue, getSetupQueue}`. Both put-methods run delete-then-insert in ONE `db.transaction().execute(async trx => …)` for atomicity. YAGNI: no full-row read of SkillState, no cross-agent listing, no hooks into host startup (that's task 6).
**Files touched:** `src/migrations/skills.ts` (new), `src/host/skills/state-store.ts` (new), `tests/host/skills/state-store.test.ts` (new), `.claude/journal/host/skills.md`.
**Outcome:** Success — 10/10 new tests pass; full skills suite 56/56 passes; `tsc --noEmit` clean.
**Notes:** `updated_at`/`created_at` omitted on insert so the `sqlEpoch(dbType)` default fires. `pendingReasons` persists as JSON text — `getPriorStates` only returns kind (it's all the reconciler needs), so we don't deserialize reasons on the read path. `getSetupQueue` orders by `skill_name` asc for deterministic output, even though callers typically don't care. Consumers: `src/host/skills/current-state.ts` (task 3) reads `getPriorStates`; `src/host/skills/reconcile-orchestrator.ts` (task 4) writes both tables.

## [2026-04-16 23:12] — Phase 2 Task 1: snapshot builder

**Task:** Build a pure snapshot builder that walks `.ax/skills/**/SKILL.md` in a given bare-repo ref and returns a `SkillSnapshotEntry[]` for the phase-1 reconciler to consume.
**What I did:** TDD. Wrote failing test (valid+invalid pair sorted, no-`.ax/skills`, empty repo, orphan skill dir). Implemented `buildSnapshotFromBareRepo(bareRepoPath, ref)` using `execFile` (no shell) with `git ls-tree -r --name-only <ref> -- .ax/skills/`, regex `^\.ax/skills/([^/]+)/SKILL\.md$`, then `git show <ref>:<path>` + `parseSkillFile`. Sort by name ascending. No try/catch swallowing; empty stdout naturally yields `[]` when no skills tree exists.
**Files touched:** `src/host/skills/snapshot.ts` (new), `tests/host/skills/snapshot.test.ts` (new), `.claude/journal/host/skills.md` (new).
**Outcome:** Success — 4/4 new tests pass, full skills suite 46/46 passes, `tsc` clean.
**Notes:** Kept it minimal (YAGNI): no caching, no parallel exec, sequential awaits. `encoding: 'buffer'` + `Buffer.toString('utf-8')` to be robust with non-ASCII skill content. `maxBuffer: 16 MiB` on `git show` to defensively allow large SKILL.md files without surprising truncation. Tests exercise real git subprocesses on real bare repos — not mocked.
