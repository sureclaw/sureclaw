# tool-catalog

Pure data layer for the tool-dispatch-unification migration: `CatalogTool` type + session-scoped registry, feeding the one-liner prompt renderer and the eventual unified dispatcher.

## [2026-04-22 05:30] — Phase 8 Task 8.1 + 8.2: wide-surface advisory + scripts/ guidance

**Task:** Final Phase 8 work. (8.1) Emit an informational diagnostic when a skill's MCP server or OpenAPI source has a wide surface (>20 MCP tools or >30 OpenAPI operations) and no `include:` filter is set — surfaces via the B1-B3 banner pipeline so skill authors see the nudge at catalog-populate time, not weeks later when they notice prompt budget issues. (8.2) Teach skill-creator about `.ax/skills/<name>/scripts/` for multi-step recipes so the LLM doesn't re-derive the same dance every turn from prose.
**What I did:** Added `WIDE_MCP_THRESHOLD = 20` and `WIDE_OPENAPI_THRESHOLD = 30` constants to `src/host/skills/catalog-population.ts`. MCP path: after `client.listTools()` returns, if `mcpTools.length > 20 && !server.include`, push a `catalog_wide_mcp_server` diagnostic (severity: info). OpenAPI path: after `buildOpenApiCatalogTools` returns (which is what the adapter filters through include/exclude), if `catalogTools.length > 30 && !source.include`, push a `catalog_wide_openapi_source` diagnostic. `!source.include` is the right gate because when include is set, catalogTools.length is the POST-filter count and reflects the author's deliberate scope. Does NOT affect population — every tool still lands in the catalog. 5 new tests: MCP wide+no-include pushes, MCP at-threshold (20) does NOT push, MCP wide+include does NOT push (author scoped deliberately), OpenAPI wide+no-include pushes with counts/context, OpenAPI wide+include does NOT push. Also updated `skills/skill-creator/SKILL.md`: rewrote the include-scoping advice under a new `### Scoping wide surfaces with include:` subsection with examples for both MCP and OpenAPI patterns, and added a new `### Scripts for multi-step recipes` subsection under "Body structure" teaching `.ax/skills/<name>/scripts/` as the place for reusable recipes.
**Files touched:** `src/host/skills/catalog-population.ts` (+44 lines), `tests/host/skills/catalog-population.test.ts` (+5 tests), `skills/skill-creator/SKILL.md` (include-scoping rewrite + scripts section).
**Outcome:** Success. 28/28 catalog-population tests green (was 23, +5). Build clean. Phase 8 is now complete — rollout plan wraps up here.
**Notes:** Severity is `info` not `warn` on purpose: the skill still works, just less efficiently. The banner is advisory, not alarming. The MCP threshold of 20 is aggressive by design — most modern SaaS MCP servers ship 5-12 tools (Linear: 3, Slack: 8, Sentry: 9). A skill exposing >20 is likely an auto-generated REST-to-MCP wrapper where scoping genuinely matters. OpenAPI's 30 is proportionally higher because even small REST APIs often publish 30+ operations.

## Entries

## [2026-04-21 19:05] — Task B3: chat UI diagnostic banner (surface catalog-populate failures)

**Task:** Final link of the 3-task "surface skill/catalog failures" pipeline. B1 shipped the collector + wiring; B2 emitted named SSE frames (and uniform JSON); B3 parses those frames on the client and shows a dismissible banner under the most-recent assistant message. No host changes — this is pure chat-ui.
**What I did:**
- Transport (`ui/chat/src/lib/ax-chat-transport.ts`): exported new `Diagnostic` interface (severity/kind/message/context/timestamp wire shape), added `onDiagnostic?` callback in `AxChatTransportOptions`, stored it on the class, captured as a local `diagnosticCallback` inside `processResponseStream` (same `this`-capture idiom as `statusCallback`), and slotted a `pendingEventName === 'diagnostic'` branch next to the `status` and `content_block` branches with the matching try/catch so malformed JSON is skipped silently.
- Runtime (`ui/chat/src/lib/useAxChatRuntime.tsx`): added 4th `onDiagnostic` arg, threaded via `useRef` + ref.current wrapper exactly like `onStatus`/`onRunStart` (so changing callback identity doesn't re-construct the transport and tear down the chat hook).
- App (`ui/chat/src/App.tsx`): `useState<Diagnostic[]>([])`, `handleRunStart` clears it (new turn = fresh slate), `handleDiagnostic` appends, prop-drilled into `<Thread>`.
- Thread (`ui/chat/src/components/thread.tsx`): new `DiagnosticsContext` mirroring `StatusMessageContext`, new `DiagnosticBanner` with severity-driven palette (error→rose, warn→amber, info→sky — all already in `index.css` custom tokens so no new CSS), a dismissal `useState` that auto-resets when `diagnostics.length` changes (fresh batch re-shows), `+N more` collapse when >3, pill-style top border + `bg-muted/30` body, `role="alert"`. Rendered inside `AssistantMessage` in an `AuiIf` gated on `msg.isLast === true` so old turns don't keep showing banners. Positioned between `MessagePrimitive.Parts` and `ActionBarPrimitive.Root`.
- Tests (`ui/chat/src/lib/ax-chat-transport.test.ts`, new file): 5 vitest cases exercising `processResponseStream` through a type-cast — single diagnostic, multiple diagnostics, zero diagnostics (callback never fires), malformed JSON (dropped, subsequent events keep flowing), and SSE frames split across `TextDecoderStream` chunks (carry-buffer correctness). All 5 pass.
**Files touched:**
- Modified: `ui/chat/src/lib/ax-chat-transport.ts`, `ui/chat/src/lib/useAxChatRuntime.tsx`, `ui/chat/src/App.tsx`, `ui/chat/src/components/thread.tsx`
- Created: `ui/chat/src/lib/ax-chat-transport.test.ts`
**Outcome:** Success. Transport tests 5/5 green, `npm run build` at ui/chat and repo root both clean, no new deps.
**Notes:**
- Banner severity is the **max** of all diagnostics shown (error > warn > info) — a single error promotes the whole strip to rose, because that's the most honest visual summary.
- Dismiss is per-batch: `useEffect` on `diagnostics.length` resets `dismissed` and `expanded` so a new diagnostic arriving in the same turn re-shows the banner (user won't silently miss a second failure after dismissing the first).
- Icons: `AlertTriangleIcon` (warn), `OctagonAlertIcon` (error — lucide-react replaced `AlertOctagonIcon` with `OctagonAlertIcon`), `InfoIcon` (info). Kind shown in monospace accent color so users can grep it in docs later; context.skill/source as muted subtle line under the message.
- Gate on `msg.isLast === true` only (not `thr.isRunning`) — diagnostics persist visually through stream close and stay until the next turn clears them, matches the B2 wire-shape intent.

## [2026-04-21 18:57] — Task B2 follow-up: `attach` helper + JSON diagnostics always-emit

**Task:** Code-quality reviewer flagged two Important items on commit `cbef0775` (B2). (I1, confidence 82) the 8 return sites in `processCompletion` each repeat `diagnostics: diagnostics.list()` — the next contributor adding a return will silently forget (empty diagnostics, no test fail, missing banner). (I2, confidence 80) the JSON non-streaming branch's empty-array-omits-field asymmetry forces B3's chat UI to handle two shapes (`diagnostics === undefined` vs `[]`). Fix structurally: a local `attach` helper eliminates per-site repetition; always-emit-on-JSON removes the shape fork. The pre-B2 wire-shape-compat argument held for external REST callers only — the chat UI is always-in-house and uniform `diagnostics: []` is strictly better.
**What I did:** TDD. (1) Flipped the two JSON tests in `server-request-handlers-diagnostics.test.ts` to assert `parsed.diagnostics === []` instead of `not.toHaveProperty('diagnostics')` — one for empty-array input, one for absent field (must normalize to `[]`). Updated test titles + comments to explain the new "uniformity beats wire-shape bit-compat" rationale. Verified red (2 failures, `undefined !== []`). (2) Collapsed the `diagField` spread in `server-request-handlers.ts` from `result.diagnostics?.length ? {diagnostics: result.diagnostics} : {}` to a flat `diagnostics: result.diagnostics ?? []` on the response object. Rewrote the comment block to spell out the new design ("chat UI is the primary non-streaming consumer", "snapshot-test churn is a one-time mechanical cost", "silent undefined-access bug is a recurring reliability cost"). Green. (3) Added the `attach` helper immediately after `const diagnostics = createDiagnosticCollector()` in `processCompletion`: `const attach = <T extends {responseContent: string; finishReason: 'stop'|'content_filter'}>(r: T): T & {diagnostics: readonly Diagnostic[]} => ({...r, diagnostics: diagnostics.list()})`. Generic `T` preserves whatever optional fields the caller included (contentBlocks, extractedFiles, agentName, userId) without enumerating them — critical for the main success-path return which carries 5 extra fields. (4) Swept all 8 return sites: `return { ..., diagnostics: diagnostics.list() }` → `return attach({ ... })`. Sites: scan-BLOCK content-filter, queue-dequeue-failed, fast-path success, fast-path error, no-API-creds, agent-processing-failed, main sandbox success, internal-error catch. Built + ran — 12/12 diagnostic tests green, 1324/1353 broader host (same 29 pre-existing socket-EINVAL failures as before, no regressions).
**Files touched:** `src/host/server-completions.ts` (`attach` helper + 8 return-site sweeps), `src/host/server-request-handlers.ts` (non-streaming always-emits `diagnostics: []`, comment updated), `tests/host/server-request-handlers-diagnostics.test.ts` (2 flipped tests asserting `[]` on empty/absent inputs).
**Outcome:** Success. `npm run build` clean. Targeted diagnostic tests: 12/12. Broader `tests/host/`: 1324/1353 — no new regressions. Both invariants now structural rather than discipline-based: (a) a new `processCompletion` return path without `attach(...)` stands out visibly in review (every other exit path uses the idiom), and the JSON handler's `?? []` still normalizes to the uniform shape at the boundary as a belt-and-suspenders; (b) non-streaming consumers see `diagnostics: []` uniformly, no undefined-guard needed in B3's UI.
**Notes:** Two judgment calls. (a) **Kept `CompletionResult.diagnostics` as `readonly Diagnostic[] | undefined` rather than tightening to required** — would break any external caller stubbing the result type and forces a pile of `satisfies CompletionResult` churn for zero runtime safety gain, since the JSON handler normalizes `undefined` → `[]` at the boundary already. The `attach` helper is the enforcement point for new internal code; the handler's `?? []` is the belt-and-suspenders for legacy/external callers. (b) **Didn't add `@ts-expect-error` or a lint rule requiring `attach(...)`** — the convention is discoverable (eight adjacent call sites + a helper comment pointing at it) and the consequence of missing one is "diagnostics empty on that exit path," which is the B1 fallback behavior, not a crash. A lint rule would be over-engineering for a file-local invariant.

## [2026-04-21 18:50] — Task B2: emit catalog-populate diagnostics on SSE + JSON

**Task:** Second of three tasks (B1/B2/B3) in the "surface skill/catalog failures" pipeline. B1 (commit `de990625`) added the `DiagnosticCollector` primitive and wired it INTO `populateCatalogFromSkills(...)` so two failure paths (MCP `listTools`, OpenAPI spec fetch) now push structured diagnostics as a parallel signal alongside the existing `logger.warn(...)` lines. B2's job: thread a per-turn collector through `processCompletion`, pass it to the `populateCatalogFromSkills` call inside the `getOrBuildCatalog` `build` closure, attach the snapshot to every `CompletionResult` return, and emit those diagnostics on the wire — as `event: diagnostic` named SSE frames in streaming mode, as a top-level `diagnostics` field on the JSON body in non-streaming mode. B3 (chat UI banner) reads the SSE frames next.
**What I did:** TDD. (1) Wrote `tests/host/server-request-handlers-diagnostics.test.ts` — 7 tests: streaming emits `event: diagnostic` AFTER finish-reason chunk AND BEFORE `[DONE]`; streaming emits one frame per diagnostic in insertion order; streaming with empty array emits zero frames (clean turn = no banner); streaming with absent diagnostics field behaves identically; non-streaming attaches `diagnostics` as top-level JSON field; non-streaming with empty array OMITS the field (keeps wire shape identical to pre-B2 for callers that don't care); non-streaming with absent field also omits. Used a fake `IncomingMessage` (EventEmitter with `Symbol.asyncIterator` yielding the request body) and a fake `ServerResponse` capturing every `res.write(...)` into a string array. Verified red (API didn't accept `diagnostics` yet). (2) Wrote `tests/host/server-completions-diagnostics.test.ts` — 5 integration-lite tests exercising the same `populateCatalogFromSkills({diagnostics, ...})` wire-up pattern `processCompletion` uses (MCP failure captured by the closure, OpenAPI failure captured, clean build produces empty list, cache-hit simulation leaves collector empty, two sequential turns don't share state). Chose integration-lite over a full `processCompletion` stub because the sandbox-path setup (provisioner, workspace provider, IPC socket, sandbox factory, ...) makes a meaningful end-to-end test infeasible at the unit layer — and the full-stack check happens in the SSE test where a mocked `runCompletion` returns `diagnostics` and we assert the wire. Verified red. (3) Extended `CompletionResult` with `diagnostics?: readonly Diagnostic[]`, imported `createDiagnosticCollector` into `src/host/server-completions.ts`, created a fresh collector at the top of `processCompletion` (one per turn — not shared, not module-scoped), passed it into `populateCatalogFromSkills` via the new `diagnostics` field inside the `getOrBuildCatalog` `build` closure (captured by reference — pushes from the closure survive into the outer scope), and threaded `diagnostics: diagnostics.list()` through every `return {...}` site inside `processCompletion` (fast-path success, fast-path error, sandbox success, sandbox agent-failure, content-filter block, internal-error paths). Cache-hit behavior falls out naturally: when `getOrBuildCatalog` returns from cache without firing `build`, the collector stays empty, the snapshot is `[]`, and the UI sees no banner — correct because the user already saw the banner on the turn that did the build. (4) Updated `CompletionHandlerOpts.runCompletion` return type in `server-request-handlers.ts` with the same `diagnostics?: readonly Diagnostic[]` field. (5) Emitted on SSE — between the final `sendSSEChunk(...)` finish-reason frame and `res.write('data: [DONE]\n\n')`, iterate `result.diagnostics ?? []` and `sendSSENamedEvent(res, 'diagnostic', d)` each one. Order matters: SSE clients treat anything before the finish-reason chunk as streaming content. (6) Handled non-streaming — added `const diagField = result.diagnostics?.length ? { diagnostics: result.diagnostics } : {};` spread into the JSON response envelope. Empty-array path omits the field entirely rather than emitting `diagnostics: []`, keeping the wire shape identical to pre-B2 for callers that don't care. (7) Updated `processCompletionForSession`'s return-type annotation in `server.ts` to include `diagnostics?` so the field flows through the wrapper. Green across the board.
**Files touched:** `src/host/server-completions.ts` (collector creation at top of `processCompletion`, `diagnostics` threaded into `populateCatalogFromSkills` build closure, attached to every `CompletionResult` return; `CompletionResult.diagnostics` field added to the interface). `src/host/server-request-handlers.ts` (`CompletionHandlerOpts.runCompletion` return type extended; streaming mode emits `event: diagnostic` between finish-reason and `[DONE]`; non-streaming mode spreads a top-level `diagnostics` field when non-empty). `src/host/server.ts` (`processCompletionForSession` return type annotation extended). `tests/host/server-request-handlers-diagnostics.test.ts` (new, 7 tests). `tests/host/server-completions-diagnostics.test.ts` (new, 5 tests).
**Outcome:** Success. `npm run build` clean. Targeted (the two new files + existing `server-completions-*.test.ts` + `server-request-handlers-status.test.ts` + `diagnostics.test.ts` + `skills/catalog-population.test.ts`): 54/54. Broader (`tests/host/` full): 1324/1353 passing, 29 pre-existing failures in `server.test.ts`/`server-history.test.ts`/`server-multimodal.test.ts` (Unix socket EINVAL from long tmpdir paths — verified same 29 fail on `de990625` before my changes). No regressions introduced. B3 can now extend `ui/chat/src/lib/ax-chat-transport.ts` to parse `event: diagnostic` frames and render a banner.
**Notes:** Three judgment calls. (a) **Attach `diagnostics: diagnostics.list()` to EVERY return path, even legacy error returns like "message not queued" that happen BEFORE the catalog build** — those always attach an empty list. Cheap, keeps the contract uniform (`CompletionResult.diagnostics` is always set when the caller is in the post-collector world), avoids a "sometimes undefined, sometimes empty" wart that B3 would have to special-case in the UI. TypeScript doesn't force uniform presence (the field is optional) but runtime uniformity is worth the extra `.list()` calls. (b) **Non-streaming omits the field on empty** vs streaming which emits zero frames — wire-shape asymmetry is deliberate. Streaming's "zero frames" is the natural state of the protocol (clients count frames, not a boolean field). Non-streaming is JSON, and emitting `"diagnostics":[]` would surface in JSON consumers' snapshot tests / typed clients for zero reason. A consumer who WANTS the field on every response can treat absent as empty. (c) **Integration-lite vs full `processCompletion` test** — the test spec explicitly permitted this ("a lot of server-completions tests are integration-style and heavy"). The real end-to-end check happens at the SSE layer where `runCompletion` is mocked to return a mocked result with diagnostics — if the wire emits them, the threading inside `processCompletion` is proven by the fact that the field exists on the result type and is read by the handler. The integration-lite test pins the per-turn freshness + closure-captures-by-reference invariants, which matter for correctness and are hard to spot in types.

## [2026-04-21 18:35] — Task B1: diagnostic collector + catalog-population wiring

**Task:** First of three tasks (B1/B2/B3) building a user-visible diagnostic surfacing pipeline. Today's trigger: user installed an OpenAPI-backed `petstore` skill, its catalog tools never appeared (spec fetch/parse failure), and the only signal was a `catalog_populate_openapi_parse_failed` host log entry invisible to the chat UI. B1's job: host-side in-memory collector + wire the two user-facing catalog-populate failure paths (MCP `listTools`, OpenAPI spec fetch) to push structured diagnostics alongside the existing log lines. B2 will emit those as SSE events, B3 will render them as a dismissible banner.
**What I did:** TDD. (1) Wrote `tests/host/diagnostics.test.ts` — 7 tests: empty-on-construction, push+list preserves insertion order, push adds valid ISO-8601 timestamp matching `\d{4}-\d{2}-\d{2}T...Z`, context field preserved, reset clears + buffer reusable after reset, overflow cap at 50 (push 51 → list has 50 = 49 originals [entries 2..50] + `diagnostic_overflow` marker at tail), immutability (mutating returned array doesn't affect collector). Verified red (module missing). (2) Implemented `src/host/diagnostics.ts` — exports `DiagnosticSeverity`, `Diagnostic`, `DiagnosticCollector` types + `createDiagnosticCollector()` factory. Overflow design: on the push that crosses MAX_DIAGNOSTICS (50), drop two oldest real entries and append one overflow marker — buffer settles at 49 retained originals + 1 marker. Subsequent pushes in overflow mode drop oldest real entry and slot the new one before the marker (keeps marker at tail, keeps recent real entries, silently caps at 50). `list()` returns `buf.slice()` as `readonly Diagnostic[]` — defensive copy blocks `as Diagnostic[]` casts from mutating state. Green. (3) Wrote failing tests in `tests/host/skills/catalog-population.test.ts` under a new `describe('diagnostics')` block — 5 tests: MCP listTools rejection pushes one `catalog_populate_server_failed` with skill+server in message and context; OpenAPI fetch rejection pushes one `catalog_populate_openapi_source_failed` with skill+spec-URL in message; no-collector path still works (counters populated, no throw); both-kinds in one call lands both entries; `toolRegisterFailures` (duplicate names) deliberately does NOT push (skill-author concern). Verified red. (4) Wired catalog-population — added optional `diagnostics?: DiagnosticCollector` to `PopulateCatalogFromSkillsInput`, destructured in the handler, added `.push({...})` calls in the MCP server-failure catch and the OpenAPI source-failure catch. Kept existing `logger.warn(...)` calls EXACTLY as they were — diagnostics are a parallel signal, not a replacement. Chose kind `catalog_populate_openapi_source_failed` for the OpenAPI diagnostic (vs. log event name `catalog_populate_openapi_parse_failed`) because "the spec source failed" is closer to the user's mental model than "we couldn't parse it" — fetch/parse/dereference all land in the same catch, and from the user's POV the source just didn't work. Green.
**Files touched:** `src/host/diagnostics.ts` (new, 90 LOC), `src/host/skills/catalog-population.ts` (added `diagnostics?` field to input + 2 `.push` call sites), `tests/host/diagnostics.test.ts` (new, 7 tests), `tests/host/skills/catalog-population.test.ts` (+5 tests).
**Outcome:** Success. `npm run build` clean. Targeted (`tests/host/diagnostics.test.ts` + `tests/host/skills/catalog-population.test.ts`): 30/30. Broader (`tests/host/skills/` + `tests/host/diagnostics.test.ts`): 209/209 — no regressions. B1 foundation ready; B2 (SSE emission) can now wire `createDiagnosticCollector()` into `server-completions.ts` and `populateCatalogFromSkills(...)` will automatically start pushing the existing-turn's failures into it.
**Notes:** Three judgment calls. (a) **Kind vs. log event name divergence** — the OpenAPI diagnostic's `kind` is `catalog_populate_openapi_source_failed` but the log event stays `catalog_populate_openapi_parse_failed`. Deliberate. Log event names are a host-ops vocabulary ("parse-dereference-fetch blew up") and renaming them would churn ops grep patterns. Diagnostic kinds are a UI vocabulary ("the source didn't work"). Kept them separate with an inline comment pointing this out so a future reader doesn't "fix" it. (b) **`toolRegisterFailures` deliberately NOT pushed** — duplicate-name collisions are a deterministic skill-author bug (two skills both want `api_petstore_list_pets`), not a transient user-facing failure. The user can't fix it; the skill author can. Surfacing it in the UI would only confuse. Added a negative test to pin the behavior. (c) **Overflow "second marker" semantics** — on overflow, pushes beyond the cap don't stack markers; one marker stays at the tail and newer real entries slot in before it. Simpler than counting suppressed-count-N in the marker message (and the UI can always say "at least one additional diagnostic suppressed" without a precise count). If B2/B3 needs a count, `collector` can grow a `suppressedCount()` accessor later without breaking the wire format.

## [2026-04-21 17:52] — Task 7.4 quality follow-up: URLSearchParams, header CRLF, repeated path tokens

**Task:** Code-quality review flagged three Important items on the combined 7.4 work. (1) Query-string carry-over was string-pieced (`queryPieces.push(url.search.slice(1))` then `queryPieces.join('&')`), fragile against a `baseUrl` ending with `?foo=1` or trailing `&`. (2) Header values from LLM-controlled args were passed to `fetch` without CRLF validation — undici rejects them today with a cryptic "Invalid header value", but a future custom `fetchImpl` makes it a CRLF-injection vector. (3) Path substitution used `String.replace(/\{.../g, cb)` with `delete workingArgs[name]` inside the replacer callback, so a spec with a repeated token like `/a/{id}/b/{id}` threw "Missing required path parameter: id" on the second match even though the same arg should be reused.
**What I did:** TDD. (1) Wrote 5 failing tests FIRST: baseUrl-with-trailing-slash-and-existing-query merges cleanly (`/v1/pets?foo=1&limit=10`), header value with CRLF rejected with named header, header value with bare LF rejected, header value with null byte rejected, ordinary printable value passes through, and repeated path tokens `/a/{id}/b/{id}` substituted to `/a/5/b/5`. Verified all 5 red. (2) Replaced string-pieceing with `URLSearchParams.append` — iterate query params, primitives/arrays use `stringifyForSearchParams` (skip manual `encodeURIComponent`; `URLSearchParams` handles it). Also replaced `api_key_query` auth injection with `url.searchParams.append('api_key', value)`. Removed the now-dead `serializeQueryParam` helper and the trailing `url.search = '?' + queryPieces.join('&')` block. (3) Added `isInvalidHeaderName` (`[^\x21-\x7E]` — rejects whitespace/control/non-ASCII) and `isInvalidHeaderValue` (`[\x00-\x1F\x7F]` — rejects all C0 controls + DEL). Before `headers[p.name] = stringValue`, both name and value are checked; violations throw `Invalid header value for ${name}: contains control characters` which flows through the handler's try/catch into `dispatch_failed` with the readable message. (4) Refactored path substitution into a two-pass design: pass 1 walks `path.matchAll(/\{([^}]+)\}/g)` collecting the unique set of referenced path-token names; pass 2 validates each name has a value in args and uses `path.replaceAll('{name}', encodePrimitive(raw))` to substitute ALL occurrences at once; a final loop deletes the consumed names from `workingArgs` AFTER the full replacement pass so repeated tokens reuse the same arg. (5) Discovered a pre-existing bug while writing the baseUrl-with-query test: the old `baseUrl.replace(/\/+$/, '') + substitutedPath` string concat spliced the operation path INTO the baseUrl's query when baseUrl ended with `?foo=1` (got `/v1/?foo=1/pets` instead of `/v1/pets?foo=1`). Fixed by parsing baseUrl as a `URL` first, reconstructing `${origin}${pathname_sans_trailing_slash}${substitutedPath}` explicitly, applying `applyUrlRewrite` to THAT, re-parsing, then copying baseUrl's `searchParams` onto the new URL so the baked-in query survives. Works uniformly for fragments too (drops them at the `baseUrl.pathname` step, matching the spec's intent: the spec's server URL defines the origin/prefix, the operation's path is what we route).
**Files touched:** `src/host/ipc-handlers/openapi-dispatcher.ts` (path substitution + URL composition + query building + header validation), `tests/host/ipc-handlers/call-tool-openapi.test.ts` (+5 tests).
**Outcome:** Success. `npm run build` clean. `tests/host/ipc-handlers/call-tool-openapi.test.ts` 32/32 (was 26, +5 for this fix plus +1 for the discovered baseUrl bug). Broader targeted (`tests/host/ipc-handlers/` + `tests/host/tool-catalog/adapters/openapi.test.ts` + `tests/host/ipc-server.test.ts`): 206/206. Bug-fix-with-test policy satisfied for all three original findings AND the discovered baseUrl splice bug.
**Notes:** Three judgment calls. (a) **Tab (0x09) rejected in header values** — RFC 7230 technically allows horizontal tab in field values, but no sane use case in this codebase emits one and rejecting simplifies the regex. Easy to permit if a real skill ever needs it. (b) **`replaceAll` over a second `String.replace(regex, cb)`** — the two-pass design uses `path.replaceAll('{name}', encoded)` per unique name. Simpler to reason about than a single-pass regex+callback that threads state through a closure, and `{`+`}` aren't regex meta-chars when passed as a string to `replaceAll`. Cost: O(tokens × path.length) vs O(path.length) — negligible for realistic paths (<10 tokens, <200 chars). (c) **Fragment dropped from baseUrl** — if a skill author sets `baseUrl: 'https://api.example.com/v1#anchor'` the anchor won't survive because we build the composed URL from `origin + pathname`. Documented in the inline comment; no known real-world case for fragments in an API `server.url`, and `URLSearchParams` semantics mean anchors wouldn't be correctly appended to request URLs anyway.

## [2026-04-21 17:44] — Task 7.4 security follow-up: redact api_key_query credential in failure logs

**Task:** Spec reviewer flagged that the 7.4 dispatcher (commit `ce1a7256`) logged `finalUrl` verbatim on both fetch-error and non-2xx failure paths. For the `api_key_query` auth scheme the credential lives in the URL query string (`?api_key=<secret>`), so logging the URL leaked the credential on every 4xx/5xx or network failure. The task brief's "NO request body or headers in logs" rule extends in spirit to "no credential-adjacent material in logs" — `api_key_query` is the one scheme where the URL itself is credential-adjacent.
**What I did:** TDD. (1) Wrote 7 failing tests before any code change: 5 unit tests on a hypothetical `redactCredentialsFromUrl` helper (happy redaction, special-chars in value, non-credential params intact, no-query pass-through, unparseable URL pass-through) + 2 integration tests that spy on logger warns during a failing dispatch (non-2xx 401 + closed-port network failure). Integration tests required a `vi.doMock('src/logger.js')` + `vi.resetModules()` + dynamic-import pattern because the dispatcher captures its `logger = getLogger().child(...)` at module-load time and you can't swap the underlying pino destination after the fact. Verified red. (2) Exported `redactCredentialsFromUrl(rawUrl)` from `openapi-dispatcher.ts` — parses the URL, iterates `searchParams.keys()`, replaces value with `***` for any key in `CREDENTIAL_QUERY_KEYS` (a `Set<string>` currently containing only `'api_key'`). Case-insensitive. Returns input unchanged on parse failure so a malformed URL can't crash a `warn()` call site. (3) Wrapped both `url: finalUrl` log payloads (fetch-catch branch + non-2xx branch) with `redactCredentialsFromUrl(finalUrl)`. Left the actual fetched URL untouched — this is purely about log hygiene. (4) Added a prominent code comment at `CREDENTIAL_QUERY_KEYS` noting that any future configurable `api_key_query` parameter name must be added to this allowlist in lockstep, or the redaction will silently miss the new name.
**Files touched:** `src/host/ipc-handlers/openapi-dispatcher.ts`, `tests/host/ipc-handlers/call-tool-openapi.test.ts`, `.claude/journal/host/tool-catalog.md`.
**Outcome:** Success. `npm run build` clean. `tests/host/ipc-handlers/call-tool-openapi.test.ts` 26/26 (was 19, +7 for redaction). Broader targeted: `tests/host/tool-catalog/adapters/` + `tests/host/ipc-handlers/` + `tests/host/ipc-server.test.ts` 203/203. Bug-fix-with-test policy satisfied — integration tests would have caught the original leak (they fail red without the fix, verified).
**Notes:** Three judgment calls. (a) **Allowlist over denylist** — redacted only the specific query-param names we know carry credentials (`api_key`), not every query param. Redacting every param would pollute logs for legitimate ones (`?limit=10` → `?limit=***`) and make failure diagnostics nearly useless. Allowlist + a comment pointing at future configurability is the right tradeoff while we only emit one credential-carrying key. (b) **`url: redactCredentialsFromUrl(finalUrl)` inline at both call sites** rather than a local helper variable — keeps the call sites self-documenting (reader sees at a glance that the URL is sanitized) and matches existing style. A `const logUrl = redactCredentialsFromUrl(finalUrl)` at top of the function would hide the guarantee from whoever scans the log site later. (c) **Integration test via `vi.doMock` + dynamic import** rather than piping pino through a Writable — I tried the Writable approach first (cleaner in principle) but it didn't work because the dispatcher's `const logger = getLogger().child(...)` captures a wrapped pino instance at module-load time; a later `resetLogger()` + `initLogger({stream})` creates a NEW wrapped instance the dispatcher's `logger` variable doesn't see. `vi.resetModules()` + `vi.doMock` gives us a fresh dispatcher module whose `getLogger` is our spy. Worth remembering for any future test that needs to assert on structured log payloads emitted from a module-scoped logger.

## [2026-04-21 17:35] — Task 7.4: dispatch OpenAPI calls in call_tool

**Task:** Phase 7 / Task 7.4 of tool-dispatch-unification — replace the `unsupported_dispatch` branch in `call-tool.ts` with a real OpenAPI dispatcher. Both `mcp` and `openapi` must flow through the same `_select` projection + auto-spill envelope so the agent can't tell which kind it called. New `CallToolOpenApiDispatcher` interface, new default implementation in `openapi-dispatcher.ts`, new required `openApiProvider` field on `CallToolDeps`, new `params: Array<{name, in: 'path'|'query'|'header'}>` field on `OpenApiDispatch` (adapter populates, dispatcher reads).
**What I did:** (1) Extended `OpenApiDispatch` in `src/types/catalog.ts` with `params` (Zod `.default([])`) so operations without parameters are still legal without a schema migration. (2) Updated `src/host/tool-catalog/adapters/openapi.ts` to filter parameters to `path|query|header` and populate `dispatch.params` with `{name, in}` entries in spec-declared order. Cookie params stay skipped upstream. (3) Extended `tests/host/tool-catalog/adapters/openapi.test.ts` — asserted `listPets` has `params: [{name: 'limit', in: 'query'}]`, `getPetByID` has `params: [{name: 'id', in: 'path'}]`, `createPet` has `params: []` (body-only), plus a new mixed-location test covering path + query + header and confirming cookie drops out. (4) Added `CallToolOpenApiDispatcher` interface to `call-tool.ts` with `dispatchOperation({baseUrl, method, path, operationId, credential, authScheme, params, args, ctx})`. Made `openApiProvider` required on `CallToolDeps` with a factory-time guard (matches the 7.3 tightening we did for `fetchOpenApiSpec`). (5) Extracted `projectAndEnvelope(result)` helper inside the handler closure so MCP and OpenAPI branches share the exact same `_select`+spill path, not just the same envelope shape. (6) Replaced the `unsupported_dispatch` branch with a real openapi branch that calls `deps.openApiProvider.dispatchOperation({...tool.dispatch, args: cleanedArgs, ctx})`. (7) Wrote `src/host/ipc-handlers/openapi-dispatcher.ts` (`makeDefaultOpenApiDispatcher({skillCredStore, urlRewrites, fetchImpl?})`) — does path-token substitution with URL-encoding, query param serialization (primitive + repeated-key array), header param routing, auth injection for all four schemes, JSON body serialization with Content-Type, `applyUrlRewrite(baseUrl, ...)` before dispatch, and response parsing (JSON on 2xx; raw text fallback; `OpenAPI call failed: <status> <statusText>: <truncated body>` throw on non-2xx). Never logs request body/headers. (8) Factored out `resolveCredentialValueByEnvName` into new `src/host/skills/skill-cred-resolver.ts` — value-only lookup (scope precedence: user > agent > first match, `process.env` fallback when no DB rows). The existing `resolveMcpAuthHeadersByCredential` builds `Bearer` headers on top, but for OpenAPI we need the raw value (bearer + basic + api_key use different shapes on the same underlying cred). (9) Updated `IPCHandlerOptions` in `ipc-server.ts` with `callToolOpenApiDispatcher` field, gated `call_tool` wiring on all three wires (catalog + mcp + openapi) being present. (10) Wired `callToolOpenApiDispatcher: skillCredStore ? makeDefaultOpenApiDispatcher({skillCredStore, urlRewrites: config.url_rewrites}) : undefined` into `server-init.ts` parallel to the MCP dispatcher block. (11) Wrote `tests/host/ipc-handlers/call-tool-openapi.test.ts` — 19 tests using a real `node:http` mock server (ephemeral port per test) covering happy GET path substitution + URL-encoding, POST with JSON body, primitive + array query params, header params, mixed path+query+body+header, all four auth schemes, credential+authScheme mismatch, missing path param, 4xx + 5xx with status in error, non-JSON 2xx pass-through, URL rewrites, `_select` projection post-dispatch, and auto-spill triggered on an OpenAPI response. (12) Updated existing `call-tool.test.ts` — added `unusedOpenApiProvider` throwing stub threaded into all MCP-only handler constructions (matches the 7.3 `unusedFetchOpenApiSpec` pattern); rewrote the `unsupported_dispatch` test to verify openapi now routes through `openApiProvider`; added a factory-guard test for missing `openApiProvider`. (13) Updated `ipc-server.test.ts` spill-threshold tests with the same throwing stub so they remain MCP-focused.
**Files touched:** `src/types/catalog.ts` (params field), `src/host/tool-catalog/adapters/openapi.ts` (emit params), `src/host/ipc-handlers/call-tool.ts` (handler branch + interface + projection helper), `src/host/ipc-handlers/openapi-dispatcher.ts` (new, 240 LOC), `src/host/skills/skill-cred-resolver.ts` (new, 50 LOC), `src/host/ipc-server.ts` (wire-up), `src/host/server-init.ts` (wire-up), `tests/host/tool-catalog/adapters/openapi.test.ts` (assert params), `tests/host/ipc-handlers/call-tool.test.ts` (stub threading + new tests), `tests/host/ipc-handlers/call-tool-openapi.test.ts` (new, 450 LOC), `tests/host/ipc-server.test.ts` (stub threading).
**Outcome:** Success. `npm run build` clean. Targeted suites: `tests/host/tool-catalog/adapters/` 9/9, `tests/host/ipc-handlers/` 64/64 (was 45, +19 for openapi integration tests), `tests/host/ipc-server.test.ts` 35/35, `tests/host/skills/` suite 247/247. Combined targeted run: 358/358. Full suite: 33 pre-existing macOS `listen EINVAL` socket-path failures unrelated to this change (documented in the 7.2/7.3 journal entries), 2855 passing.
**Notes:** Three judgment calls. (a) **Hardcoded auth names for v1** — `X-API-Key` header name and `api_key` query param name are hardcoded in the dispatcher. Configurable names would land naturally as optional fields on the frontmatter `auth` block (e.g. `header_name: 'Authorization'`), but that touches the schema + adapter + dispatcher and is cleaner as a follow-up after real-world petstore-style skills land. Documented the limitation in the dispatcher's module docblock. (b) **Shared projection helper inside the closure, not extracted to module scope** — `projectAndEnvelope` closes over `selector`, `req.tool`, and `envelope` (which itself closes over `spillThresholdBytes` from deps). Hoisting to module scope would require passing all three as parameters, which is uglier than inlining the closure. Keeps both dispatch branches identical — 8 lines each, zero shape drift. (c) **Credential resolver split** — `resolveMcpAuthHeadersByCredential` returns `{Authorization: 'Bearer <v>'}` in one call, which works for MCP (bearer-only) but not OpenAPI (4 schemes → 4 shapes). Factored out a value-only helper (`resolveCredentialValueByEnvName`) instead of adding a scheme param to the existing function — less churn to existing MCP call sites, no risk of MCP-side regression. The existing `resolveMcpAuthHeadersByCredential` stays as-is and can be rewritten on top of the new helper in a follow-up cleanup if that saves LOC. Not part of this task's scope.

## [2026-04-21 17:25] — Task 7.3 review follow-up: required factory + YAML flow-syntax fallback

**Task:** Code-review flagged two issues on commit 0a92f067. (1) `fetchOpenApiSpec` was declared optional on `PopulateCatalogFromSkillsInput`, rationalized as "avoid test-fixture churn" — reviewer correctly called out that the tradeoff is wrong because a silently missing factory turns every `openapi[]`-declaring skill into zero tools + one log line, which is a compile-time wiring mistake escaping to runtime. `getMcpClient` is required for exactly this reason. (2) `parseSpecContent` branched on the first character (`{`/`[` → JSON, else → YAML), which breaks YAML flow syntax: `{openapi: "3.0.3", ...}` is valid YAML (unquoted keys) but invalid JSON, and the first-char branch threw "Failed to parse JSON spec" without falling through.
**What I did:** (1) Flipped `fetchOpenApiSpec` to required on the interface and rewrote its docstring to reflect the new contract ("symmetry with `getMcpClient`… for tests that don't exercise the openapi path, pass a throwing stub"). (2) Deleted the now-dead `if (!fetchOpenApiSpec)` branch in the openapi loop — type system guarantees the factory. (3) Added a module-level `unusedFetchOpenApiSpec` throwing stub to `catalog-population.test.ts` and wired it into all 9 MCP-only tests; stub never fires because those skills don't declare `openapi[]`. (4) Rewrote `parseSpecContent` to try-JSON-first, fall-through-to-YAML-on-fail, compose both error messages when both fail so skill authors can see whichever is closer to their intent. (5) Added a failing-first TDD test for YAML flow syntax starting with `{` (`{openapi: "3.0.3", info: {title: tiny, ...}, paths: {/ping: {...}}}`); verified red pre-fix, green post-fix.
**Files touched:** `src/host/skills/catalog-population.ts` (interface + dead-branch delete), `src/host/skills/openapi-spec-fetcher.ts` (parseSpecContent rewrite), `tests/host/skills/catalog-population.test.ts` (9 test-fixture updates + 1 shared stub), `tests/host/skills/openapi-spec-fetcher.test.ts` (1 new test).
**Outcome:** Success. `npm run build` clean. `tests/host/skills/ tests/host/tool-catalog/` = 239/239 (was 238, +1 YAML-flow test). Targeted suites (fetcher + population) = 27/27.
**Notes:** The reviewer's framing of the required-vs-optional tradeoff was right and my original reasoning was wrong — test fixtures are cheap, wiring bugs are not. The stub pattern (`async () => { throw }`) is strictly better than an optional-with-runtime-error guard: compile-time safety + zero observability cost (stub never actually runs for MCP-only skills). The YAML-flow-syntax bug was a latent footgun — a skill author converting a JSON spec to YAML would keep the `{` start and only the keys/quoting would change, which is exactly the case that broke. Try-JSON-first-then-YAML matches SwaggerParser's own posture and the `js-yaml`/`yaml` library design (YAML is a superset of JSON 1.2 by spec, so YAML.parse succeeds on valid JSON — but JSON.parse is ~10x faster on strict JSON and gives clearer errors on malformed JSON, so trying it first is still a worthwhile optimization).

## [2026-04-21 17:15] — Task 7.3: wire OpenAPI adapter into catalog population

**Task:** Phase 7 / Task 7.3 of tool-dispatch-unification — parallel loop to the MCP wiring inside `populateCatalogFromSkills` that iterates skill frontmatter `openapi[]` sources, fetches + parses specs, feeds them to `buildOpenApiCatalogTools`, and registers the resulting catalog tools. Per-source failures counted separately from MCP failures (`openApiSourceFailures`), partial builds skip the cache write same policy as MCP.
**What I did:** (1) Extended `PopulateCatalogFromSkillsInput` with an optional injected `fetchOpenApiSpec(skill, source)` factory — same pattern as `getMcpClient` — so the orchestrator stays pure and unit-testable with a `vi.fn()` mock. (2) Added `openApiSourceFailures` as a third counter on `PopulateCatalogResult`; separate from `serverFailures` because MCP vs OpenAPI have distinct debugging paths (different failure modes, different log events). (3) Added the parallel openapi loop after the MCP loop: per-source try/catch wraps fetch + buildOpenApiCatalogTools, per-tool try/catch wraps catalog.register (for dupe-name tolerance). Fires `catalog_populate_openapi_parse_failed` with skill+spec+baseUrl+error. (4) Created `src/host/skills/openapi-spec-fetcher.ts` — the default closure that production wires via `makeDefaultFetchOpenApiSpec({getBareRepoPath, urlRewrites})`. Two branches: `https://…` spec values go direct to `SwaggerParser.dereference(url)`; workspace-relative paths (`./openapi.yaml`, `specs/api.json`) are resolved against the skill's git-repo directory (`.ax/skills/<skill>/<normalized>`), read via `git show refs/heads/main:<path>`, JSON/YAML-parsed, then handed to SwaggerParser as an already-in-memory object. (5) Wrote `tests/host/skills/openapi-spec-fetcher.test.ts` — 8 tests covering workspace-relative + URL branches, JSON+YAML parsing, absolute-path rejection, `..` traversal rejection (direct + nested `specs/../../etc/passwd`), v2 Swagger rejection with a short-circuit check before SwaggerParser runs, and missing-file behavior. (6) Extended `tests/host/skills/catalog-population.test.ts` with 7 new openapi cases: happy path, include filter, exclude filter, auth block round-trip, multiple sources, one-bad-source-doesn't-poison-rest, duplicate-name register failures counted separately, MCP+OpenAPI in same skill, skipped when `ok: false`. (7) Wired `makeDefaultFetchOpenApiSpec` into `server-completions.ts` at the `populateCatalogFromSkills` call site, closing over `deps.agentSkillsDeps.getBareRepoPath(agentId)` and `config.url_rewrites`. Updated the `getOrBuildCatalog` cache-write gating to flag partial when `serverFailures > 0 OR openApiSourceFailures > 0` (same no-cache-on-transient-failure policy). (8) Updated the cache.ts BuildResult docstring to reflect both MCP and OpenAPI sources as valid `partial` triggers.
**Files touched:** `src/host/skills/catalog-population.ts` (extended), `src/host/skills/openapi-spec-fetcher.ts` (new, 150 LOC), `src/host/server-completions.ts` (wire-up), `src/host/tool-catalog/cache.ts` (docstring), `tests/host/skills/catalog-population.test.ts` (+9 tests), `tests/host/skills/openapi-spec-fetcher.test.ts` (new, 8 tests).
**Outcome:** Success. `npm run build` clean. Skills + tool-catalog suites: 238/238 (was 221, +17). Full host suite: same 29 pre-existing macOS `listen EINVAL` failures as prior commits, unrelated to this change.
**Notes:** Three judgment calls. (a) **Proxy vs direct** — MCP catalog discovery fetches direct (host-side, not agent-sandbox egress); matched that posture for OpenAPI spec fetches. Documented in the fetcher file header. The proxy's job is to gate agent egress, not host discovery. (b) **Workspace path resolution via git-show, not safePath** — skills live in a bare git repo, not a real filesystem dir, so `safePath` (which uses `resolve()` against cwd) doesn't fit. Implemented equivalent containment via `posix.normalize` + explicit `../` rejection + absolute-path rejection. Unit-tested direct (`../etc/passwd`) AND nested (`specs/../../../etc/passwd`) traversal attempts. (c) **External $ref** — workspace-relative specs pass the parsed object to SwaggerParser, which means external `$ref: "./other.yaml"` can't be followed (no filesystem context). Decided that's acceptable for v1: skill authors inline their component schemas, same pattern the petstore fixture uses. Can be revisited if we see a real external-ref use case.

## [2026-04-21 17:00] — Task 7.2: OpenAPI adapter (buildOpenApiCatalogTools)

**Task:** Phase 7 / Task 7.2 of tool-dispatch-unification — mirror the MCP adapter for OpenAPI. Pure transform: already-parsed v3 `OpenAPIV3.Document` + skill/baseUrl/auth/filters in → `CatalogTool[]` out. Adapter itself does no I/O; fetching + dereferencing is Task 7.3's job.
**What I did:** (1) Vendored a 4-operation petstore fixture at `tests/fixtures/openapi/petstore-minimal.json` (listPets / createPet / getPetByID / deletePet; covers query params, required path params, JSON requestBody). (2) Wrote `tests/host/tool-catalog/adapters/openapi.test.ts` — 8 tests: one-tool-per-operation, inputSchema shape (parameters + `body`), method uppercased, `path` keeps template braces, `dispatch.operationId` stays camelCase, credential/authScheme round-trip, include filter against bareoperationId, exclude after include, missing operationId skipped with warn, v2 rejected, explicit camelCase→snake_case conversion cases (`listPets`/`getPetByID`/`UserV2_create`). Confirmed red. (3) Installed `@apidevtools/swagger-parser@12.1.0` + `openapi-types@12.1.3` as production deps (used at runtime by Task 7.3's catalog-population). Adapter itself only imports `OpenAPIV3` type from `openapi-types`. (4) Implemented `src/host/tool-catalog/adapters/openapi.ts` — `isOpenApiV3` gate throws on non-3.x input, `toSnakeCase` with two regex transitions handling trailing acronyms (`getPetByID` → `get_pet_by_id`) and `UPPER_run + UPPER_lower` pairs, `buildInputSchema` merges parameters + `requestBody.content['application/json'].schema` (skips cookie params with warn, skips body on `body` name collision with warn), dispatch block populated per spec with `credential` + `authScheme` carried from the optional auth input. (5) Filter pass runs against a parallel `Array<{tool, operationId}>` to match against bare operationId (skill authors write globs for what's in the spec, not our derived snake-case name). `outputSchema` explicitly NOT added — CatalogTool has no such field and the plan's parenthetical mention is a forward-looking note, not a requirement.
**Files touched:** `src/host/tool-catalog/adapters/openapi.ts` (new, 233 LOC), `tests/host/tool-catalog/adapters/openapi.test.ts` (new, 190 LOC), `tests/fixtures/openapi/petstore-minimal.json` (new), `package.json` + `package-lock.json` (deps).
**Outcome:** Success. 8/8 new tests pass; 42/42 across `tests/host/tool-catalog/` (was 34, +8); 221/221 across `tool-catalog/` + `skills/`. `npm run build` clean.
**Notes:** Two judgment calls. (a) `outputSchema` — plan text says "captures outputSchema" but CatalogTool schema has no outputSchema field and adding one crosses the host/agent boundary. Documented the skip in the file docblock; a follow-up task can add it when a concrete consumer materializes. (b) v2 handling — plan says "convert to v3 first or reject"; picked reject with a descriptive error pointing at swagger2openapi. Conversion would add a dep + ongoing fidelity-of-output concerns for a hypothetical user. Easy to add later. Also kept the adapter dep-minimal: it imports only `openapi-types` (type only) + `minimatch` + the shared logger. `@apidevtools/swagger-parser` is NOT imported by the adapter — Task 7.3 will use it upstream for the parse + dereference, passing the already-resolved `OpenAPIV3.Document` here.

## [2026-04-20 17:50] — Fix: git-sidecar revert failed silently on NEW SKILL.md files

**Task:** User reported that installing a new Linear skill still produced the "description: Invalid input; credentials.0.authType: Invalid option" error in the admin UI even though the write-time + commit-time validation layers are in place. The SKILL.md the agent produced had `description:` missing and `authType: apiKey` (camelCase, should be snake_case `api_key`).
**Root cause:** `src/agent/git-sidecar.ts:164-168` (pre-fix) tried to revert .ax/ changes by running `git reset HEAD -- .ax/` then `git checkout -- .ax/`. That works for MODIFIED tracked files — `checkout --` restores them to HEAD content. For brand-new UNTRACKED files (every new-skill case), `git checkout -- <path>` fails silently with "pathspec did not match any file(s)" (we swallow with catch), the file stays in the working tree, and the subsequent `git add -A` on line 176 re-stages it. The rejected SKILL.md commits anyway; the host's skill-state reconciler parses it and surfaces state `invalid` with the raw Zod error — which is what the user sees.
**What I did:** Extracted the revert sequence into an exported `revertAxChanges(opts)` helper and added a third step: `git clean -fd -- .ax/` after the reset + checkout. That removes any untracked .ax/ additions so the revert is actually a revert, not a re-stage. Added `tests/agent/git-sidecar-revert.test.ts` with 4 cases in a real tmp git repo: (1) untracked SKILL.md is removed (was the bug), (2) modified-then-reset SKILL.md restored to HEAD, (3) non-.ax changes untouched, (4) no-op when nothing staged. Verified the test FAILS without the clean step (checked by temporarily removing it — case 1 and case 3 both fail with "file still exists after revert") and PASSES with it.
**Files touched:** src/agent/git-sidecar.ts, tests/agent/git-sidecar-revert.test.ts, .claude/journal/host/tool-catalog.md
**Outcome:** Success. Build clean. `tests/agent/` 433/433 pass (+4 new). Bug-fix-with-test policy satisfied — test would have caught the bug, verified against the old code.
**Notes:** Second-order lesson: when you "revert" via staged-state reset, you also have to account for files that weren't in HEAD to begin with. `git reset HEAD` removes them from the index; `git checkout --` can't restore them because there's no HEAD content to restore; `git clean -fd` is the missing third step. This is the same general class as our 2026-04-19 fix for "defensive writes need defensive reverts" — deleting an effectful pipeline without enumerating each effect. Here the effect was filesystem state, not index state, so the two commands needed to cover different strata.

## [2026-04-20 17:00] — Fix: sessionId placeholder rewrite desynced token ctx from catalogMap

**Task:** Agent running Linear skill via execute_script kept hitting `call_tool("mcp_linear_get_team"): unknown tool` even though `describe_tools(['mcp_linear_get_team'])` in the same turn returned the full schema (resultLen=661). User reported from live kind cluster logs.
**Root cause:** Two-step `sessionId` lifecycle:
  1. `server.ts:349-388` creates turnToken + registers `activeTokens.set(turnToken, {ctx: {sessionId: ORIGINAL, ...}})` — ORIGINAL still contains `:_:` placeholder (e.g. `http:dm:_:alice:t1`) because agentId hasn't been resolved yet.
  2. `server-completions.ts:765` inside `processCompletion` runs `sessionId = rewriteSessionPlaceholder(sessionId, agentId)` — replaces `:_:` with `:<agentId>:`. `catalogMap.set(REWRITTEN, ...)` at line 1429 uses the rewritten form.
  The agent's `http-ipc-client.ts:80-86` stamps `_sessionId: REWRITTEN` on every request (from stdin payload), and `ipc-server.ts:234-240` overrides `ctx.sessionId` with it → catalog lookup hits the rewritten key → `describe_tools` works. The `ax.callTool` preamble posts plain `{action, tool, args}` without the `_sessionId` stamp, so the handler reads `entry.ctx.sessionId = ORIGINAL` → `catalogMap.get(ORIGINAL)` returns undefined → "unknown tool".
**What I did:** Added `updateTurnCtx?: (updates: {sessionId?, agentId?}) => void` to `CompletionDeps`. `server.ts` wires it with `entry.ctx = {...entry.ctx, ...updates}` keyed on the current turnToken. Both rewrite sites in `server-completions.ts` (lines ~683 in-process and ~773 sandbox) now call `deps.updateTurnCtx?.({sessionId, agentId})` immediately after the placeholder rewrite. Regression test added in `tests/host/internal-ipc-route.test.ts` verifying that mutating entry.ctx between two POSTs on the same token is visible to the handler on the second POST (the exact shape of the fix).
**Files touched:** src/host/server-completions.ts, src/host/server.ts, tests/host/internal-ipc-route.test.ts, .claude/journal/host/tool-catalog.md
**Outcome:** Success. `npm run build` clean. `tests/host/internal-ipc-route.test.ts` 6/6 pass. Full host suite: 29 failures all pre-existing macOS `listen EINVAL` socket-path. Agent suite 429/429. The bug only reproduces in k8s mode with a sessionId that contains `:_:` (which is every per-user provisioner-resolved session), so unit coverage + kind-cluster smoke is the right shape.
**Notes:** Token ctx drift vs. per-turn maps is a pattern worth watching — any future per-turn map keyed on sessionId has the same footgun if the token is registered before agentId resolves. The `updateTurnCtx` callback is the single point of reconciliation now; if another per-turn bit gets added, thread it through the same channel.

## [2026-04-20 15:10] — Follow-up: delete latent tool_batch IPC handler

**Task:** Phase 6 follow-up (#131) — delete the `tool_batch` IPC handler. After the Task 6.3 codegen delete, nothing in `src/agent/` emits `tool_batch`; the handler, schema, wiring, and 553-line test file were dead weight. The whole-phase code review explicitly called this out.
**What I did:**
  - Verified zero non-dead callers: `rg dispatchBatch src/` had only the definition; `rg tool_batch src/agent/ tests/agent/` only hit an informational comment + the `knownInternalActions` exemption.
  - Deleted `src/host/ipc-handlers/tool-batch.ts` (281 lines) and `tests/host/ipc-handlers/tool-batch.test.ts` (553 lines).
  - Deleted `ToolBatchSchema` block from `src/ipc-schemas.ts`. The auto-registry picks up the deletion reflectively — no registry edit needed.
  - Deleted from `src/host/ipc-server.ts`: the `createToolBatchHandlers`/`ToolBatchProvider`/`ToolBatchOptions` import (line 19), the `toolBatchProvider` field on `IPCHandlerOptions` (line 85), and the spread-in at line 135.
  - Deleted the 44-line `toolBatchProvider: (providers.mcp || mcpManager) ? {…} : undefined` wiring block (plus 4-line legacy-fallback comment) from `src/host/server-init.ts`. Imports (`callToolOnServer`, `resolveMcpAuthHeaders`, `skillCredStore`) kept — still used by the `call_tool` dispatcher wiring a few lines down.
  - Rewrote the `// Mirrors the server-init.ts toolBatchProvider wiring` comment in `src/host/inprocess.ts:288` to drop the now-broken cross-ref; rationale stands on its own.
  - Deleted `dispatchBatch()` + its three private helpers (`resolveRefs`, `deepResolve`, `evaluatePath`) from `src/host/tool-dispatcher.ts`. Grep confirmed the helpers were only reachable from `dispatchBatch`. Updated the file header comment (line 4) — used to say "Both tool_batch IPC and tool-router.ts… call into this single dispatcher," now says "tool-router.ts (from pi-agent tool_use) calls into this single dispatcher."
  - Removed `'tool_batch',` + its comment from the `knownInternalActions` set in `tests/agent/tool-catalog-sync.test.ts:217`. Required — `IPC_SCHEMAS` no longer contains a `tool_batch` action, so the entry would be an orphan.
  - Deleted the stale `refresh-tools` Playwright mock route (13 lines) from `ui/admin/tests/fixtures.ts:333-345` and simplified the no-longer-needed "matches only the exact shape" preamble comment on the `/skills` mock above it. The refresh-tools endpoint was deleted in Task 6.3.
  - Left `src/agent/execute-script.ts:12` informational comment alone per the task brief (it documents the catalog-era intent — still correct).
**Files touched:**
  - Deleted: `src/host/ipc-handlers/tool-batch.ts`, `tests/host/ipc-handlers/tool-batch.test.ts`.
  - Modified: `src/ipc-schemas.ts`, `src/host/ipc-server.ts`, `src/host/server-init.ts`, `src/host/inprocess.ts`, `src/host/tool-dispatcher.ts`, `tests/agent/tool-catalog-sync.test.ts`, `ui/admin/tests/fixtures.ts`.
**Outcome:** Success. `npm run build` clean. `tests/agent/tool-catalog-sync.test.ts` 8/8 passing (confirms schema-catalog invariant still holds without the exemption). Full `tests/host/` + `tests/agent/` run: 1611 passing, 29 failures — all are pre-existing `listen EINVAL` macOS-tmpdir socket-path failures in `server.test.ts`, `server-history.test.ts`, `server-multimodal.test.ts` (reproduce on clean HEAD, unrelated). Final grep sweep across `src/ tests/ ui/`: the only remaining `tool_batch` hit is the intentional `execute-script.ts:12` comment.
**Notes:** Atomic delete — one commit, one revert window. Now that `tool_batch` is gone, `resolveHeaders` and `getServerMetaByUrl` remain on the `DispatchContext`/`tool-router.ts` path via `call_tool` and `pi-agent tool_use` — nothing to prune there. The next natural cleanup is the `providers.mcp` legacy fallback in `tool-router.ts` and `server-completions.ts`, but that needs a separate review of live usage.

## [2026-04-20 14:35] — Task 6.5: documentation sweep for deleted codegen

**Task:** Sweep skills, docs, lessons, and source comments for references to the now-deleted codegen pipeline (`.ax/tools/`, `toolgen`, `toolModuleIndex`, `tool-index-loader`, `PTC`, `refresh-tools`, `tool-stubs`, `response-wrapping`). Document the new `ax.callTool` + catalog path where relevant.
**What I did:**
  - Rewrote the "Cap'n Web Tool Batching" block in `.claude/skills/ax/SKILL.md` as "Tool Catalog + ax.callTool Bridge" — names the right files (`src/host/tool-catalog/`, `src/host/ipc-handlers/{describe-tools,call-tool}.ts`, `src/agent/execute-script.ts`, `src/agent/tool-catalog.ts`) and the `ax.callTool(name, args, opts?)` bridge.
  - Updated `.claude/skills/ax-host/SKILL.md` file table: replaced the two `src/host/toolgen/*` rows with `describe-tools.ts`, `call-tool.ts`, and `tool-catalog/` rows; flagged `tool-batch.ts` as legacy/scheduled for deletion.
  - Updated `.claude/skills/ax-provider-storage/SKILL.md` + `.claude/skills/ax-persistence/SKILL.md`: marked `tool-stubs.ts` as vestigial (no live consumers).
  - Fixed `skills/skill-creator/SKILL.md` line 123: "no `.ax/tools/<skill>/` commit lands" → "no catalog entries appear on the next turn". Left the three `execute_script` references alone (they're about it being second-class, still accurate).
  - Pruned stale "from toolgen scripts" parenthetical in `src/host/tool-dispatcher.ts:4`. Replaced two "(PTC)" comments in `src/agent/tool-catalog.ts` + `src/agent/mcp-server.ts`. Rewrote three `refresh-tools`-era comment blocks in `src/host/server-completions.ts`, `src/host/skills/catalog-population.ts`, `src/plugins/mcp-manager.ts` to drop the deleted admin-button references.
  - Updated two comment blocks in `tests/plugins/mcp-manager.test.ts` to match.
  - Lessons sweep: in `.claude/lessons/host/entries.md`, marked 3 entries `Status: superseded 2026-04-20` (the codegen-specific ones — agent-UX hints-in-generated-file, response-wrapping module headers, JSDoc-must-match-signature), updated 1 in place to reflect the `ax.callTool` port (defensive guard / no-args), and annotated 4 more with "(Historical: …)" context lines where the meta-lesson survives even though the code doesn't. Same treatment for 1 superseded + 1 annotated in `.claude/lessons/infrastructure/entries.md`. Fixed a missing title and updated the `.ax/tools/` reference in `.claude/lessons/agent/entries.md`.
**Files touched:**
  - Skills: `.claude/skills/ax/SKILL.md`, `.claude/skills/ax-host/SKILL.md`, `.claude/skills/ax-provider-storage/SKILL.md`, `.claude/skills/ax-persistence/SKILL.md`, `skills/skill-creator/SKILL.md`
  - Source comments: `src/host/tool-dispatcher.ts`, `src/host/server-completions.ts`, `src/host/skills/catalog-population.ts`, `src/plugins/mcp-manager.ts`, `src/agent/tool-catalog.ts`, `src/agent/mcp-server.ts`, `tests/plugins/mcp-manager.test.ts`
  - Lessons: `.claude/lessons/host/entries.md`, `.claude/lessons/agent/entries.md`, `.claude/lessons/infrastructure/entries.md`
  - Journal: this file
**Outcome:** Success. `npm run build` clean; `npm test -- tests/agent/` = 429/429 passing. Final grep sweep: remaining hits are either deliberate superseded-markers in lessons, negative-assertion tests (`tests/agent/{local-sandbox,tool-catalog}.test.ts`), append-only journal/plan docs, or a positive "No codegen, no generated `.ax/tools/` files" mention in `ax/SKILL.md`.
**Notes:** `src/providers/storage/tool-stubs.ts` is orphan code (no live consumers) — flagged in the two storage skills as vestigial, but deletion is out of scope for a docs sweep. Similarly `ui/admin/tests/fixtures.ts` has a `refresh-tools` Playwright route mock whose matching endpoint was deleted — also orphan, left as a follow-up cleanup rather than touching UI tests. The task plan explicitly warned against touching `docs/plans/**` and journal entries (both left untouched).

## [2026-04-20 13:55] — Task 6.3 (host side): delete toolgen + tool-module-sync

**Task:** Task 6.3 of tool-dispatch-unification — delete the `.ax/tools/` codegen pipeline. With catalog/ax.callTool now the source-of-truth (Tasks 2-5 + 6.1-6.2), the per-skill codegen that wrote `.ax/tools/<skill>/*.js` at approve time is dead weight. This commit covers the host half.
**What I did:**
  - Deleted `src/host/toolgen/` (codegen.ts, generate-and-cache.ts, index.ts, openapi.ts) + its 5-file test tree under `tests/host/toolgen/`.
  - Deleted `src/host/skills/tool-module-sync.ts` + `tests/host/skills/tool-module-sync.test.ts` — the approve-time + refresh-tools sync driver.
  - Deleted the refresh-tools endpoint (`POST /admin/api/agents/:agentId/skills/:skillName/refresh-tools`) from `server-admin.ts` + its test file `server-admin-refresh-tools.test.ts`. Dropped the now-unused `loadSnapshot` import.
  - Removed `syncToolModules` from `AdminDeps`, `AdminSetupOpts`, `ApproveDeps`, and `HostCore`. Dropped the binding closure in `server-init.ts` (was 11 lines: "workspace provider required… syncToolModulesForSkill(…)"). Dropped the `syncToolModules: core.syncToolModules` pass-through in `server.ts`. Dropped the same field from `server-webhook-admin.ts`'s `setupAdminHandler` destructure/forward.
  - Removed the tool-sync block from `approveSkillSetup()` in `server-admin-skills-helpers.ts` (was steps 10 + the `toolSync`/`toolSyncError` audit fields in step 11). Skills now approve cleanly without any approve-time codegen side effect; the catalog covers discovery per-turn.
  - Pruned test stubs: removed `syncToolModules: async () => { throw … }` from server-admin.test.ts (2 instances), server-admin-oauth-start.test.ts, server-admin-oauth-providers.test.ts. In server-admin-skills.test.ts: deleted the entire `tool-module sync on approval` describe block (6 tests) + the `syncToolModulesMock` fixture threading.
**Files touched:**
  - Deleted: `src/host/toolgen/{codegen,generate-and-cache,index,openapi}.ts`, `src/host/skills/tool-module-sync.ts`, `tests/host/toolgen/*.test.ts` (5 files), `tests/host/skills/tool-module-sync.test.ts`, `tests/host/server-admin-refresh-tools.test.ts`.
  - Modified: `src/host/server-init.ts`, `src/host/server-admin.ts`, `src/host/server-admin-skills-helpers.ts`, `src/host/server-webhook-admin.ts`, `src/host/server.ts`, `tests/host/server-admin.test.ts`, `tests/host/server-admin-skills.test.ts`, `tests/host/server-admin-oauth-start.test.ts`, `tests/host/server-admin-oauth-providers.test.ts`.
**Outcome:** Success. `npm run build` clean, `npx tsc --noEmit` clean. All 4 updated admin test files pass (77/77). Full `tests/host/` run: host-server socket tests fail with pre-existing `listen EINVAL` (macOS tmpdir path-length issue, unrelated — reproduced on clean HEAD). Admin / skills / toolgen-adjacent tests all pass.
**Notes:** The approve-helper change is the subtle one — previously a skill transitioning to `enabled` with MCP servers would get `.ax/tools/<skill>/` committed in the same HTTP request (best-effort, errors swallowed into `toolSyncError`). That side effect is now gone entirely. Per-turn catalog build (`populateCatalogFromSkills`) covers everything the committed tree used to: tool discovery happens each turn against the live MCP server, not against a stale committed snapshot. Skill-cred-store rows still land synchronously (credentials are hard state; codegen was derived state), so the approve flow still blocks until the skill is actually usable.

## [2026-04-19 23:15] — Task 4.4 prep: thread config.url_rewrites into MCP host calls

**Task:** Task 4.4's e2e test needs to redirect skill frontmatter URLs (`https://mock-target.test/mcp/linear`) to the mock server's dynamic port from the host process. `config.url_rewrites` already exists and does this for sandbox-outbound traffic via the web proxy, but MCP calls happen on the HOST and bypass the proxy entirely. Need to apply the same rewrite at MCP dispatch time.
**What I did:**
  - Extracted the existing `rewriteUrl` closure from `src/host/web-proxy.ts` into `src/plugins/url-rewrite.ts` as `applyUrlRewrite(originalUrl, rewrites)`. Kept identical semantics (hostname match, preserve path+query, no-op on empty/undefined rewrites).
  - Migrated `web-proxy.ts` to call the shared helper — single code path, no duplicate implementation.
  - Threaded `urlRewrites?: UrlRewriteMap` into `BuildTurnMcpClientFactoryInput`; applied `applyUrlRewrite(url, urlRewrites)` right before `connectAndListTools` call in the factory closure.
  - Added `config.url_rewrites` wiring in `server-init.ts` call_tool dispatcher adapter: `applyUrlRewrite(meta.url, config.url_rewrites)` before `callToolOnServer`.
  - Added passthrough in `server-completions.ts` where `buildTurnMcpClientFactory` is constructed (catalog build path).
  - 10 unit tests for `applyUrlRewrite` (pass-through, empty map, hostname mismatch, path preservation, query preservation, case-insensitive match, Map-vs-plain-object inputs, malformed URL inputs). 2 additional tests in `tests/host/skills/mcp-client-factory.test.ts` verifying the factory applies rewrites.
**Files touched:** `src/plugins/url-rewrite.ts` (new), `src/host/web-proxy.ts`, `src/host/skills/mcp-client-factory.ts`, `src/host/server-init.ts`, `src/host/server-completions.ts`, `tests/plugins/url-rewrite.test.ts` (new), `tests/host/skills/mcp-client-factory.test.ts`.
**Outcome:** Success. 50 tests pass across url-rewrite + mcp-client-factory + web-proxy (was 48 — added 12 new). `npm run build` clean. Production behavior unchanged — `config.url_rewrites` is unset by default, helper is a pure pass-through.
**Notes:** Kept the helper dependency-free (no logger, no imports besides stdlib URL) so it's reusable from any layer. The Map-or-plain-object acceptance is a minor ergonomics win — `config.url_rewrites` is a plain `Record<string, string>`, web-proxy passed a `Map` historically; helper normalizes both.

## [2026-04-19 21:40] — Task 4.3 review fix: tighten spill wiring

**Task:** Code-quality review on f5354fd6 surfaced 4 minor follow-ups. (1) `measureSerialized` stringified the result and threw it away, then `buildPreview` re-stringified — wasted work on 30 KB payloads and a stale "we stringify once and reuse" comment. (2) `config.tool_dispatch?.spill_threshold_bytes` in `server-init.ts` used optional chaining, but `tool_dispatch` is non-optional per `src/types.ts:149` and the Zod schema fills the default — dead-defensive `?.` violates the no-fallbacks-for-internal-code rule. (3) `randomBytes(4)` = 32 bits; birthday collisions non-trivial over a pod's tool-call lifetime. (4) Both unit suites verified the threshold threading on their own side but nothing caught the drift case where `IPCHandlerOptions.spillThresholdBytes` and `CallToolDeps.spillThresholdBytes` get renamed independently.
**What I did:** (1) Rewrote `measureSerialized` to return `{exceeds, serialized: string | undefined}` — stringify once, reuse. Rewrote `buildPreview(serialized: string)` to take the already-stringified form. Rewired `envelope()` to thread `serialized` through. Shortened the truncation tail to "full response at /tmp/tool-<id>.json" per the review's wording. Added an `|| serialized === undefined` guard so the envelope falls through to `{result}` if JSON.stringify threw — can't preview what we can't serialize, let the IPC layer surface the real error. (2) Dropped the `?.` to `config.tool_dispatch.spill_threshold_bytes`, updated the comment to explain why it's safe. (3) Bumped `randomBytes(4)` → `randomBytes(8)` (16 hex chars = 64 bits), updated the JSDoc + inline comment. All existing test fixtures supply their own `idGenerator` so no assertions on id length needed updating. (4) Added `IPC Handler spill threshold plumbing (Task 4.3)` describe block in `tests/host/ipc-server.test.ts` — 2 tests: (a) `createIPCHandler({ ..., spillThresholdBytes: 100 })` + mock dispatcher returning ~200-byte result → envelope comes back `{ok: true, truncated: true, full, preview}`, no `result` field; (b) option omitted + ~500-byte payload → envelope comes back `{ok: true, result}`, no `truncated` field. Fails loudly if someone renames `spillThresholdBytes` on one side but not the other.
**Files touched:** `src/host/ipc-handlers/call-tool.ts` (rewrote measureSerialized + buildPreview, rewired envelope), `src/host/server-init.ts` (drop `?.`), `src/agent/tools/describe-tools.ts` (randomBytes(8)), `tests/host/ipc-server.test.ts` (+2 e2e tests, +2 imports).
**Outcome:** Success. 179/179 across ipc-handlers + agent/tools + ipc-server (was 177); `npm run build` clean. Full suite: 2800 pass, 34 pre-existing failures (unchanged). No new failures.
**Notes:** The e2e test wires through `createIPCHandler` not `createIPCServer` — no socket setup needed, the handler function itself is the boundary between `IPCHandlerOptions` and `CallToolDeps`. Drift detection is equivalent and the test runs in ~2ms instead of 50ms+ for a socket round-trip. Also confirmed the `resolveCatalog: () => reader` closure form works end-to-end (matches real server-init wiring at line 469) — not just the `catalog: new ToolCatalog()` unit-test form.

## [2026-04-19 21:30] — Task 4.3: auto-spill responses >threshold to /tmp/tool-<id>.json

**Task:** Phase 4.3 of tool-dispatch-unification — stop large `call_tool` responses from blowing up the LLM context window. When the stringified post-projection result exceeds `config.tool_dispatch.spill_threshold_bytes` (default 20 KiB), split the work: host returns a `{truncated, full, preview}` envelope, agent writes `full` to `/tmp/tool-<id>.json` inside the sandbox and hands the LLM a stub `{_truncated, _path, preview}`. Host stays filesystem-free (option A from the plan).
**What I did:** (1) TDD host side — added 5 tests to `call-tool.test.ts`: (a) oversized response returns truncated envelope, (b) under-threshold still returns `{result}`, (c) default threshold (20480) kicks in when not passed, (d) `_select .| length` projection that shrinks a 50KB array to a scalar does NOT trigger truncation (spill check runs AFTER projection), (e) preview is bounded + contains a marker from the raw payload. Ran, confirmed 3 fail (2 passed accidentally because no check existed and everything flowed through as `{result}`). (2) Implemented in `call-tool.ts`: added `spillThresholdBytes?` to `CallToolDeps`, imported `DEFAULT_TOOL_DISPATCH_SPILL_THRESHOLD_BYTES` from `src/config.ts`, added `CallToolTruncatedEnvelope` type, added `measureSerialized`/`buildPreview`/`envelope` helpers. `envelope()` is called at both projection-success AND no-projection exits so the shrink-past-threshold path works. Preview caps at 500 chars + "... [truncated; agent spilled...]" suffix. Byte measurement uses `Buffer.byteLength(JSON.stringify(x), 'utf8')`. Updated file docblock with the new step 5. (3) TDD agent side — added 5 tests to `describe-tools.test.ts`: (a) truncated envelope → spill write + stub with `_truncated: true, _path: /tmp/tool-abc12345.json, preview`, (b) normal `{result}` pass-through (no file write), (c) writeFile throws → `{_spill_failed, result: full}` fallback, (d) id generator threaded through to path, (e) single-arg form still works (backwards compat). Ran, confirmed 3 fail. (4) Implemented in `describe-tools.ts`: added `SpillFs` + `CallToolDeps` interfaces, `defaultFs` wrapping `node:fs/promises.writeFile`, `defaultIdGenerator` using `crypto.randomBytes(4).toString('hex')`. Second arg to `createCallToolTool` is optional so existing callers (runners, unit tests that only test pass-through) don't need to change. Write uses `JSON.stringify(full, null, 2)` for readable `bash cat` output. Updated file docblock to explain option A spill semantics + /tmp lifetime. (5) Threaded config value through: added `spillThresholdBytes?: number` to `IPCHandlerOptions` in `ipc-server.ts`, passed it to `createCallToolHandler`; in `server-init.ts` sourced from `config.tool_dispatch?.spill_threshold_bytes`.
**Files touched:** `src/host/ipc-handlers/call-tool.ts` (spill check, envelope helper, types, docblock), `src/agent/tools/describe-tools.ts` (spill write, deps, defaults, docblock), `src/host/ipc-server.ts` (options field + pass-through), `src/host/server-init.ts` (config wiring), `tests/host/ipc-handlers/call-tool.test.ts` (+5 tests), `tests/agent/tools/describe-tools.test.ts` (+5 tests).
**Outcome:** Success. 177/177 across ipc-handlers + agent/tools + tool-catalog; `npm run build` clean. Full suite: 2798 pass, 34 pre-existing failures (33 macOS Unix-socket EINVAL + 1 `sandbox-isolation > exposes exactly 13 IPC tools` — pre-existing from Phase 3, unrelated). No new failures.
**Notes:** Couple of subtle choices: (a) `envelope()` is a closure inside `createCallToolHandler` so it captures `spillThresholdBytes` once instead of reading `deps.spillThresholdBytes ?? DEFAULT` at each exit site — avoids a defaults-drift bug if someone adds a third exit later. (b) `measureSerialized` returns `{exceeds: false, bytes: 0}` on circular-refs (JSON.stringify throws) rather than spilling — if we can't stringify, we can't write it to disk either; let it fall through and the IPC layer will surface a clearer error at its own serialization step. (c) `CallToolDeps.fs` is a local minimal `{writeFile}` interface, not `Pick<typeof import('node:fs/promises')>` — tests inject a Map without faking the whole Node fs module. (d) `/tmp` is sandbox-local tmpfs per pod; collisions across turns are fine because each turn's agent only needs its own ids to be unique. Kept id at 8 hex chars per the spec. (e) Spill-failure path prefixes the error key with `_spill_failed` (not `error`) so it doesn't collide with the structured-error envelope — the agent can distinguish "host errored" from "host succeeded but agent couldn't persist the spill".

## [2026-04-19 21:10] — Task 4.2: wire `_select` jq projection through call_tool

**Task:** Phase 4.2 of tool-dispatch-unification — turn `_select` from a stripped-and-ignored field into a live jq projection knob. Depends on 4.1's `applyJq` wrapper. Also requires re-enabling schema augmentation in `describe_tools` and re-adding `_select?` to the prompt one-liners, all three of which were deliberately gated behind 4.2 to avoid setting an LLM trap (advertise a projection that doesn't project).
**What I did:** (1) TDD — added 3 call-tool tests (happy path: `.issues | length` → number; malformed `_select: '.['` → `select_failed` kind; empty-string / non-string `_select` → skipped), updated 1 existing test (dropped the "projection is Task 4.2" parenthetical), updated the describe-tools test to assert augmentation is live, updated the render test + agent prompt module test to expect `_select?` in the params list. Ran all 4 test files, confirmed 4 new failures at the right assertions. (2) Replaced `stripSelect(args) -> args` with `extractSelect(args) -> {cleanedArgs, selector}`. Selector is `undefined` when missing/non-string/empty — follows the "don't turn a confused model into a hard failure" rule from the task spec. (3) Restructured the MCP branch in `call-tool.ts` to split dispatch-try-catch from projection-try-catch so a `select_failed` only fires after successful dispatch. Added `'select_failed'` to `CallToolErrorKind`, updated the file-level docblock. (4) Replaced the `augmentSchemaWithSelect` stub in `describe-tools.ts` with the real non-destructive clone implementation from the spec; updated the file-level docblock (swapped "disabled until 4.2" → "enabled; paired with applyJq"). (5) Catalog render: dropped the suppression, appended `.concat('_select?')` to the params list, rewrote the why-comment. (6) Agent `call_tool` description: added the `args._select` hint and the new `select_failed` kind to the error union. (7) Chased two stale comments that would lie after the flip: `src/host/ipc-server.ts:131-137` (describe_tools handler-wiring comment) and `src/agent/tools/describe-tools.ts:58-66` (agent-side call_tool stub docblock).
**Files touched:** `src/host/ipc-handlers/call-tool.ts` (extractSelect, projection stage, new error kind, new docblock), `src/host/ipc-handlers/describe-tools.ts` (augment with `_select`, new docblock), `src/types/catalog-render.ts` (append `_select?`), `src/agent/tool-catalog.ts` (call_tool description), `src/host/ipc-server.ts` + `src/agent/tools/describe-tools.ts` (stale-comment cleanup), `tests/host/ipc-handlers/call-tool.test.ts` (+3 tests, 1 updated), `tests/host/ipc-handlers/describe-tools.test.ts` (augmentation now expected), `tests/host/tool-catalog/render.test.ts` + `tests/agent/prompt/modules/tool-catalog.test.ts` (expect `_select?` in params).
**Outcome:** Success. 176/176 passing across ipc-handlers + tool-catalog + agent tools + prompt modules + types; `npm run build` clean. Full suite: 2788 pass, 34 pre-existing failures (33 macOS Unix-socket EINVAL + 1 `sandbox-isolation > exposes exactly 13 IPC tools` from Tasks 3.3/3.4 landing `call_tool`/`describe_tools` — was already failing on main, unrelated to 4.2).
**Notes:** The `extractSelect` "skip on empty/non-string" rule isn't strictly needed if the IPC Zod schema is strict — but the handler is the last line of defense and the spec explicitly asked for graceful-skip. Cheap to keep. Considered adding a `select_failed` branch that still returns the raw `result` so the agent can salvage — rejected because the error message already gives the selector back, and the spec's 4-error union is cleaner than a 4-error + 1-warning shape.

## [2026-04-19 21:00] — Task 4.1 review fix: harden jq wrapper

**Task:** Code-quality review on 12e61daf surfaced 3 Important issues. (1) Per-chunk `chunk.toString('utf8')` on stdout/stderr corrupts multi-byte codepoints whose UTF-8 bytes straddle a Node `data` event boundary — fine for the ASCII test fixtures, broken the moment Task 4.3 ships real MCP JSON carrying CJK / emoji / accented Latin. (2) `describe.skipIf(!hasJq)` silently green-lights CI if the Dockerfile ever drops jq (hard prod dep per container/agent/Dockerfile:16). (3) Spec promised distinct error messages for 5 failure modes but only "bad selector" was under test — at minimum the timeout + ENOENT branches needed coverage.
**What I did:** (1) Replaced the per-chunk string accumulators with `Buffer[]` arrays and decoded once in `close` via `Buffer.concat(bufs).toString('utf8')`. No decoder state to manage, no codepoint corruption. Added a comment pointing at Task 4.3 as the concrete motivator. (2) Gated the skip on `JQ_TESTS_ALLOW_SKIP=1` — missing jq with no opt-in throws at module load with `'jq not found on PATH — set JQ_TESTS_ALLOW_SKIP=1 to skip locally'`. Verified both paths: `PATH=/node-only vitest` fails loudly; `JQ_TESTS_ALLOW_SKIP=1 PATH=/node-only vitest` skips the whole suite (8 tests skipped, exit 0). (3) Added a timeout test using `def f: f; f` (left-recursive infinite loop jq can't short-circuit — spawn timeout is the only exit path) with a 2s vitest test budget, and an ENOENT test that scope-mutates `process.env.PATH` to `/nonexistent` with finally-restore. Skipped the non-JSON-stdout case per review guidance — unit test would need to mock `spawn` and the branch is pure defensive.
**Files touched:** `src/host/tool-catalog/jq.ts` (buffer-collect rewrite, ~8 LOC swapped), `tests/host/tool-catalog/jq.test.ts` (+fail-loud guard, +2 tests — timeout, ENOENT).
**Outcome:** Success. 33/33 tool-catalog tests pass (was 31, +2); `tsc` clean. Manually verified both `ALLOW_SKIP=1` (clean skip) and no-opt-in (loud throw) paths with `PATH=/nvm/node-only`.
**Notes:** First attempt at testing ALLOW_SKIP ran `PATH=/nonexistent npx vitest` which breaks because `npx` itself vanishes from PATH — switched to a node-only PATH pointing at the nvm bin dir. ENOENT test could theoretically race if vitest worker pool shared env between tests, but `process.env.PATH` mutation is main-thread and vitest runs describe blocks serially inside a file; finally-restore keeps the blast radius to one test.

## [2026-04-19 20:55] — Task 4.1: jq wrapper for `_select` projection

**Task:** Phase 4 of tool-dispatch-unification — add response handling. Task 4.1 is the plumbing: a `applyJq(data, selector)` wrapper that the `call_tool` host handler will use to apply jq filters to MCP response JSON. Must shell out to the already-installed `jq` binary (dockerfile baked it in, no node-jq / jq-wasm) and return 0/1/N outputs as `null` / unwrapped value / array.
**What I did:** (1) TDD — wrote `tests/host/tool-catalog/jq.test.ts` with 6 cases (identity, field access, array length, multi-output collection, zero-output null, malformed-selector stderr surfacing) and `describe.skipIf` on a sync `jq --version` probe so contributors without jq see skip not fail. Ran to confirm all fail at import. (2) Implemented `src/host/tool-catalog/jq.ts` with `node:child_process.spawn('jq', ['-c', selector])` + 500ms timeout via spawn's own `timeout: 500` option (SIGTERM with `code === null` means timeout). Errors have distinct messages: `'jq not found on PATH'` (ENOENT on `error` event), `'jq timed out'` (signal === 'SIGTERM' path), jq stderr first-line on non-zero exit, and `'jq produced non-JSON output: <preview>'` if stdout parse fails. (3) Multi-output handling: `-c` emits one JSON value per line; split on `\n`, drop blanks, `JSON.parse` each. 0 → null, 1 → unwrapped, N → array. (4) `stdin.on('error')` swallows EPIPE via debug-level log (jq can bail on a bad selector before reading stdin; the `close` handler surfaces the real error either way).
**Files touched:** `src/host/tool-catalog/jq.ts` (new, ~105 LOC), `tests/host/tool-catalog/jq.test.ts` (new, 6 tests).
**Outcome:** Success. 6/6 jq tests pass; 31/31 across `tests/host/tool-catalog/` (no regressions); `npm run build` clean. Dev probe confirmed `/opt/homebrew/bin/jq` 1.7.1; container image already has jq.
**Notes:** Caught one tsc error on first build — local `Logger.debug` signature is `(msg: string, details?: Record)` (msg first, details second), opposite of pino's native shape. Fixed by flipping arg order. macOS jq 1.7.1 emits `"jq: error: syntax error, unexpected end of file ... at <top-level>, line 1:"` for `.[` — the test matches on `/syntax/i` to stay stable across jq versions. Didn't wire this into `call_tool` yet; that's Task 4.2's scope.

## [2026-04-19 18:30] — Task 2.5 review fix: OAuth-race + concurrent-miss test

**Task:** Code-quality review on 98871814 flagged two Important-severity issues. (1) OAuth race: `invalidateCatalog` fires while a build is in flight, the invalidate hits an empty store (no-op), the build resolves with pre-OAuth headers, and `store.set()` poisons future turns with stale tools until the next push. Push path was self-healing (HEAD-sha in key), OAuth path is not (credentials change without advancing HEAD). (2) Concurrent-miss test described invariants it didn't enforce — only one `getOrBuildCatalog` call, no actual concurrency.
**What I did:** (1) Added a module-level `generations: Map<agentId, number>` counter to `cache.ts`. `getOrBuildCatalog` seeds the agent's entry to 0 on first build (so `invalidateAllCatalogs` can find it), captures `genAtStart` before awaiting `build`, re-checks after, and skips `store.set()` if it moved. Either invalidate-path bumps: `invalidateCatalog(agentId)` bumps the one agent; `invalidateAllCatalogs()` iterates every known agent + bumps each. The freshly-built array is still returned to the caller — the guard is purely about not poisoning future turns. (2) Rewrote the "concurrent misses" test to actually fire two `getOrBuildCatalog` calls for the same key before either resolves, then asserts both builds ran, each caller got its own result, and the last-writer-wins entry is the one that ended up cached. Added 2 new race tests: invalidate-during-build (targeted) and invalidateAllCatalogs-during-build (broad). Module docstring extended with a note on the stale-entry accumulation (same small leak as snapshotCache — add LRU if RSS ever grows).
**Files touched:** `src/host/tool-catalog/cache.ts` (+generations map, seed-on-first-build, generation checks, expanded docstring), `tests/host/tool-catalog/cache.test.ts` (concurrent-miss test rewritten; +2 new race tests).
**Outcome:** Success. 12/12 cache tests pass; 62/62 across tool-catalog + hook-endpoint + admin-oauth-flow; 1207/1207 host tests (was 1205 pre-fix — +2 new race tests). `tsc --noEmit` clean. Pre-existing 33 macOS-socket failures unchanged.
**Notes:** Took Fix #2 option **A** (rewrite to exercise concurrent misses) — the behavior is worth locking in so a future dedup change shows up as a deliberate test update rather than a silent regression. One observation from implementing the counter: `invalidateAllCatalogs` must bump every agent with an in-flight build, not just agents with cached entries — so `getOrBuildCatalog` seeds the entry to 0 before awaiting. Without that seed, a first-ever build for a fresh agent that races `invalidateAllCatalogs` would slip past the guard (no generation entry, no bump, genAtStart == genAtEnd == 0, write goes through). Flagged the theoretical delimiter-injection and stale-entry-accumulation as documented-only per spec.

## [2026-04-19 18:15] — Task 2.5: Cache catalog per (agentId, userId, HEAD-sha)

**Task:** Phase 2/3 bolt-on of tool-dispatch-unification — the per-turn `populateCatalogFromSkills` was paying ~200–400ms of `listTools` round-trips every single turn, duplicating work already done once per HEAD via `ensureToolsDiscoveredForHead`. Cache the built `CatalogTool[]` so the first turn after a skill HEAD change pays the build cost and every subsequent turn at the same HEAD hits an O(1) Map lookup.
**What I did:** (1) New `src/host/tool-catalog/cache.ts` — module-scoped `Map<string, CatalogTool[]>` keyed `${agentId}:${userId}:${headSha}`, with `getOrBuildCatalog`, `invalidateCatalog(agentId)` (prefix sweep), `invalidateAllCatalogs()`, and `catalogCacheSize()` for tests. No TTL/LRU — invalidation is explicit, entries are small, HEAD changes are infrequent. Value is the array (not `ToolCatalog` instance) because instances carry mutation risk across turns. (2) Rewrote the catalog-build block in `src/host/server-completions.ts:1358–1403` to route through `getOrBuildCatalog`, passing `probeHead(agentId)` for the sha and `currentUserId` for per-user scoping; `loadSnapshot` + `populateCatalogFromSkills` now live inside the `build` closure so they only run on miss. `stdinPayload.catalog` now reads the cached array instead of `toolCatalog.list()`. (3) Wired invalidation in `src/host/skills/hook-endpoint.ts` (alongside `snapshotCache.invalidateAgent` on post-receive) and in `src/host/admin-oauth-flow.ts` (after an admin OAuth callback lands new credentials — auth headers change, so catalog might). (4) 10 tests in `tests/host/tool-catalog/cache.test.ts` covering miss/hit/per-user/per-head/per-agent scoping, invalidate/clear, prefix-safety (`agent-a` ≠ `agent-a-extra`), concurrent-miss shape. (5) 1 test appended to `tests/host/skills/hook-endpoint.test.ts` asserting the hook drops the targeted agent's catalog entries and leaves bystander entries alone.
**Files touched:** `src/host/tool-catalog/cache.ts` (new, 101 LOC), `src/host/server-completions.ts` (catalog block rewritten, +2 imports), `src/host/skills/hook-endpoint.ts` (+1 import, +1 call, +comment), `src/host/admin-oauth-flow.ts` (+1 import, +1 call, +comment), `tests/host/tool-catalog/cache.test.ts` (new, 187 LOC, 10 tests), `tests/host/skills/hook-endpoint.test.ts` (+46 LOC, +1 test).
**Outcome:** Success. 10/10 cache tests pass; 39/39 tool-catalog + hook tests pass; 1205/1205 host tests pass (excluding the 33 pre-existing macOS Unix-socket failures in server.test/server-history/server-multimodal/integration that `git stash` baseline also fails). `tsc --noEmit` clean. Micro-benchmark: simulated 300ms build for miss, <1µs avg per hit over 1000 iterations — confirmed O(1) behaviour.
**Notes:** Key scoping **Option A** (agentId, userId, headSha). `resolveMcpAuthHeaders` in `server-completions.ts:176` is user-scoped (picks the skill_credentials row for the caller), so two users on the same agent + HEAD can see different tool lists — B would leak user-1's tools to user-2. Option C didn't apply because `agentId = sandboxResolvedAgent?.id ?? config.agent_name` is workspace identity, not per-user. Deviated from "mirror snapshotCache" by going module-scoped rather than constructor-injected — the explicit API shape in the spec takes no cache handle, and the invalidation surface is only 2 sites, so singleton felt appropriate. Invalidation sites wired: `hook-endpoint.ts` (post-receive) + `admin-oauth-flow.ts` (OAuth callback success). No others — `snapshotCache` has the same 2 (no skill-approval-specific site because approval commits via workspace provider, which fires the post-receive hook).

## [2026-04-19 17:37] — Task 2.3: Ship catalog to agent via stdin payload

**Task:** Phase 2 / Task 2.3 of tool-dispatch-unification — deliver the per-turn `ToolCatalog` (populated in Task 2.2) to the sandboxed agent via the stdin JSON payload. Type must cross the host/agent boundary without the agent importing from `src/host/`.
**What I did:** (1) Moved `CatalogTool` + `CatalogToolSchema` + `validateCatalogTool` from `src/host/tool-catalog/types.ts` to a new `src/types/catalog.ts` (pure, depends only on `zod`); kept the old path as a re-export so Task 2.1/2.2 call sites stay green without churn. (2) Added `catalog?: CatalogTool[]` to both `AgentConfig` and `StdinPayload` in `src/agent/runner.ts`; extended `parseStdinPayload` to pick up `parsed.catalog` if it's an array (else `undefined`); `applyPayload` sets `config.catalog` when defined. (3) Added `catalog: toolCatalog.list()` to the `JSON.stringify(…)` stdin payload in `src/host/server-completions.ts` right after `skills:`. (4) 3 unit tests in `tests/agent/runner.test.ts` for `parseStdinPayload` (extract / default / ignore-non-array) + 1 in `tests/agent/agent-setup.test.ts` verifying `AgentConfig` accepts the field without breaking prompt construction (rendering lands in Task 2.4).
**Files touched:** `src/types/catalog.ts` (new, 43 LOC), `src/host/tool-catalog/types.ts` (rewritten as 14-LOC re-export), `src/agent/runner.ts` (+10 LOC: import, `AgentConfig.catalog`, `StdinPayload.catalog`, parse branch, applyPayload branch), `src/host/server-completions.ts` (+4 LOC: `catalog: toolCatalog.list()` line + comment), `tests/agent/runner.test.ts` (+35 LOC: 3 tests), `tests/agent/agent-setup.test.ts` (+22 LOC: 1 test).
**Outcome:** Success. 37/37 targeted tests pass under vitest AND bun native; `tsc --noEmit` clean. 33 pre-existing macOS Unix-socket failures in `tests/host/server*.test.ts` + `tests/integration/` unchanged (confirmed via `git stash` baseline).
**Notes:** Chose option (A) — `src/types/catalog.ts` as a new directory. CLAUDE.md says "shared cross-cutting types live in `src/types.ts`" but that file is already large (242 LOC, tied to provider registry); splitting catalog into its own file keeps the import surface clean and leaves room for sibling shared types (e.g. dispatch envelope in Task 2.5+). Re-export route taken over update-all-imports — 5 existing call sites would churn vs. 14 LOC of re-export, and the re-export doubles as a signpost that the types moved. Agent-side parse trusts the shape (no Zod re-validation); host already validates on `ToolCatalog.register` via `validateCatalogTool`, and the JSON roundtrip preserves the object exactly.

## [2026-04-19 17:28] — Task 2.2 review cleanup: extract turn MCP client factory + O(1) URL lookup

**Task:** Follow-up on code-quality review of `571ed362`. Reviewer raised 3 Important findings; fixing #2 (extract helper from `processCompletion`) and #3 (replace per-call snapshot walk with O(1) lookup). #1 (duplicate `listTools` round-trip) and the `ok === false` tolerance are deferred as documented debt.
**What I did:** (1) Created `src/host/skills/mcp-client-factory.ts` with `buildTurnMcpClientFactory({mcpManager, agentId, snapshot, resolveAuthHeaders}) → (skillName, serverName) => CatalogMcpClient`. Builds a `Map<serverName, url>` once at construction from the snapshot; every `listTools()` reads O(1) from the map instead of re-walking. Absent server returns `[]` rather than throwing. Auth resolver is injected rather than imported, to avoid a circular `server-completions.ts` ↔ factory dependency and to keep the factory independently testable. (2) Added `tests/host/skills/mcp-client-factory.test.ts` with 4 unit tests: happy-path delegation + auth fallback, unknown-server no-op (empty array), explicit meta.headers take precedence over the resolver, and parse-failure entries are skipped when building the URL map. `connectAndListTools` is mocked via `vi.mock` so no network is touched. (3) Collapsed the 56-line inline block in `server-completions.ts` to the 15-line call site the reviewer sketched, adjusted imports, and added an inline comment documenting the deferred #1 (duplicate `listTools`) as known efficiency debt to be resolved in Phase 3 when the `.ax/tools/` codegen path retires.
**Files touched:** `src/host/skills/mcp-client-factory.ts` (new, 77 LOC), `tests/host/skills/mcp-client-factory.test.ts` (new, 120 LOC), `src/host/server-completions.ts` (imports swap + -41/+16 in the population block + 12-line debt comment), `.claude/journal/host/tool-catalog.md` (this entry), `.claude/journal/host/index.md` (bullet).
**Outcome:** Success. 4 new factory tests pass; 23/23 across catalog-population + mcp-client-factory + tool-catalog; 169/169 in `tests/host/skills` + `tests/host/tool-catalog`; `tsc --noEmit` clean.
**Notes:** Route taken for Fix #3 — per-factory `Map` (not `url` on `getServerMeta`). `getServerMeta` has 3 test files covering it and a public-shape contract in `McpConnectionManager`; the factory Map is ~10 LOC confined to one file and avoids touching those. The reviewer left this as a judgment call and asked for the smaller diff. The injected `resolveAuthHeaders` closure also dodges the latent circular import that would have appeared if the factory imported `resolveMcpAuthHeaders` from `server-completions.ts` directly (which imports the factory).

## [2026-04-19 17:20] — Task 2.2: Populate catalog from MCP servers at session start

**Task:** Phase 2 / Task 2.2 of tool-dispatch-unification — add the session-bootstrap hook that walks the skill snapshot, calls `listTools()` per declared MCP server, runs the results through `buildMcpCatalogTools`, and populates a per-turn `ToolCatalog`. Runs in parallel with the legacy `.ax/tools/` codegen; agent still uses the legacy path.
**What I did:** (1) Added optional `include`/`exclude` string-array fields to `McpServerSchema` in `frontmatter-schema.ts` so skill authors can scope the catalog surface before we ship Phase 2 to the agent. (2) Wrote 6 tests in `tests/host/skills/catalog-population.test.ts` (two from the plan, plus exclude-filter / error-isolation / multi-skill / empty-servers coverage). (3) Implemented `populateCatalogFromSkills` in `src/host/skills/catalog-population.ts` — iterates snapshot → frontmatter.mcpServers → injected `getMcpClient().listTools()` → `buildMcpCatalogTools` → `catalog.register`, with per-server try/catch so one flaky server doesn't poison the whole catalog. Logs failures via `getLogger().child({component: 'catalog-population'})`. (4) Wired into `server-completions.ts` right after the existing `ensureToolsDiscoveredForHead` block — builds `new ToolCatalog()`, supplies a `getMcpClient` closure that looks up URL/transport/headers from `mcpManager.getServerMeta` + `resolveMcpAuthHeaders`, calls `connectAndListTools`, then `catalog.freeze()` after population. Error-swallowed at the population level too so a catalog-build failure never refuses a turn while the agent is still on the legacy codegen path.
**Files touched:** `src/host/skills/catalog-population.ts` (new, 96 LOC), `tests/host/skills/catalog-population.test.ts` (new, 108 LOC), `src/host/skills/frontmatter-schema.ts` (+14 LOC: include/exclude fields on `McpServerSchema`), `src/host/server-completions.ts` (+56 LOC: imports + catalog population block), `.claude/journal/host/tool-catalog.md` (this entry).
**Outcome:** Success. 6/6 new tests pass; 165/165 in `tests/host/skills/` + `tests/host/tool-catalog/` pass; `tsc --noEmit` clean. Session-level test (Step 6) skipped per brief's fallback — existing `tests/host/server.test.ts` pre-existing macOS socket failures (21/21) make that surface expensive to extend meaningfully; unit coverage is the hard requirement.
**Notes:** Tolerance choice — `entry.ok === false` skips parse failures but accepts the plan's loose fixtures that omit `ok` entirely (the test inputs used `{name, frontmatter} as never` without an `ok` field). The real `SkillSnapshotEntry` always has `ok: true` when `frontmatter` is present; the looser check keeps the unit tests honest without taking on a test-only type narrowing in the production code. Parallel-path state — both `.ax/tools/` codegen and the new catalog populate the same info; Task 2.3 ships `catalog.list()` via stdin, Task 2.4 renders it in the system prompt, Phase 3 retires the codegen path. The catalog is built per-turn rather than cached on the session entry because skills/approvals can change between turns and the snapshot cache already handles the hot-path dedup.

## [2026-04-19 17:08] — Task 2.1: MCP adapter (buildMcpCatalogTools)

**Task:** Phase 2 / Task 2.1 of tool-dispatch-unification — write the pure-transformation adapter that converts MCP tool-list input (name, description, inputSchema) into `CatalogTool` entries, applying optional include/exclude glob filters.
**What I did:** Wrote the 3 failing tests verbatim from the plan, confirmed FAIL (missing module), then implemented `buildMcpCatalogTools` per the reference — filters with `minimatch`, maps to `{ name: mcp_<skill>_<toolName>, skill, summary, schema, dispatch: { kind: 'mcp', server, toolName } }`. Description falls back to tool name; schema falls back to `{ type: 'object' }`.
**Files touched:** `src/host/tool-catalog/adapters/mcp.ts` (new, 32 LOC), `tests/host/tool-catalog/adapters/mcp.test.ts` (new, 34 LOC).
**Outcome:** Success. 3/3 tests pass. `tsc --noEmit` clean.
**Notes:** Adapter stays oblivious to downstream validation — `CatalogToolSchema` enforces `^(mcp|api)_[a-z0-9_]+$`, so if a skill or tool name ever carries disallowed characters, the registry will reject at insert time rather than here. Keeps the adapter a thin functional transform.

## [2026-04-19 16:59] — Task 1.3: One-liner prompt render helper

**Task:** Phase 1 / Task 1.3 of tool-dispatch-unification — add `renderCatalogOneLiners` to produce the "## Available tools" block that Phase 2 Task 2.4 will inject into the agent system prompt.
**What I did:** Wrote the two failing tests from the plan, confirmed FAIL, then implemented `renderCatalogOneLiners` per the reference — groups tools by skill, emits `### <skill>` headers and `- name(params) — summary` one-liners, marking optional params with `?` and always appending `_select?` (jq projection hint). Returns `''` for an empty catalog, otherwise rendered block with trailing whitespace stripped.
**Files touched:** `src/host/tool-catalog/render.ts` (new, 26 LOC), `tests/host/tool-catalog/render.test.ts` (new, 27 LOC).
**Outcome:** Success. 2/2 tests pass. `tsc --noEmit` clean.
**Notes:** The plan's test fixture asserted `(team, state?) —` but the reference impl always appends `_select?`, so the real output is `(team, state?, _select?) —`. Plan explicitly calls `_select?` intentional, so this is a test-fixture bug. Fixed the assertions to include `_select?` rather than silently dropping it from the implementation. Keeps the design-doc contract intact.

## [2026-04-19 16:56] — Task 1.2: Session-scoped ToolCatalog registry

**Task:** Phase 1 / Task 1.2 of tool-dispatch-unification — build the session-scoped tool registry on top of the `CatalogTool` type from Task 1.1.
**What I did:** Wrote 5 failing tests (register/get, duplicate rejection, insertion-order list, listBySkill filter, freeze semantics), then implemented `ToolCatalog` as a thin wrapper around a `Map<string, CatalogTool>`. Validates every input through `validateCatalogTool` before insertion; `freeze()` flips a one-way boolean checked before any write.
**Files touched:** `src/host/tool-catalog/registry.ts` (new, 33 LOC), `tests/host/tool-catalog/registry.test.ts` (new, 55 LOC).
**Outcome:** Success. 5/5 tests pass. `tsc --noEmit` clean.
**Notes:** `Map` iteration order = insertion order in ES2015+, so no extra bookkeeping is needed for the ordering guarantee. Frozen check intentionally precedes validation so sealed catalogs don't waste cycles parsing bad input.

## [2026-04-19 16:52] — Task 1.1: CatalogTool shape + validator

**Task:** Kick off tool-dispatch-unification Phase 1 — define the `CatalogTool` TS type and a Zod-based `validateCatalogTool` entry point. Pure data layer, no wiring.
**What I did:** Added `src/host/tool-catalog/types.ts` with the `CatalogToolSchema` (discriminated union over `mcp` and `openapi` dispatch kinds) and exported `CatalogTool` + `validateCatalogTool`. Drove from three tests in `tests/host/tool-catalog/types.test.ts`.
**Files touched:** `src/host/tool-catalog/types.ts` (new, 34 LOC), `tests/host/tool-catalog/types.test.ts` (new, 29 LOC).
**Outcome:** Success. 3/3 tests pass. `tsc --noEmit` clean.
**Notes:** The plan's spec used `z.record(z.unknown())` (Zod-3 signature). Under Zod 4 that silently produces a broken schema (`Cannot read properties of undefined (reading '_zod')` at parse time). Fixed with `z.record(z.string(), z.unknown())` — Zod 4 requires explicit key + value types. Worth remembering for future schema work in this codebase.

