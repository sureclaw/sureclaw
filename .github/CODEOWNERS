# Security-critical paths require explicit review

# Provider allowlist — controls what code can be loaded
src/provider-map.ts         @ax/security

# Path traversal protection
src/utils/safe-path.ts      @ax/security

# IPC boundary — the critical trust boundary
src/ipc.ts                  @ax/security
src/ipc-schemas.ts          @ax/security

# Taint budget enforcement
src/taint-budget.ts         @ax/security

# Sandbox providers — kernel-level isolation
src/providers/sandbox-*     @ax/security

# Credential providers — secret handling
src/providers/creds-*       @ax/security

# Policies — sandbox profiles
policies/                   @ax/security

# Security docs
SECURITY.md                 @ax/security
docs/plans/ax-security-hardening-spec.md @ax/security

# CI/CD — supply chain
.github/                    @ax/security
